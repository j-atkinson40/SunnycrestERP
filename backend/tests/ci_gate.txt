# Scoped CI test gate — the curated set of test files known GREEN against a
# fresh migrated DB (alembic upgrade head, NO seed), one path per line.
#
# WHY SCOPED, not the whole suite: an S-5 CI-clean investigation ran all 5145
# backend tests against a fresh migrated DB and found ~220 non-passing —
# dominated by seed-dependence (~106 tests `.one()` on rows only a seed
# creates) and SQLite-schema tests (9, running against a non-Postgres schema).
# Those are a "how many of our tests are real" problem, not a "make CI green"
# problem, and are routed to a dedicated CI-clean arc (see STATE.md). A
# full-suite gate would be red on day one and get bypassed; this gate is green
# and enforces the arc's work + the ratchets NOW. GROW this list as files are
# made CI-clean — never add a file that isn't green here.

# — Accounting Substrate Arc (period lock, service seams, Plaid, EPD) —
tests/test_plaid_sync_loud_failure.py
tests/test_journal_entry_characterization.py
tests/test_reconciliation_matching_characterization.py
tests/test_reconciliation_matching_rework.py

# — Ratchets —
tests/test_route_write_ratchet.py

# — Scheduler isolation (S-1c) + its neighbor (both teardown-fixed in S-5) —
tests/test_workflow_scheduler_pair_isolation.py
tests/test_workflow_scheduler_scheduled_dispatch.py

# — Tenant wipe tool (W-1b): derivation + three guards (against the REAL
#   preserve-list) + full throwaway-tenant wipe lifecycle. Creates its own
#   tenant, cleans up after itself (COMPANY-LITTER ratchet). —
tests/test_wipe_tenant.py

# — Books Review Phase 2 A-1b: reconciliation exception model (r148) — schema
#   pins (candidates key to txn, no match_status copy, uniqueness, CHECK, cascade). —
tests/test_reconciliation_exception_model.py

# — Books Review Phase 2 A-2: durable non-destructive scoring — candidate
#   persistence, near-amount band, invoice/bill EPD fallback, exception
#   creation, no-double-clear claim set, idempotent re-run. —
tests/test_reconciliation_scoring_a2.py

# — Books Review Phase 2 A-3: durable payment-claim table (r149) — UNIQUE(payment_id),
#   claim-table pool exclusion, race-loser (SAVEPOINT + IntegrityError → not swallowed),
#   period-lock gate, cascade release. —
tests/test_reconciliation_claim_a3.py

# — Books Review Arc B B-1: Plaid counterparty persisted raw (r150) — merchant_name /
#   merchant_entity_id / counterparties[] landed by sync, description back-compat kept. —
tests/test_plaid_counterparty_b1.py

# — Books Review Arc B B-3: reconciliation review queue — source builder filters on
#   the source transaction's match_status (not exception.resolved), candidates via
#   Option A, Accept commits the SELECTED candidate reusing _try_claim, manual==auto
#   state parity, period-lock gate, coding branch. —
tests/test_reconciliation_review_b3.py

# — Books Review Arc B B-4: flag/park (r151) — discriminated return_trigger_kind,
#   flag_id FK, ask→task_completed subscriber, hold→doc-attach hook, terminal→run
#   summary (adjustment), same-exception reopen, no scheduler. —
tests/test_reconciliation_flags_b4.py

# — Books Review Arc B B-5: one-to-many producer (r152 CHECK) — subset-sum k<=3
#   exact-total (exclusions), payment_group candidate, synthetic id, all-or-none
#   accept (member pre-claimed → zero claims, txn stays unmatched). —
tests/test_reconciliation_one_to_many_b5.py

# — Books Review Arc B B-6: end-to-end through the assembled engine — queue builds
#   + candidates reach extras; both card forms have data; Accept (single + group)
#   leaves the queue + queue_count agrees; Flag ask→Task→park→complete→return. —
tests/test_books_review_e2e_b6.py

# — Queue-count perf C-1: the membership seam (ss_cert_triage) — count and build
#   share one membership expression + cannot disagree; snooze is a SQL anti-join
#   matching the legacy post-filter; expired snooze ignored; tenant isolation. —
tests/test_queue_count_membership_c1.py

# — Queue-count perf C-2: count == build membership, all 10 converted builders
#   (task/cash_receipts/month_end/ar_collections/expense/aftercare/catalog/
#   safety/workflow_review/reconciliation); aftercare excludes null/empty
#   entity_id in both paths. —
tests/test_queue_count_membership_c2.py

# — Queue-count perf C-3: email_unclassified window-function membership — SQL
#   membership == the Python list_unclassified reference (diff empty, identity
#   proof); count is exact + uncapped while the build stays display-capped at 50
#   (so count != len(build) at >50, by design); display shape + ordering. —
tests/test_queue_count_membership_c3.py

# — Queue-count perf C-4: batched counts_for_user — equivalent to per-queue
#   queue_count (anti-divergence), queue_ids subset + configs= reuse, and issues
#   materially fewer SQL statements than the per-queue loop (floor hoisted). —
tests/test_queue_count_membership_c4.py

# — Queue-count perf C-5: new count preserves the OLD path's numbers — new ==
#   reconstructed pre-C-1 count (materialize + snooze) per queue;
#   email_unclassified is the documented uncapped-vs-capped exception at >50. —
tests/test_queue_count_membership_c5.py

# — Ledger Posting arc L-1: contra config — r153 FK (financial_accounts.gl_account_id
#   → tenant_gl_mappings), keyword + contra GL resolvers fail closed on
#   unmapped/dangling/foreign, and update_account no longer nulls an omitted field
#   (C-1 hasattr fix) while honoring an explicit null. —
tests/test_reconciliation_gl_l1.py

# — Ledger Posting arc L-2: keyword rows book a balanced draft JE (r154) or do not
#   clear. The cleared_total arithmetic is hand-pinned BOTH ways (configured →
#   clears, unconfigured → clears nothing, partial → per-row), the ledger's cash
#   leg is netted against platform_cleared_balance, the four blocked reasons stay
#   distinct, and a blocked keyword row reaches Books Review as a CONFIG card
#   rather than falling through to the coding card. —
tests/test_reconciliation_gl_l2.py

# — Ledger Posting arc L-2.1a: the financial-accounts form's round trip. The form
#   sends every optional field as `x || null`, and exclude_unset reads an explicit
#   null as a deliberate clear — so a column the form SENDS but cannot HYDRATE is
#   wiped on the next save. Pins that list_accounts surfaces what the form must
#   hydrate, that an untouched edit preserves it, and that an explicit null still
#   clears (the clearing path gl_account_id depends on).
#   L-2.1b adds the other half of the same write contract: gl_account_id is
#   validated at the boundary through validate_gl_account, because the r153 FK
#   gives existence only — a FOREIGN-TENANT mapping id satisfied it and was
#   written. Foreign and nonexistent must read identically so the error is not a
#   cross-tenant existence oracle. —
tests/test_reconciliation_account_form.py

# — Ledger Posting arc L-3: a CODED row books, or it does not clear. The last
#   place a human could retire a bank line against nothing — the coding accept —
#   now posts a balanced two-legged draft JE (operator's account against the
#   bank's contra) before the row leaves the queue. Pins the arithmetic BOTH
#   directions as hand-computed literals, asserts the platform journal_entries
#   count is unchanged on every refusal path (no account / contra unset / contra
#   dangling / locked period / already-booked), carries the L-2.1b existence-
#   oracle discipline to this boundary (foreign and nonexistent byte-identical,
#   inactive named), and pins that an OMITTED note does not clear match_notes —
#   Plaid's bank-retraction marker lives there. —
tests/test_reconciliation_coded_l3.py

# — AR-0: the EPD AR-account resolver. `_find_ar_account` matched
#   platform_category ILIKE '%ar%' — substring, free-text column, .first(), no
#   ORDER BY, wrapped in a bare except. On sunnycrest's real 224-row chart NOT
#   ONE of the nine categories contains "ar", so it returned None on every call
#   and the caller's `ar_account_id or gl_account_id` fallback booked BOTH legs
#   to the discount account: balanced, posted, recording nothing. Replaced by an
#   explicit configured id (settings `accounting_gl.ar`) validated at use through
#   require_gl_account — the L-2.2 single definition, no third check. Pins the
#   substring class as unreachable, both refusal paths posting nothing, and the
#   AR-0c primitive guards (unbalanced + all-legs-one-account). Also pins, NOT
#   fixed, that CustomerPayment has no discount columns at all — seven attributes
#   the EPD path writes are unmapped and vanish on commit. —
tests/test_epd_ar_account_ar0.py

# — AR-1 C-1: the Sage import posts to AR. `import_open_invoices` created
#   invoices at status sent/overdue — ISSUED — and never called
#   post_invoice_to_ar, so every imported invoice was visible to aging, to the
#   financials board and to the nightly sweeper's computed total while
#   current_balance stayed put. The sweeper then wrote that drift away with an
#   alert that has never fired. Pins that an imported invoice moves the balance,
#   that a re-run skip does not post twice, and that the drift is now zero. Also
#   pins, NOT fixed, that a NEGATIVE Sage balance (a customer credit) is
#   imported as a POSITIVE receivable — sign inverted — which is why credit rows
#   are deliberately not posted and are surfaced as import warnings instead. —
tests/test_ar_import_posts_c1.py

# — AR-1 C-2: the AR drift sweeper REPORTS before it corrects. Three defects
#   went in and two were invisible by construction: generate_insight was called
#   with a signature it does not have (TypeError into a bare except, zero rows in
#   behavioral_insights on dev AND production), and money was compared as floats
#   in the one job whose purpose is comparing two money values. The correction is
#   RETAINED deliberately — removing it while the only report destination had
#   never rendered a row would leave drift neither fixed nor seen — but the
#   BEFORE-value is now captured first, as one AgentAnomaly per drifting customer
#   plus the structured set on the job's report_payload, because the correction
#   destroys the primary evidence. NOT a Books Review exception: that model
#   requires a transaction and a run an AR drift does not have. Phase 4 removes
#   the correction once a triage queue exists to receive it. —
tests/test_ar_drift_report_c2.py

# — AR-1 C-3: ONE membership expression, ONE value expression. Five distinct
#   status filters and three different term-counts were in use; an `open`
#   invoice was counted by the board and the sweeper and INVISIBLE TO AGING, and
#   a `posted` finance charge was invisible to everything but the sweeper.
#   Membership is now an EXCLUSION — draft, void — because an inclusion list is
#   how those two dropped out; `paid` and `write_off` are members that self-zero,
#   which is why the list is only two long. Value is Invoice.balance_remaining,
#   now a hybrid_property so Python and SQL are the SAME expression rather than
#   two kept in step by a comment. Synthetic proof, deliberately: production has
#   no credits, no write-offs, no `open` and no `posted`, so a production diff
#   would print zeros. Pins the partial write-off — the case only the terms can
#   fix, since it keeps its ordinary status. —
tests/test_ar_membership_seam_c3.py

# — AR-1 C-4: validation across the arc, through the REAL readers rather than
#   reconstructions of them. One customer, nine invoices covering every status
#   and every term, hand-computed once to 2592.50, and get_ar_aging, the
#   consolidated expression, the hybrid's Python branch and the sweeper's own
#   computation each asked independently whether they agree. Plus the end-to-end
#   import → balance → no-drift path. AND the honest clause: the sweeper REPORTS
#   AND STILL CORRECTS, pinned as the phase-3 state with
#   report_payload["corrections_applied"] is True, so nobody reads "drift
#   reports" as "the rewrite stopped". Phase 4 flips it deliberately. —
tests/test_ar_consolidation_validation_c4.py
