fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.1
pydantic[email]==2.10.4
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
cryptography>=42.0  # explicit pin — Fernet at-rest encryption (Plaid tokens + credential_service); was transitive-only via python-jose
passlib[bcrypt]==1.7.4
# PINNED EXACTLY, NOT RANGED — and the reason is the whole point.
# This was `anthropic>=0.42.0`: a floor with no ceiling. On 2026-08-23 14:09:25 UTC a
# rebuild resolved that to 1.0.0, a major release that removed `temperature`, `top_p`,
# and `top_k` from `Messages.create()`. Every call through
# intelligence_service._call_anthropic (which passes `temperature` unconditionally)
# died with TypeError for two days.
#
# Nothing in this repo changed. This file was untouched since 18 Jul; the last push was
# 20 Aug. The failure had no author — a rebuild took a major version because the
# constraint permitted it. That is why this is `==` and not `<1.0.0`: a narrower range
# is the same mechanism with a smaller window.
#
# 0.125.0 verified against the complete import surface this codebase uses —
# Anthropic, AsyncAnthropic, APIError, RateLimitError — plus temperature/top_p/top_k
# present in the Messages.create signature.
anthropic==0.125.0
python-multipart>=0.0.18
redis>=5.0.0
requests>=2.31.0
twilio>=9.0.0
beautifulsoup4>=4.12.0
httpx>=0.27.0
# iCalendar (RFC 5545) + iTIP (RFC 5546) composition + parsing —
# Calendar primitive Step 3 outbound + iTIP REPLY ingestion.
icalendar~=6.1
PyMuPDF>=1.24.0
openpyxl>=3.1.0
pdfplumber>=0.10.0
python-docx>=1.1.0
apscheduler>=3.10.0
resend>=2.0.0
weasyprint>=60.0
jinja2>=3.1.0
boto3>=1.34.0
dropbox>=12.0.0
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.114.0
Pillow>=10.0.0
numpy>=1.26.0
sse-starlette>=1.6.0,<2.0.0
playwright>=1.40.0
bleach>=6.1.0
tinycss2>=1.5.0
