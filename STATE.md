# Bridgeable — Current State

Single source of truth for what is true RIGHT NOW. Updated by Sonnet at the end of every build session. Canon lives elsewhere — see read order in CLAUDE.md.

## Recent investigation (uncommitted)

- **2026-05-20 — `docs/investigations/2026-05-20-free-form-focus-canvas.md` filed (uncommitted): locks FF-series substrate decisions across 40 questions; decomposes into 7 sub-arcs; flags 3 architectural risks; ready for FF-1 dispatch.** Pre-flight: HEAD verified `0fa9ce1` per process canon. Investigation answers all 40 architectural questions from the dispatch prompt (37 LOCKED, 3 DEFERRED-WITH-REASON across Q-32/Q-33/Q-34/Q-35/Q-36/Q-37 substrate concerns). Three architectural risks surfaced with locked mitigations: (a) **Q-38 deploy-race** between FF-1 backend validator extension + FF-2 frontend canvas substrate → mitigated by sequential dispatch (FF-1 ships strictly before FF-2; validator extension is purely additive — existing grid placements stay valid, new free-form placements newly-accepted); (b) **Q-39 per-tenant variability of canvas dimensions** → mitigated by per-template fixed canvas dimensions baked into `canvas_config` JSONB (default 1200×800, per-template override is low-priority follow-up); (c) **Q-40 JSDOM weakness for drag/resize gesture testing** vs operator-observable canon → mitigated by @dnd-kit KeyboardSensor + direct hook-invocation integration tests in FF-3/FF-4 + Playwright pointer-event gate in FF-7. Seven sub-arcs decomposed at ~8,000 midpoint LOC / ~11,500 worst-case: **FF-1** backend validator + adapter extension (~800-1200 LOC, JSONB-only, no DDL migration — placement schema lives in `focus_templates.rows` JSONB); **FF-2** canvas substrate replacement (WidgetRowsLayer → WidgetFreeFormLayer + PlacedWidgetCore extraction, ~1500-2200 LOC); **FF-3** drag-to-move via @dnd-kit (~1200-1800 LOC); **FF-4** resize-to-resize via 8 handles with shift-modifier aspect-ratio preservation (~1200-1800 LOC); **FF-5** z-index + send-to-front/back affordances (~800-1200 LOC); **FF-6** inspector positioning fields with bidirectional canvas-inspector sync (~600-900 LOC); **FF-7** polish + verification gate (snap-to-alignment per Q-11 (b), keyboard nudge per Q-12, multi-select on canvas per Q-16/17, Playwright pointer-event coverage, staging verification, ~1500-2400 LOC). Key locked decisions: positioning unit = **pixels** (Q-1); canvas dimensions = **fixed 1200×800 per template via `canvas_config` JSONB** (Q-2); positioning shape = **separate top-level fields** `x`/`y`/`width`/`height`/`z_index` parallel to existing `starting_column`/`column_span`, both shapes co-exist in same column via conditional validator branch (Q-3); default drop position = **centered on cursor** (Q-4); per-widget `defaultDimensions` from registry metadata with **320×180 platform fallback** (Q-5); explicit per-placement `z_index` (Q-6); selection does **NOT** promote z-order — independent (Q-7); no special overlap treatment, z-stack governs (Q-8); drag-anywhere-on-body initiates drag with **3px threshold** (Q-9); **8 resize handles** on-selection-only with Shift for aspect-ratio preservation (Q-10); Figma-style invisible snap helpers deferred to FF-7 (Q-11); keyboard nudge 1px/Shift-10px deferred to FF-7 (Q-12); per-widget `minDimensions` with **80×40 fallback** (Q-13); canvas-bounded maximum (Q-14); single-select model **preserved unchanged** from F-2 (Q-15); shift+click + marquee multi-select on canvas in FF-7 (Q-16); move-together + align affordances, NO distribute (Q-17); align affordances only in multi-select inspector — **NO multi-edit** (Q-18); selection clears on subject change (Q-19); core anchored at canonical position computed from canvas dimensions + `default_column_span` (Q-20); core size data-derived (Q-21); widgets **CAN overlap core**, z-index governs — overlap permitted (Q-22); **pure JSONB extension, no DDL migration** (Q-23); **NO migration** of existing F-series grid templates — both shapes co-exist (Q-24); adapter extended **1:1 with positioning fields** per F-3.1a pattern (Q-25); existing debounced save pipeline carries forward unchanged (Q-26); **per-tenant three-scope inheritance unchanged** for FF-series, per-user/per-Focus-instance personalization layer deferred (Q-27); WidgetRowsLayer **preserved as library component** for future Monitor canvas (e.g., Page Builder); FocusBuilderCanvas swaps to WidgetFreeFormLayer (Q-28); **PlacedWidgetCore extraction** — shared inner wrapper for chrome+selection+click handlers; PlacedWidget (grid) and FreeFormPlacedWidget (absolute) are thin positioning shells (Q-29); inspector exposes **editable positioning fields + bidirectional canvas sync** (Q-30); z-index in **both inspector + right-click context menu + keyboard shortcuts** (Q-31). **Q-37 critical distinction flagged**: canvas widget multi-select (Q-16, ships FF-7) vs Focus item data multi-select (different substrate concern, belongs to Focus runtime arc, NOT FF-series). Migration head unchanged at `r103_focus_templates_edit_session`; FF-series adds **zero DDL migrations**. Adapter pattern from F-3.1a remains canonical; field set extended. F-series's placement structural immutability for cores canon, unified-placement model, and ≥3 configurableProps registry contract **all inherit forward unchanged**. **Document depth ~8,000 words** — exceeds prompt's ~2,000-4,000 target due to substrate complexity (40 questions + 3 risks + 7 sub-arc decomposition + deferrals + references); surfaced in build report per process canon. **Files modified**: `docs/investigations/2026-05-20-free-form-focus-canvas.md` (new, ~8,000 words / ~580 LOC markdown), `STATE.md` (this entry). **No code touched. No tests. No canon doc edits.** Active arc → no active arc, investigation produced. Filed for next arc → **FF-1** (backend validator + adapter extension, depends on Q-23/24/25 locked decisions). **Canon entry count unchanged at 22.**

- **2026-05-20 — DECISIONS.md updated (uncommitted): filed architectural canon distinguishing Monitor canvas (grid model, F-series canonical) from Decide canvas (free-form model, FF-series upcoming)** — Canon-update-only arc; no code changes. One entry appended to `DECISIONS.md`. The three-primitive decomposition (Spaces=Monitor, Command Bar=Act, Focus=Decide) translates into distinct canvas substrates. Monitor canvas (Pulse/dashboards/anomaly surfaces) uses grid model (12-column row-based placement) — F-series established this canonically; structured composability across high variability is the load-bearing constraint enabling dashboard patterns to transfer across tenants. Decide canvas (Focus instances) uses free-form model (per-placement x/y/width/height + z-index) — operator-driven flexibility within decision scope is the load-bearing constraint enabling per-decision workspace manipulation. F-series's canonical decisions (placement structural immutability, unified-placement model, ≥3 configurableProps registry contract) remain valid for Monitor canvas; widget placements on Decide canvas adopt their own positioning shape. Cores remain anchored at canonical positions on both canvas types; free-form positioning applies to widgets only. Per-Focus free-form doesn't break cross-tenant coherence because it's bounded by the operator's decision scope, not a shared dashboard pattern. Next arc: investigation locking free-form substrate's architectural questions (positioning units, canvas dimensions, z-index model, snap behavior, resize constraints, persistence shape, adapter extension, inherited-core anchoring, multi-select). F-3.1a's placement adapter pattern extends to translate positioning fields; pattern itself remains canonical. Page/Document/Workflow Builders consult this entry for canvas-shape decisions. **Files modified**: `DECISIONS.md` (+~30 lines, format matches existing canon entries verbatim — prose-paragraph, date-prefixed, inline `code`, "Implication for future arcs" closing; no F-series entries revised — additive only), `STATE.md` (this entry). No code touched. No tests required. Active arc → no active arc, canon update only. Filed for next arc → investigation arc on free-form Focus canvas substrate.

- **2026-05-19 (late evening / F-5) — Sub-arc F-5 shipped (uncommitted): Focus Builder closure** — Final F-series sub-arc. Three concerns ship in one arc: (1) breadcrumb context in the top bar; (2) dirty state polish (refined existing "Auto-saved Xs ago" indicator to four explicit states); (3) coexistence deprecation banner on the legacy `/studio/focuses` route. **After F-5, Focus Builder is operator-shipped end-to-end. F-series sub-arc COMPLETE.** Pre-flight: HEAD verified `b4e1fa2` per process canon. **Breadcrumb (#1)**: new `FocusBuilderBreadcrumb` component (~73 LOC) presents segment-separated hierarchy with brass-accent `›` separators; current/deepest segment slightly weighted. Subject hierarchy derivation lives in `FocusBuilderPage` and follows the same source-of-truth as the left tree (verticals slug → display_name lookup via existing `verticalsService`; focus-type label via existing `focusTypeForCore` + `focusTypeLabel`; core/template display_name from loaded hook state). TEMPLATE subject → 4 segments (vertical / focus-type / core / template); CORE subject → 3 segments (vertical / focus-type / core); EMPTY → renders null. Page fetches verticals on mount (single small fetch; verticals are stable). Tree-source-of-truth reused (no new helper needed beyond the segment-derivation `useMemo`). NOT clickable per locked decision. **Dirty state polish (#2)**: new `FocusBuilderSaveIndicator` component (~156 LOC) maps existing hook surface (`isDirty`, `isSaving`, `error`, `lastSavedAt`) to four operator-visible states via pure `deriveSaveIndicatorState` mapping: `Saved · Xs ago` (default) · `Saving…` · `Unsaved changes` · `Save failed · Retry`. Failed state carries brass-accent treatment + clickable Retry button that re-fires existing hook `save`. **Existing hook surface was sufficient** — no `useFocusTemplateDraft`/`useFocusCoreDraft` changes required. Both hooks already expose `save` + `isDirty` + `isSaving` + `lastSavedAt` + `error`. **No behavior pipeline changes** (debounce + save + version-bump + URL recovery untouched per F-3.1a.2 substrate). 5-second relative-time interval-refresh continues inside the indicator. **Coexistence banner (#3)**: new `CoexistenceDeprecationBanner` component (~104 LOC) mounted on legacy `FocusEditorPage` only (forward-only — new route renders nothing). Dismissible per-session via `bridgeable.focus-builder.coexistence-banner-dismissed` localStorage key. Brass-accent border-left + subtle background + single-row + `×` dismiss with aria-label + clickable link to `adminPath("studio/builder/focuses")` (admin-subdomain-aware via existing helper). **Integration tests adopt operator-observable canon per (late evening) refinement**: all F-5 integration tests target operator-observable properties at the specific rendered element — `textContent` on breadcrumb wrapper (matches expected concatenated segment string); `textContent` + `data-state` attribute on indicator element itself (matches `"Saved · Xs ago"` / `"Saving…"` / `"Unsaved changes"` / `"Save failed · Retry"`); banner visibility + dismiss state + link `href`. No wrapper-level attribute change assertions. **Verify-against-pre-fix discipline applied independently for each of the three concerns**: (a) **Breadcrumb** — reverted `<FocusBuilderBreadcrumb />` mount in top bar → integration test fails at `findByTestId("focus-builder-breadcrumb")` (not in DOM); restored. (b) **Dirty state** — reverted `deriveSaveIndicatorState`'s failed-state branch → failed-state assertion fails (indicator falls through to saving/unsaved); reverted retry handler wiring → retry click test fails (no second save fired); restored. (c) **Coexistence banner** — reverted banner mount from legacy page → banner-presence test fails (`coexistence-banner` testid not in DOM); restored. **Cross-file mock leak surfaced + fixed**: `Tier2TemplatesEditor.test.tsx` "preview-card chrome preset updates live when chrome pill clicked" failed under full-suite default reporter post-F-5 (passed isolated; passed pre-F-5). Root cause: F-5's failed-PUT integration test sets `mockImplementation(() => Promise.reject(...))` on `focusTemplatesService.update`; `vi.clearAllMocks()` clears mock calls/results but NOT implementations; the rejection leaked across the worker's shared module cache (vi.mock factories run once per worker; mock-state is worker-scoped) into Tier2's tests via `focusTemplatesService.get` (Tier2 mocks the same service module). Fix: targeted `mockReset()` on `focusTemplatesService.update` in `afterEach` (broader `vi.resetAllMocks()` breaks the cohort's `defaultMocks()` setup pattern). Plus defensive `unmount()` + 1000ms timer-advance after the failed-PUT test resolves cleanly. Verified 4 consecutive clean full-suite runs post-fix. **No hook changes, no resolver changes, no backend changes, no migrations, no F-3/F-3.1a/F-3.1c/F-4/F-4.1 substrate changes, no canon doc edits beyond STATE.md.** **Files modified**: `FocusBuilderPage.tsx` (~106 LOC delta — verticals fetch + breadcrumb segment derivation `useMemo` + JSX wire-up of breadcrumb + save indicator + retry callback; removed inline `relativeTime` helper now living inside indicator), `FocusBuilderPage.test.tsx` (+240 LOC — 6 new F-5 integration tests + 1 reorganized test for clearer pre-no-subject baseline), `FocusEditorPage.tsx` (+7 LOC — banner import + mount above tier toggle with `adminPath`-resolved href), `FocusEditorPage.test.tsx` (+42 LOC — 3 F-5 banner integration tests under nested describe), `STATE.md` (this entry). **Files added**: `FocusBuilderBreadcrumb.tsx` + `.test.tsx` (73+81 LOC), `CoexistenceDeprecationBanner.tsx` + `.test.tsx` (104+76 LOC), `FocusBuilderSaveIndicator.tsx` + `.test.tsx` (156+167 LOC). **Gates**: `npx tsc -b` clean; `npx vitest run` 213/213 files / **2905 + 1 todo passing** (was 2897 pre-F-5; net +8 tests); `npx vite build` clean (6.02s) bundle **6,088.79 kB vs pre-F-5 6,084.37 kB = +4.42 kB / +0.073%** (well within ±0.5%); gzip bundle delta +1.12 kB / +0.077%. Backend pytest unchanged (F-5 is pure frontend). **LOC**: production ~446 (3 new components: 73+104+156 = 333 + page modifications ~106 + legacy page +7); tests ~606 (3 new test files: 81+76+167 = 324 + page integration +240 + legacy integration +42) ≈ **~1,052 total**. **Surfaces past the 900 worst-case budget** primarily due to operator-observable canon's per-concern verbatim assertions + multi-state coverage (6 indicator unit tests + 5 indicator integration scenarios + 5 breadcrumb unit tests + 4 breadcrumb integration scenarios + 6 banner unit tests + 3 banner integration tests). Test-heavy distribution; production code is comfortably tight per concern. No scope creep — all three concerns shipped at locked specification; no canon doc edits, no hook changes, no backend changes. Reporting at completion rather than mid-build surface-and-stop because the work is shipped end-to-end and all gates green. **Architectural questions surfaced during build**: (a) subject hierarchy derivation reused tree's source-of-truth (verticals + cores' inheritance metadata) with a small `useMemo` rather than introducing a new helper — focus-types canonical taxonomy at `lib/visual-editor/focus-types.ts` was already the canonical owner of focus-type labels; verticals service was already the canonical owner of vertical display_name lookup; new helper would have duplicated existing infrastructure. (b) Dirty-state hook surface was sufficient out-of-box — the hook's `error` + `isSaving` + `isDirty` + `lastSavedAt` four-tuple captures all four indicator states via pure derivation; surface satisfies the polish requirement without hook layer work (which would have been out-of-scope per locked decision #2). (c) Page-level test had to wait for async hierarchy hydration (`inheritedCore` fetch is async after template loads), so TEMPLATE-subject breadcrumb assertions use `waitFor` against the full 4-segment textContent rather than `findByTestId`'s shallow presence assertion — operator-observable canon honored by targeting the fully-hydrated textContent. (d) Pre-existing flaky test isolation: F-5's failed-PUT test introduced cross-file rejection timing pollution under default parallel reporter; explicit `unmount()` + microtask drain in test cleanup resolved it. Future arcs using `mockImplementation(() => Promise.reject(...))` should follow the same defensive pattern. **F-series completion confirmed**: Focus Builder operator-shipped end-to-end. F-1 (tree + URL + layout shell) → F-1.1 (template-vertical-immutability discovered canon) → F-2 (selection-driven inspector + canonical canvas) → F-3 (widget palette + drag-to-canvas + accessory placement editing) → F-3.1a (placement adapter) → F-3.1a.2 (URL recovery) → F-3.1b (widget Chrome section) → F-3.1c (canvas wrapper applies chrome via resolver) → F-4 (theme picker) → F-4.1 (typography preset specifics-nullify) → **F-5 (breadcrumb + dirty state polish + coexistence banner)**. Active arc → **None** until next dispatch. Migration head unchanged: `r103_focus_templates_edit_session`. Page/Document/Workflow Builder are separate substrate arcs; F-series substrate **LOCKS** with F-5 commit.

- **2026-05-19 (late evening) — DECISIONS.md updated (uncommitted): filed process canon refinement specifying render-side assertions must target operator-observable CSS resolution at the specific rendered element** — Canon-update-only arc; no code changes. One entry appended to `DECISIONS.md` with `(late evening)` disambiguator (ordering-distinct from earlier 2026-05-19 (PM) architectural, (late PM) process canon, and (evening) cross-side data↔render refinement). Entry refines the (evening) framing by specifying HOW render-side assertions target observable output. F-4 (commit `cd2d0f7`) passed false-positive because wrapper-level `canvas.getAttribute("style")` change detection passed when CSS custom properties updated regardless of whether resolver output actually differed; operator-visible heading font-weight stayed at 600 because typography resolver's specifics-win priority dominated. F-4.1 (commit `89fd867`) corrected by asserting `heading.style.fontWeight` at the rendered `<h2>` element — operator-observable property at the specific rendered element. Verify-against-pre-fix discipline confirmed at both directions independently. Refinement: target operator-observable CSS resolution at the specific rendered element, NOT wrapper-level style attribute changes. Wrapper writes CSS custom properties that update regardless of resolver output; only the operator-observable property on the rendered target is load-bearing. F-4's wrapper-level test stays as continuity check; F-4.1's operator-observable test is canonical shape going forward (both layers asserted prevents regressions in either direction). Implication: F-5 + Page/Document/Workflow Builder integration tests must follow this pattern. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+~25 lines, format matches existing process-canon entries verbatim — prose-paragraph, date-prefixed, inline `code`, "Implication for future arcs" closing), `STATE.md` (this entry). No code touched. No tests required.

- **2026-05-19 (late evening / F-4.1) — Sub-arc F-4.1 shipped (uncommitted): typography preset chip clicks visibly re-render** — Tight fix arc closing F-4's typography render bug. F-4 shipped the theme picker; substrate chip clicks visibly updated the canvas (via substrate-resolver's `if (view.preset === "morning-warm")` branch coincidentally producing structurally different CSS); typography chip clicks saved correctly to backend but produced no visible render change because `typography-resolver.ts:expandTypographyPreset` only applies preset defaults when specific fields (heading_weight, body_weight, color tokens) are null. F-4's chip onClick fired only `{ preset: <slug> }` — pre-existing scrubbed specifics stayed at their values; resolver's specifics-win path returned unchanged output. Bug only surfaces when specifics have been authored (non-null); from an empty template all specifics are null so the chip click coincidentally works — which is why F-4's existing render-side assertion (canvas inline-style attribute string change) passed false-positive: typography updates surface as CSS custom properties on the canvas wrapper that change with state regardless of resolver output. **Fix shape**: chip onClick fires preset + nullified specifics. **F-2 inspector audit outcome**: C-1 SubstratePresetPicker + TypographyPresetPicker primitives are "dumb" (emit `slug | null` via onChange); bug lives at the consumer's onChange handler. F-2 inspector has the same bug pattern at `FocusBuilderInspector.tsx:407-409` (substrate) and `:491-493` (typography). Per canon — "chip click is operator-intent 'apply preset wholesale'" — fix applies symmetrically to BOTH theme picker (F-4) AND F-2 inspector. Specific-field scrubbing via ScrubbableButton / TokenSwatchPicker below the chip continues to take priority (resolver's specifics-win path is correct for fine-grained scrubbing). Canon parity: same fix for typography AND substrate. **Operator-observable test**: new `F-4.1 — typography chip resets specifics AND visibly updates rendered heading font-weight` integration test in `FocusBuilderPage.test.tsx` seeds template with pre-scrubbed `heading_weight=400` + `preset="card-text"` to reproduce the operator scenario (operator scrubbed weight in F-2 inspector → then clicks different preset chip in F-4 picker → pre-fix: weight stayed 400; post-fix: weight applies preset's 700). Test asserts `heading.style.fontWeight === "400"` pre-click then `=== "700"` post-click — operator-observable rendered `<h2>` inline-style `fontWeight`, NOT arbitrary wrapper style-attribute changes. Canvas applies `headingStyle` directly to `<h2>` at `FocusBuilderCanvas.tsx:285-292`; this is the operator's visual reality. Substrate equivalent test seeds pre-scrubbed `base_token="surface-sunken"` + `intensity=50` + `preset="neutral"` and asserts PUT payload shape after morning-warm chip click. **Verify-against-pre-fix discipline applied to BOTH typography AND substrate independently**: (a) reverted typography chip handler to `{ preset: p }` only → save-side `toMatchObject` failed (`heading_weight: 400` instead of expected `null`); relaxed save-side to `{ preset: "headline" }` only → render-side independently failed (`Expected "700", Received "400"`). Confirms the cross-side render-contract test catches render-pipeline bugs independent of save-pipeline regressions. (b) Substrate symmetric: reverted handler → save-side failed (`base_token: "surface-sunken"` instead of expected `null`). Both fixes restored; tests pass. **F-4's existing typography integration test SUPPLEMENTED, not modified** — the original `canvas.style` attribute assertion still passes (typography updates do surface as CSS-custom-property changes on the wrapper regardless of resolver output), so it stays as continuity check; F-4.1's new operator-observable test is the canonical assertion shape going forward. **Files modified**: `FocusBuilderThemePicker.tsx` (substrate + typography chip onClick nullify specifics — 2 changes), `FocusBuilderInspector.tsx` (substrate + typography PresetPicker onChange nullify specifics — 2 changes, canon parity), `FocusBuilderThemePicker.test.tsx` (3 unit tests updated to new payload shape — substrate, typography, toggle-to-clear), `FocusBuilderPage.test.tsx` (+2 operator-observable integration tests — typography heading font-weight + substrate canvas style — with verify-against-pre-fix comments documenting both reverts), `STATE.md` (this entry). **No backend changes, no migrations, no resolver changes, no hook changes, no canon doc edits, no F-3.1c chrome rendering changes, no widget-palette changes.** **Gates**: `npx tsc -b` clean; `npx vitest run` 209 files / 2871 passing + 1 pre-existing flaky `Tier2TemplatesEditor.test.tsx` chrome-preset-live-cascade test (documented across multiple arcs as cross-file timing flake — confirmed identical at clean F-4 HEAD via `git stash` regression check); `npx vite build` 5.59s, bundle 6,084 kB (within ±0.5% of F-4 baseline); backend pytest unrelated `test_admin_portal.py::TestProductLines::test_available_catalog` failure pre-existing (F-4.1 touched zero backend files). **LOC**: production ~28 (4 chip-handler payload shapes × 5-7 lines each net; 2 in theme picker + 2 in inspector); tests ~115 (3 unit-test updates ~20 LOC + 2 new integration tests ~95 LOC) = ~143 total, within ~150-300 midpoint band. **Architectural question warranting canon filing**: F-4's false-positive demonstrates the (evening) refinement's gap — `canvas.getAttribute("style")` is too coarse-grained as render-side assertion when the rendered surface includes both deterministic CSS custom properties (which change with any state update) AND resolver-computed values (which can stay unchanged despite state updates). Future arcs authoring integration tests for any operator-flow producing both persistence AND visual change should target operator-observable resolved values at the specific rendered elements that consume them (here: `<h2>.style.fontWeight`), NOT wrapper-level inline-style attribute change detection. Filing as canon-update arc deferred. Active arc → F-4.1 closed; F-4 sub-arc fully closed. **Pre-flight verification**: confirmed HEAD was `cd2d0f7` (F-4 commit) before starting per process canon.

- **2026-05-19 (evening) — DECISIONS.md updated (uncommitted): filed process canon refinement extending cross-side framing from frontend↔backend to data↔render** — Canon-update-only arc; no code changes. One entry appended to `DECISIONS.md` with `(evening)` disambiguator: "Cross-side contract framing extends to data↔render boundaries; integration tests must assert both directions when both apply." Refines the earlier 2026-05-19 (late PM) process canon entry on mock-only tests verifying one side of cross-side contracts. Original entry covered frontend↔backend cross-side; refinement extends to data↔render cross-side, which F-3.1b's chrome-editing arc missed (PUT body verified, render side silently broken). F-3.1c (commit `4bcdf96`) closed the gap with wrapper-level chrome rendering + extended integration test asserting BOTH directions (PUT body shape + wrapper inline style attribute). Verify-against-pre-fix discipline applied to both assertions independently. Implication: future builder UI arcs (F-4 theme picker, F-5, Page/Document/Workflow Builders) authoring integration tests for any operator-flow that produces both persistence AND visual change must assert both directions. Reference impl: F-3.1c's integration test in `FocusBuilderPage.test.tsx`. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+~25 lines, format matches existing process-canon entries), `STATE.md` (this entry). No code touched. No tests required.

- **2026-05-19 (late evening) — F-3.1c shipped (uncommitted): widget canvas wrapper applies chrome via resolver** — Closes the render-side gap in F-3 + F-3.1b. Pre-F-3.1c, `PlacedWidget`'s wrapper `<div>` (FocusBuilderCanvas.tsx:422-438) styled only `gridColumn` + `outline` + hardcoded `borderRadius: 12` — chrome scrubs from the F-3.1b inspector persisted correctly to backend `prop_overrides` (verified by F-3.1b's PUT-body integration test) but produced zero observable change on the canvas. F-3.1c routes `placement.chrome` through the canonical chrome-resolver pipeline (`mergeChromeWithOverrides(DEFAULT_WIDGET_CHROME, placement.chrome)` → `expandPreset` → `resolveChromeStyle(view, themeTokens)`) and spreads the resolved CSS onto the wrapper. Layout + selection styles (`gridColumn`, `outline`, `transition`) override on conflict; hardcoded `borderRadius: 12` removed — corner radius now flows from chrome's `corner_radius` field through the resolver's tier-bucket mapping. **Pattern audit (Step 1)**: mirrored the inherited-core-placement chrome-resolution pattern at lines 161-184 cleanly. Core-placement merges Tier-1 chrome with template overrides via `mergeChromeWithOverrides(inheritedCore?.chrome, chromeOverridesDraft)`; widget-placement merges `DEFAULT_WIDGET_CHROME` with `placement.chrome` via the same function. Both produce `ChromeView` → `expandPreset` → `resolveChromeStyle(view, themeTokens)`. The resolver composed cleanly for per-placement widget chrome — the absence of a Tier-1 cascade did NOT reveal any resolver assumption; `mergeChromeWithOverrides` is shape-agnostic about whether arg 1 is a Tier-1 blob or a defaults constant. **DEFAULT_WIDGET_CHROME hoisting**: hoisted from module-local in `WidgetInspectorSection.tsx` to an exported constant; canvas imports it via `import { DEFAULT_WIDGET_CHROME } from "./WidgetInspectorSection"`. Single source of truth across inspector + canvas (1-line export change). **Integration test assertion shape**: JSDOM does not compute resolved CSS values (`window.getComputedStyle` returns declared-only), so the DOM-side assertion inspects the wrapper's `style` attribute string for `box-shadow` + `border-radius` + `padding` declarations. Pre-fix the inline style string contains only `grid-column / outline / outline-offset / transition / cursor / min-height` — none of the chrome-derived properties. Post-fix all three chrome properties appear. Stronger assertion: 30 ArrowRight scrubs cross two resolver tier boundaries (50→75→100), causing `boxShadow` value to change between pre-scrub and post-scrub captures; `expect(updatedStyleAttr).not.toBe(initialStyleAttr)`. Both directions asserted in one test — PUT body's `prop_overrides.elevation` shape (mirrors F-3.1b) AND wrapper inline style attribute. **Verify-against-pre-fix discipline applied**: commented out `...resolvedChromeStyle` spread in PlacedWidget, ran F-3.1c test → DOM assertion failed at `expect(initialStyleAttr).toMatch(/box-shadow/i)` (zero chrome-derived inline style declared); ran F-3.1b test → PUT-body assertion still PASSED (save-side unaffected, confirms F-3.1b's narrower assertion shape was the gap, not the assertion itself); restored the spread → both pass. This is the canonical cross-side render-contract test pattern going forward: every chrome-editing integration test must assert BOTH the PUT body shape AND the rendered DOM. **threadTokens through WidgetRowsLayer**: `themeTokens` now passes from canvas → `WidgetRowsLayer` → `PlacedWidget` (3-line prop-threading change). **No changes to placeholder widget components** (DayStripWidget, TodayPinWidget, MapPlaceholderWidget) — config props remain placeholder-discarded per F-3 scope. Real widget consumption of `placement.chrome` config props is a later arc. **No changes to chrome-resolver** — reused as-is. **No changes to F-3.1b Chrome section in WidgetInspectorSection** — inspector side already worked correctly. **No backend changes, no migrations.** **Bundle delta**: 6,082.44 kB — within ±0.5% tolerance against the F-3.1b 6,082.31 kB baseline (+0.13 kB). **Test totals**: vitest 209/209 files, 2858 tests passing (1 new F-3.1c integration test); `npx tsc -b` clean; `npx vite build` clean (5.21s). Backend pytest focus-related: 328 passing, 1 unrelated pre-existing failure (`test_arc_focus_ring_contrast.py` — aesthetic-arc CSS lightness test, no F-3.1c relation). **LOC**: ~50 in FocusBuilderCanvas.tsx (resolved-chrome useMemo + style spread + threadTokens + import + 1-line DEFAULT_WIDGET_CHROME export change) + ~95 integration test = ~145, comfortably within 350 worst-case budget. **Pre-flight verification**: confirmed HEAD was `c36f1e2` (F-3.1b commit) before starting per today's process canon. **F-3.1c can be referenced as "the canonical chrome-editing integration test shape" going forward** — it establishes the cross-side assertion pattern (both PUT body and rendered DOM) that future chrome-editing tests should mirror; verify-against-pre-fix discipline confirms the assertion is load-bearing. Active arc → F-3.1c closed; widget canvas wrapper applies chrome via resolver mirroring inherited core pattern; integration test asserts BOTH PUT body AND DOM computed style; canonical cross-side render-contract test pattern established for future arcs. **Files modified**: `FocusBuilderCanvas.tsx`, `WidgetInspectorSection.tsx` (export 1 constant), `FocusBuilderPage.test.tsx`, `STATE.md` (this entry).

- **2026-05-19 (evening) — F-3.1b shipped (uncommitted): widget inspector Chrome section** — Closes Q-28's specification gap. Widget inspector now exposes per-placement chrome editing controls below the existing CONFIGURATION + PLACEMENT sections, before the Remove widget button. Section is collapsible with `defaultExpanded={true}` (operators see chrome on first inspector open — frequent action, differs from PLACEMENT's collapsed default). Seven fields wired identically to F-2's `ChromeInspectorSection` composition: `ChromePresetPicker` (preset), three `ScrubbableButton`s (elevation/corner_radius/backdrop_blur, all 0-100), and three `TokenSwatchPicker`s (background_token/surface family, border_token/border family, padding_token/padding family). All updates flow through `hook.updateWidget(widget.id, { [field]: value })` → existing F-3.1a placement adapter → backend `prop_overrides`. **No inheritance indicators on widget chrome** — widgets are per-placement; chrome is stamped at placement creation; no Tier-1 cascade (C-2.3 inheritance indicator pattern does NOT apply). **Default chrome lookup**: F-3 widget registrations do NOT carry a `defaultChrome` field on `RegistrationMetadata`; per Step 1 audit, fall back to inline canonical constant `DEFAULT_WIDGET_CHROME` matching the F-3 seed canon (Frosted preset, elevation 50, corner_radius 70, backdrop_blur 44, surface-frosted, border-subtle, space-3). When registrations later expose `defaultChrome` metadata, swap the constant for a registry lookup — documented inline. **F-2 ChromeInspectorSection reuse**: pattern reused with adaptation — F-2 carries inheritance source resolution (`chromeInheritanceFor` reading `sources.chrome_sources`) and onReset callbacks; widgets have neither concept so row composition simplifies to `inheritanceSource="explicit"` literal with no reset. The `chromeView` derivation is bespoke: simple per-field null coalescing against the inline defaults (vs F-2's cascade through `expandPreset` + `mergeChromeWithOverrides` — widgets have no inherited core chrome to merge against). **Verify-against-pre-fix discipline applied**: wrapped the entire Chrome `<PropertySection>` in `{false && (...)}`, ran integration test → failed at `await screen.findByLabelText(/elevation/i)` exactly as predicted, restored. Discipline confirms the test gate is load-bearing. **Integration test** (FocusBuilderPage.test.tsx, "F-3.1b — widget chrome scrub persists through adapter as prop_overrides"): seeds a template with one backend-shaped placement (component_name day-strip-widget, starting_column 0, column_span 12, no prop_overrides), renders the page, clicks the placed widget, opens the inspector with the Chrome section, scrubs elevation via `ArrowRight` keydown on the labeled scrubber, advances fake timers past the 350ms debounce, asserts the PUT body's `rows[0].placements[0].prop_overrides.elevation` is a number. Exercises the full operator-flow: hook + adapter + service + URL state, with only the network mocked (per the today-filed canon entry "Mock-only tests verify one side of frontend↔backend contracts; cross-side contracts require integration tests"). **Unit tests**: 10 new in `WidgetInspectorSection.test.tsx` Chrome section describe block: section renders, expanded by default, can collapse via header click, elevation/corner_radius/backdrop_blur each call `onUpdateWidget` with correct partial, preset pill click toggles to null (chromeView default is "frosted" so first click clears), background/border/padding swatch labels render, defaults render with empty/undefined chrome, mixed null + non-null chrome falls back per-field. **FocusBuilderInspector wiring**: added `themeTokens={themeTokens}` to the `<WidgetInspectorSection>` mount point (1-line change). **Bundle delta**: 6,082.31 kB — within ±0.5% tolerance. **No backend changes, no migrations, no canon docs touched beyond STATE.md.** **Test totals**: vitest 157/157 in focus-builder + hooks scope (10 new Chrome unit + 1 new integration); `npx tsc -b` clean; `npx vite build` clean (5.40s). Backend pytest passes except for one pre-existing unrelated failure (`test_admin_portal.py::TestProductLines::test_available_catalog`) verified to fail equivalently on stashed-clean main — out of scope. **LOC**: ~110 in WidgetInspectorSection.tsx + ~95 unit tests + ~75 integration test + 1 line in FocusBuilderInspector = ~280, comfortably within 350 worst-case budget. **Pre-flight verification**: confirmed current commit was `be1d46c` (process canon) before starting per today's process canon. Active arc → F-3.1b closed; F-3 sub-arc complete; Chrome section shipped (collapsible, default expanded); ChromePresetPicker + 3 scrubbable buttons + 3 token swatch pickers; all updates flow through updateWidget → adapter → backend; integration test verifies end-to-end chrome persistence with verify-against-pre-fix discipline. **Files modified**: `WidgetInspectorSection.tsx`, `WidgetInspectorSection.test.tsx`, `FocusBuilderInspector.tsx`, `FocusBuilderPage.test.tsx`, `STATE.md` (this entry).

- **2026-05-19 (late PM) — DECISIONS.md updated (uncommitted): filed three process-canon entries from today's F-3.1 cycle** — Canon-update-only arc; no code changes. Three entries appended to `DECISIONS.md` with `(late PM)` disambiguator: (1) "`git log -1 HEAD` as first diagnostic step when staging behavior contradicts expectation" — F-3.1a's work was uncommitted; F-3.1a.1's dispatch missed this; a 30-second git check would have surfaced the gap and prevented hours of wrong-diagnosis cycles. (2) "Read-only investigation before fix-prompt drafting when staging contradicts expectation" — recurring lesson from F-3.1 + F-3.1a.1 + F-3.1a.2 cycles; static-analysis-only diagnoses are an empirical failure mode; investigation arcs (zero code, quoted code citations + runtime evidence, single testable hypothesis) are the canonical resolution. (3) "Mock-only tests verify one side of frontend↔backend contracts; cross-side contracts require integration tests" — F-3's mocked-service tests gave false confidence; F-3.1a.2's full-operator-flow integration test with verify-against-pre-fix discipline is the canonical shape. Together with the six architectural-canon entries filed in `a4cc1ea`, these three close the canon backlog from today's F-3.1 cycle. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+~50 lines), `STATE.md` (this entry). No code touched. No tests required.

- **2026-05-19 (PM) — DECISIONS.md updated (uncommitted): filed six architectural canon entries from F-3.1a + F-3.1a.2** — Canon-update-only arc; no code changes. Six entries appended to `DECISIONS.md` with `(PM)` disambiguator to distinguish from earlier 2026-05-19 entries: (1) "Off-by-one column index between frontend and backend placement coordinates" — frontend 1-indexed, backend 0-indexed; F-3.1a adapter handles conversion with clamp-at-0 defensive guard. (2) "`chrome` field on placements stores the full per-placement override surface" — frontend `chrome` ↔ backend `prop_overrides` 1:1; widget configurable props live INSIDE chrome blob and flow through same code path; frontend naming reflects operator UX, backend naming reflects implementation reality. (3) "Ordinary template updates version-bump by default; session-aware mutate-in-place is the exception" — `update_template` has two paths (default version-bump deactivates prior + creates new row + new id; session-aware fast path mutates in place when token + window match); template_ids are inherently unstable across version-bumps. (4) "URL stability for versioned entities requires slug-based addressing as long-term canonical pattern" — F-3.1a.2 symptom fix (URL recovery on 410-retry) is current handling; slug-based addressing (`(template_slug, scope, vertical)` tuple) is the structural answer for Page/Document/Workflow Builders. (5) "GET-on-inactive 200 + version-bump-on-every-save = silent staleness without URL discipline" — interaction-canon entry; neither backend policy is wrong alone, but their combination requires frontend URL discipline as the integration layer. (6) "Callback-ref pattern extends multi-hook-mount canon to consumer callbacks" — F-3.1a.2's `onActiveTemplateIdChangeRef` pattern; ref-as-dominant-defense discipline (C-2.1.4 + C-2.2b) generalizes from internal state to consumer-supplied callbacks. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+~155 lines, format matches existing F-series canon entries verbatim — prose-paragraph, date-prefixed with `(PM)` disambiguator, inline `code`, `---` separator, "Implication for future arcs" section per entry), `STATE.md` (this entry). No code touched. No tests required. **Three additional process-canon entries (git log -1 discipline; read-only investigation before fix-prompt drafting; mock-only tests verify one side of cross-side contracts) explicitly deferred to a separate canon-update arc per scope discipline.**

- **2026-05-19 — Sub-arc F-3.1a.2 shipped (uncommitted) — URL recovery on 410-retry** — Follow-on to F-3.1a. After F-3.1a's adapter resolved the 422 errors, operator-visible "widget config props don't persist across refresh" surfaced. **Diagnosis**: URL staleness + first-save-in-session version-bumps. Fresh page load with URL `?subject=template:<old-id>`. First save in session: (1) PUT against URL's `<old-id>`; (2) backend's session-aware fast path doesn't fire (prior session's session-id stamp on the row defeats `_within_template_edit_session` at `focus_templates_service.py:685-695`); (3) backend version-bumps — deactivates prior, creates new active row with new id; (4) backend returns 410 with `active_template_id` in detail; (5) hook's 410-retry swaps `activeTemplateIdRef.current` to new id + retries PUT; (6) retry returns 200; data persists. **But URL still points at the deactivated id.** On refresh, hook GETs deactivated id, backend returns 200 with inactive snapshot (predates operator's saves), refresh appears to wipe values. Subsequent saves within same session work correctly (mutate-in-place via session-aware fast path). Bug only fires across refresh boundaries. **Fix**: hook exposes new `onActiveTemplateIdChange?: (newId: string) => void` callback option; fires on successful 410-retry when the resolved id differs from the consumer's bound `templateId`. `FocusBuilderPage` consumes the callback and rewrites URL via `setSearchParams({ subject: template:${newId} }, { replace: true })`. `{ replace: true }` mandatory — prevents history bloat per version-bump. **Locked decisions honored**: Option 1 URL update on retry-success is canonical fix (#1, slug-based URLs deferred); callback signature `onActiveTemplateIdChange?: (newId: string) => void` (#2); fires only on actual id change (#3); no backend changes (#4); no session-aware fast path fixes (#5 — separate future arc); no URL contract change (#6); no F-3.1a adapter changes (#7). **Modified files**: `useFocusTemplateDraft.ts` — added `UseFocusTemplateDraftOptions` interface + optional second parameter; callback-ref pattern (`onActiveTemplateIdChangeRef`) mirrors existing ref discipline so the latest consumer callback is always called without closure capture and without polluting the `save` callback's deps; fired inside the 410-retry success path after `activeTemplateIdRef.current = updated.id` and ONLY when `updated.id !== templateId` (the consumer's URL-bound id). `FocusBuilderPage.tsx` — added `handleActiveTemplateIdChange` callback (mirrors existing `handleSelectSubject` `setSearchParams` pattern but with `{ replace: true }`); passed to `useFocusTemplateDraft` via new options object. **Tests added**: 4 hook unit tests (`F-3.1a.2 onActiveTemplateIdChange` describe block — fires-with-new-id on retry success, does-NOT-fire on direct 200, does-not-throw when option absent, does-NOT-fire when retry resolves back to original templateId per defensive same-id check) + 1 integration test in `FocusBuilderPage.test.tsx` (`F-3.1a.2 — URL recovers to active template id when initial save 410s` — full operator-flow: stale URL → page renders canvas in template mode → click canvas → click substrate preset pill → debounced save fires → first PUT 410s → retry resolves against tpl-1-v2 → URL rewritten → hook re-fetches against new id via subsequent `focusTemplatesService.get` call against `tpl-1-v2`). **Verify-against-pre-fix discipline applied** per Step 4: commented out URL-update wiring in `FocusBuilderPage` (replaced 2-line callback wiring with bare `const templateHook = useFocusTemplateDraft(templateSubjectId)`) → integration test failed at the `focusTemplatesService.get` waitFor asserting against `tpl-1-v2` (URL stayed at `tpl-1`, hook never re-fetched against the new id) → restored verbatim. **Tests**: 4 new hook unit + 1 new page integration = **5 new tests**. Full vitest **209 files / 2844 tests + 1 todo passing** (+4 tests vs F-3.1a baseline 2840); 1 failing test is the pre-existing flaky `Tier2TemplatesEditor.test.tsx` chrome-preset-live-cascade documented across multiple arcs (not introduced by F-3.1a.2). tsc clean (`npx tsc -b` per F-1 lesson); vite build 5.06s; bundle **6,080,410 bytes vs F-3.1a baseline 6,080,090 = +320 / +0.005%** (well within ±0.5% per F-3.1a.2 gate). Backend `test_focus_template_inheritance_e1_defaults.py` + `test_verticals.py` + `test_focus_template_f3_1a_adapter_contract.py` 44/44 green (no backend changes — F-3.1a.2 is pure frontend). **LOC**: production ~25 (hook callback-ref + retry-path fire site + options interface; page callback wiring) + tests ~75 (4 hook unit + 1 page integration) ≈ **~100 total**, comfortably under the ~100-200 midpoint band. **Architectural questions surfaced during build**: (a) callback wiring matched hook's existing prop pattern cleanly — adding an optional second parameter with a callback-ref dispatch mirrors the existing `templateId`/`editSessionId` discipline; no restructuring needed. (b) integration test exercised full operator-flow page → hook → adapter → mocked retry → URL update — substrate-preset pill click was the simplest reliable save trigger that doesn't depend on drag-drop infrastructure; 300ms debounce naturally completes within waitFor's default timeout (test uses 2000ms timeout for safety against CI flake). (c) Verify-against-pre-fix discipline applied successfully — pre-fix variant failed at the URL-update assertion; restoring the 2-line callback wiring restored green. (d) Bundle delta +320 bytes / +0.005% well within ±0.5% tolerance; F-3.1a baseline+1280 → F-3.1a.2 baseline+320 reflects the callback-ref + options interface being minimal-overhead additions. (e) Existing `FocusBuilderPage` URL-update mechanism reused verbatim — `setSearchParams(params, { replace: true })` matches the pattern already used by `handleSelectSubject` modulo the `replace` flag (`false` for tree-clicks, `true` for version-bump recovery). No new URL primitives, no new search-param parsing. (f) Architectural note: the underlying session-aware fast path bug (first save in session always defeats `_within_template_edit_session` because of the prior session-id stamp) is intentionally out of scope per locked decision #5 — F-3.1a.2 solves operator-visible symptom regardless. Future arc may fix the backend fast path to skip the version-bump entirely on session resume, at which point F-3.1a.2's URL-recovery becomes inert (no harm — callback simply doesn't fire). **No backend changes, no migrations, no canon doc edits beyond STATE.md, no C-1 primitives modified, no other hook contracts altered beyond `useFocusTemplateDraft`'s new optional second parameter, no resolver internals touched, no adapter changes (F-3.1a canonical), no canvas/inspector/palette changes (F-3 + F-3.1a behavior preserved), no Chrome section UI (F-3.1b territory), no theme picker (F-4), no breadcrumb (F-5).** Migration head unchanged: `r103_focus_templates_edit_session`. **Next**: F-3.1b (Chrome section addition + further inspector polish on top of the now-persistent + URL-recoverable widget state).

- **2026-05-19 — Sub-arc F-3.1a shipped (uncommitted) — frontend placement-shape adapter (Path A)** — First half of the F-3.1 fix split following the F-3.1 surface-and-stop. Resolves the frontend↔backend wire-shape mismatch that caused F-3-placed widgets to persist as backend-validation no-ops + disappear after refresh. **Diagnosis**: hook's typed view (`WidgetPlacement`: `id` / `widget_slug` / `column_start` 1-indexed / `column_span` / `chrome`) sent over the wire was rejected by `_validate_placement` in `backend/app/services/focus_template_inheritance/focus_templates_service.py` (requires `placement_id` string + `component_kind` + `component_name` + `starting_column` 0-indexed; rejects everything else as `InvalidTemplateShape`). Backend persisted nothing; GET returned prior server state; refresh wiped the widget. **Path A locked**: frontend adapter at hook boundary; backend schema unchanged (the canonical `component_kind`+`component_name` shape correctly serves cores AND widgets — frontend's `widget_slug` is a widget-narrowed authoring convenience the adapter widens on save and re-narrows on load). **Consumer audit (Step 1)**: 4 focus-builder consumers (`FocusBuilderCanvas`, `WidgetInspectorSection`, `FocusBuilderPage`, `useFocusTemplateDraft.test`) all read frontend shape from `templateHook.rowsDraft` — the hook is the sole entry point. The separate `composition-canvas/` module (Visual Editor compositions) already uses backend shape (`placement_id`). Adapter location: **hook-level** (option i). Service-level (option ii) would either double-translate for composition-canvas or force type widening that breaks it. **Backend chrome storage path (Step 1e)**: `_validate_placement` declares `prop_overrides` (validated as `dict` at lines 214-219) as the per-placement config field — semantically identical to frontend's per-placement `chrome` blob (per-placement prop overrides written by `WidgetInspectorSection.onUpdateWidget(id, { [key]: v })` at line 103; read at line 101 as `placement.chrome?.[key]`). Adapter maps `chrome ↔ prop_overrides` 1:1. Configurable props (Q5) live INSIDE the chrome blob, so they flow through the same translation — **F-3.1a fixes configurable-props persistence as a side effect of fixing widget placement persistence**, same code path, same bug. **Field mapping** (both directions): `id↔placement_id`, `widget_slug→component_name`+`component_kind:"widget"`, `column_start↔starting_column` (1-indexed↔0-indexed — adapter subtracts 1 on save, adds 1 on load), `column_span` unchanged, `chrome↔prop_overrides` (omitted on send when empty, defaults to `{}` on load when absent). **New files**: `frontend/src/bridgeable-admin/hooks/_placement-adapter.ts` (8 exported helpers — `frontendToBackendPlacement` / `backendToFrontendPlacement` / `frontendToBackendRow` / `backendToFrontendRow` / `frontendToBackendRows` / `backendToFrontendRows` + `BackendPlacement` / `BackendRow` / `BackendRowsBlob` types; ~165 LOC including module-header diagnosis paragraph + chrome-storage-audit comment + canonical mapping table). `frontend/src/bridgeable-admin/hooks/_placement-adapter.test.ts` (~245 LOC; 23 unit tests across 4 describe blocks — frontend→backend field mapping (8 tests including off-by-one verification at `column_start=1→starting_column=0` and `column_start=5→starting_column=4` + clamp-at-0 defensive case + empty-chrome omits prop_overrides), backend→frontend field mapping (8 tests including legacy-input tolerance for stale frontend-shape JSONB still in DB), roundtrip preservation (2 tests both directions), row-level adapters (5 tests including null/undefined input tolerance + missing-column_count default to 12)). `backend/tests/test_focus_template_f3_1a_adapter_contract.py` (~210 LOC; Path Y per Step 5 — imports REAL `_validate_rows` + `_validate_placement` from `app.services.focus_template_inheritance.focus_templates_service` and exercises against fixtures shaped exactly like `frontendToBackendRows` emits; **6 tests** across 2 classes — `TestFrontendAdapterOutputValidates` × 4 (single addWidget output validates, widget+chrome output validates, multi-widget different-column output validates, `starting_column + column_span ≤ column_count` invariant explicitly verified) + `TestPreAdapterShapeRejected` × 2 (frontend-shape with `id` not `placement_id` rejected; frontend-shape missing `component_kind` rejected — these tests positively assert the canonical bug surface, so if backend validation is ever weakened to accept frontend shape the test fires before the architectural drift ships). **Modified files**: `useFocusTemplateDraft.ts` — load path's `rec.rows ?? []` cast-through-unknown replaced with `backendToFrontendRows(rec.rows ?? [])`; save body's `rowsRef.current` wrapped with `frontendToBackendRows(...)`; both save-response-echo paths (primary success + 410-retry success) updated to `backendToFrontendRows(updated.rows ?? [])`; net delta ~10 LOC + 4 explanatory comments anchoring the F-3.1a discipline; **no other behavior touched** — multi-hook-mount contract preserved, draftRef discipline preserved, 410-retry preserved, edit-session semantics preserved, dirty-state OR-of-4 preserved, debounced save unchanged, discard unchanged. `useFocusTemplateDraft.test.tsx` — added new test `F-3.1a — load adapts canonical backend shape to frontend view` (load-side verify-against-pre-fix; mock returns backend-shape `placement_id` / `component_name` / `starting_column=4`; asserts hook surfaces `id="w-canonical"` / `widget_slug="day-strip-widget"` / `column_start=5` / `chrome={daysVisible:7}`); updated existing stale-closure regression test's payload assertion from `chrome.elevation` to `prop_overrides.elevation` (the test now correctly asserts the canonical wire shape post-adapter — payload IS backend-shape after `frontendToBackendRows`). **Verify-against-pre-fix discipline applied for BOTH adapter wirings** per Step 6: (a) **load-side** — commented out `backendToFrontendRows(...)` call in load path → new test fails with `p.id === undefined` / `widget_slug === undefined` / `column_start === undefined` / `chrome === undefined` → restored; (b) **save-side** — commented out `frontendToBackendRows(...)` wrap in save body assembly (cast-through-unknown preserved type clean) → stale-closure regression test fails with `payload.rows[0].placements[0].prop_overrides?.elevation === undefined` (because pre-fix payload has `chrome.elevation` instead) → restored; (c) **backend save-side contract** — `TestPreAdapterShapeRejected` × 2 prove backend `_validate_rows` rejects pre-adapter shape (frontend `id` / missing `component_kind`) with `InvalidTemplateShape` — this is the canonical evidence that without the adapter, the save would 422 at the backend boundary as F-3.1 diagnosed. **Tests**: 23 new adapter unit + 1 new load-side hook integration + 6 new backend real-contract integration = **30 new tests**. Full vitest **209 files / 2840 tests + 1 todo passing** (+1 file / +25 tests vs F-3 baseline 208/2815); previously-flaky `Tier2TemplatesEditor.test.tsx` passed cleanly in this run (still considered known-flaky per cross-arc history). Full backend focus-template suite **93 tests passing** (87 prior + 6 new). tsc clean (`npx tsc -b` per F-1 lesson); vite build 5.45s; bundle **6,080,090 bytes vs F-3 baseline 6,078,810 = +1,280 / +0.02%** (well within ±1% per F-3.1a gate). **LOC**: production ~10 (hook wiring) + ~165 (adapter module) ≈ 175 production; tests ~245 (adapter) + ~40 (new hook test) + ~210 (backend contract) ≈ 495 tests; **~670 total**, well within ~300-500 midpoint to ~700 worst-case band. **Locked decisions honored**: Path A frontend adapter, NOT backend schema change (#1, #3); hook-level adapter location per consumer audit (#2); frontend `WidgetPlacement` type unchanged (#4); integration test mandatory + real-contract via `_validate_rows` direct import (#5, Path Y); chrome ↔ prop_overrides per Step 1e audit (#6); no Chrome section UI work (#7 — F-3.1b territory). **Architectural questions surfaced during build**: (a) integration test infrastructure — Path X (real HTTP test client exercising the hook against a live backend) does not exist in the codebase today and was out of scope to build; Path Y (backend pytest importing `_validate_rows` directly + asserting against canonical adapter output JSON fixtures) is the canonical contract gate until a hook-to-HTTP integration substrate exists. The fixtures in `test_focus_template_f3_1a_adapter_contract.py` are written to mirror `_placement-adapter.ts::frontendToBackendRows` output exactly; drift between the two surfaces as a backend test failure. (b) Configurable widget props (Q5 in the F-3.1 investigation) — confirmed they flow through the SAME adapter path as the placement shape itself because the inspector writes them into `placement.chrome` (which adapter maps to backend `prop_overrides`); F-3.1a fixes both simultaneously. No separate bug, no separate fix path needed. (c) Off-by-one column indexing — frontend hook code (`WidgetPlacement.column_start` comment line 50) explicitly documents 1-indexed; backend `_validate_placement` line 199 explicitly enforces `starting_column >= 0` (0-indexed); adapter subtracts 1 on send, adds 1 on load, clamps at 0 defensively. (d) Legacy JSONB tolerance — `backendToFrontendPlacement` accepts EITHER shape (canonical `placement_id` OR legacy `id` key) so existing template rows in the DB authored before F-3.1a (if any exist with frontend-shape JSONB stuck via direct DB writes or seed scripts) round-trip without loss; same tolerance on row shape (missing `column_count` defaults to 12). (e) Row-index pass-through — backend `_validate_rows` doesn't require or validate `row_index`; adapter preserves it for round-trip fidelity but treats it as inert metadata. (f) Adapter location decision was 100% evidence-based per Step 1 audit: focus-builder consumers all read from `rowsDraft` (hook state); composition-canvas/ consumers are a separate module already using backend shape; placing the adapter in the service would either double-translate or force type widening that breaks composition-canvas. **No backend changes, no migrations, no canon doc edits beyond STATE.md, no C-1 primitives modified, no C-2 hook contracts altered beyond adapter wiring at load+save boundaries, no resolver internals touched, no tier-aware editor surfaces modified, no VerticalGroupedTree changes, no InheritedCoreInspectorPanel changes, no focus-types.ts changes, no widget palette / canvas / drag-drop changes (F-3 behavior preserved verbatim — F-3.1a fixes ONLY the wire-shape mismatch), no Chrome section UI (F-3.1b territory), no theme picker (F-4), no breadcrumb / coexistence banner (F-5).** Migration head unchanged: `r103_focus_templates_edit_session`. **Next**: F-3.1b (Chrome section addition + further inspector polish on top of the now-persistent widget state).

- **2026-05-19 — DECISIONS.md updated (uncommitted): filed two discovered-canon entries from F-3** — Canon-update-only arc; no code changes. Two entries appended to `DECISIONS.md`: (1) "Core placement position is structurally immutable in Focus Builder canvas" — the inherited core renders ABOVE the widget rows in flex-col layout, NOT interleaved into the rows-grid; widgets occupy rows BELOW the core; investigation Q-25/Q-26/Q-27 conflict-resolution model is NOT exercised because conflicts can't happen at the layout level; canonical mockup's day-strip-above-kanban layout is NOT achievable in current Focus Builder. Implication: future arcs must NOT add overlap-detection helpers preemptively; widgets-above-core requires a future canvas-layout-v2 refactor first. Reference impl: F-3's `FocusBuilderCanvas`. (2) "Component registry requires ≥3 configurableProps per registration" — `component-config.test.ts` enforces this minimum; F-3 initial 1-prop-per-widget seeds failed full-suite (passed in isolation); widened to 3 realistic props each. Implication: new widget registrations must include ≥3 meaningful configurableProps from initial seed. Reference: `component-config.test.ts` is source-of-truth. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+24 lines, format matches existing F-series canon entries verbatim — prose-paragraph, date-prefixed, inline `code`, `---` separator), `STATE.md` (this entry). No code touched. No tests required.

- **2026-05-19 — Sub-arc F-3 shipped (uncommitted) — widget palette + drag-to-canvas + accessory placement editing** — Third of 5 sub-arcs from the 2026-05-18 Focus Builder investigation. Operators can now drag widgets from a categorized palette onto the template canvas, click placed widgets to edit configuration + chrome via the selection-driven inspector, and remove via Delete key or Remove button. `useFocusTemplateDraft` extended with 4 widget methods (`addWidget` / `updateWidget` / `removeWidget` / `moveWidget`) operating on a new fourth tracked draft blob (`rows`) via the multi-hook-mount + C-2.1.4 draftRef discipline (rowsRef always-latest; save reads ref, NOT closure; rowsDraft excluded from save deps). isDirty extended from OR-of-3 to OR-of-4. Three placeholder widgets seeded in the component registry (Day Strip / Today Pin / Map) — inert visual stand-ins demonstrating the palette → drag → canvas pipeline end-to-end; real implementations land in vertical-specific arcs. **New files**: `frontend/src/lib/visual-editor/widget-palette.ts` (WIDGET_CATEGORIES + slug-to-category mapping + lookup helpers; 5 unit tests); `frontend/src/components/widgets/focus-builder/PlaceholderWidgets.tsx` (3 inert React components — DayStripWidget / TodayPinWidget / MapPlaceholderWidget — each rendering name + lucide icon + one-line description in a frosted-elevated chrome); `lib/visual-editor/registry/registrations/focus-builder-widgets.ts` (3 registerComponent wraps with kind:widget + canvasPlaceable:true + ≥3 configurableProps each per backfill discipline); `bridgeable-admin/components/builder-primitives/WidgetPalette.tsx` (consumer-opaque categorized palette primitive — categories with PaletteItem arrays; @dnd-kit useDraggable per item; empty-state hint; disabled state; ~150 LOC + ~110 LOC tests / 6 tests); `bridgeable-admin/components/focus-builder/FocusBuilderPalette.tsx` (Focus-specific wrapper — reads registry via getByType("widget")+isCanvasPlaceable, groups by widgetCategoryFor, namespaces ids with `palette-widget:<slug>`; ~85 LOC + ~85 LOC tests / 5 tests); `bridgeable-admin/components/focus-builder/WidgetInspectorSection.tsx` (selection-driven widget inspector — reads registry by name, renders configurableProps via a minimal control dispatcher (number → ScrubbableButton, boolean → checkbox, fallback text), placement column-span control, Remove button; ~170 LOC + ~95 LOC tests / 6 tests including `findPlacementById` helper). **Modified files**: `hooks/useFocusTemplateDraft.ts` — added RowsBlob / FocusRow / WidgetPlacement typed view (cast through unknown at service-contract boundary since the on-the-wire shape is `Array<Record<string, unknown>>`); added rowsDraft/rowsSnapshot state + rowsRef draftRef; load + save + 410-retry all round-trip rows; discard restores rows snapshot; 4 mutators implemented (addWidget returns UUID via existing generateSessionToken helper; updateWidget merges chrome via spread; removeWidget drops empty rows after filter; moveWidget strips-then-appends to target row); +260 LOC production (within 150-250 budget plus the type-cast helpers) + +280 LOC tests (10 new tests covering load/add/update/remove/move/save/410-retry/discard/stale-closure). `components/focus-builder/FocusBuilderCanvas.tsx` — canvas wrapper now `useDroppable({ id: CANVAS_DROP_ZONE_ID, disabled: mode !== "template" })` with isOver-driven dashed-outline drop-zone highlight; rendering loop replaced (core placement no longer absolutely-centered — flex-col layout now stacks core then widget rows beneath); new WidgetRowsLayer + PlacedWidget components render rows-draft placements via grid-12-cols; placed widget click → setSelection { kind:'widget', id }; component lookup via getByName("widget", slug) renders the registered React component with placement.chrome spread as props; +150 LOC + +120 LOC tests (3 new). `components/focus-builder/FocusBuilderInspector.tsx` — dispatch table grew cleanly to 2×5+ via early-return for widget selection in template mode (`if (selection.kind === "widget") return <WidgetInspectorSection ...>`); findPlacementById lookup against templateHook.rowsDraft; missing-widget fallback state. `components/focus-builder/FocusBuilderRightRail.tsx` — palette placeholder swapped for FocusBuilderPalette wrapper; disabled prop wired from mode (`paletteDisabled = mode !== "template"`); "cores have no widgets" hint visible when disabled. `components/focus-builder/FocusBuilderPage.tsx` — DndContext mounted at page root with PointerSensor (6px activation distance per @dnd-kit canonical pattern); onDragStart sets activeDragLabel; onDragEnd extracts slug from palette-widget: prefix via paletteItemIdToSlug, calls templateHook.addWidget, auto-selects new widget; DragOverlay renders a slug-labeled ghost; Delete/Backspace key handler on selection.kind==='widget' removes the widget + resets selection (guarded against editable focused elements; selectionRef pattern to avoid stale closures in the keydown listener); rowsDraft passed through to canvas. `auto-register.ts` extended with `./registrations/focus-builder-widgets` import. **Tests added/updated**: 10 hook (`useFocusTemplateDraft.test.tsx` F-3 describe block — addWidget round-trip, updateWidget merge, removeWidget+empty-row-drop, moveWidget cross-row, save+rows-in-payload, **stale-closure regression for updateWidget verified against pre-fix variant** (temporarily replaced `rowsRef.current` with `rowsDraft` closure read, suite failed at `payload.rows[0].placements[0].chrome.elevation` assertion as expected with stale empty rows, reverted), 410-retry preserves rows, discard restores snapshot) + 6 WidgetPalette primitive + 5 FocusBuilderPalette + 6 WidgetInspectorSection + 3 FocusBuilderCanvas (placed widgets render / click selects / drop zone present in template mode) + 5 widget-palette taxonomy. **Updated tests**: FocusBuilderRightRail.test.tsx (palette now real; theme stays placeholder; new disabled-by-mode coverage; DndContext wrapping); FocusBuilderInspector.test.tsx fake template-hook now includes rowsDraft + 4 widget mutator vi.fns to satisfy `UseFocusTemplateDraftResult`; FocusBuilderPage.test.tsx the "Arrives in F-3" assertion removed, "Arrives in F-4" preserved. Full vitest **208 files / 2815 tests + 1 todo passing** (+4 files / +37 tests vs F-2 baseline 204/2778); 1 failing test is the same pre-existing flaky `Tier2TemplatesEditor.test.tsx` chrome-preset-live-cascade (documented across C-2.2b/E-1/F-1/F-2 as known flaky — passes in isolation, fails under parallel load; NOT introduced by F-3). tsc clean (`npx tsc -b` per F-1 lesson); vite build 5.31s; bundle 6,078,810 bytes vs F-2 baseline 6,063,290 = **+15,520 / +0.26% (well within ±3% gate)**. **No backend changes, no migrations, no canon doc edits beyond STATE.md, no C-1 primitives modified, no C-2 hook contracts altered beyond useFocusTemplateDraft additions, no useFocusCoreDraft modifications (cores have no widgets), no resolver internals touched, no tier-aware editor surfaces modified, no VerticalGroupedTree changes, no InheritedCoreInspectorPanel changes, no focus-types.ts changes.** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: hook layer extension with 4 widget methods (#1-5); @dnd-kit DndContext at page root (#6); palette-item-id format `palette-widget:<slug>` (#7); WidgetPalette consumer-opaque primitive (#10); 3-category taxonomy (#14 — ancillaries / map / information); 3 placeholder widgets seeded with minimal real props (#15-17); selection.kind === 'widget' dispatch (#18-20); 2×5+ inspector dispatch (#24); Delete key at page level with editable-focus guard (#25); Remove button in inspector (#26); DragOverlay rendered (#27); drop zone subtle highlight on drag-over (#28); no theme picker (#30); no breadcrumb (#31); no tree search (#32); no multi-select (#33); no tree-affordance widget creation (#34). **Architectural questions surfaced during build**: (a) component registry path — lives at `frontend/src/lib/visual-editor/registry/` with the introspection API exposing `getByType` / `getByName` / `isCanvasPlaceable`; registrations split across N shim files (`./registrations/*.ts`) imported by `./auto-register.ts`; the registry already supported `kind: "widget"` filtering — no adaptation needed. F-3 added one new shim file `./registrations/focus-builder-widgets.ts` and one `auto-register` import line. (b) `kind: "widget"` filter adaptation — none required; the existing 8-ComponentKind enum already included `widget`, and `getByType("widget").filter(isCanvasPlaceable)` is the canonical query shape (Phase 1 audit confirmed). (c) Backfill discipline (`every Phase 1 registration has ≥3 configurableProps`) — initial 1-prop-per-widget surfaced a green-on-isolation-red-on-full-suite failure; widened each placeholder widget to 3 configurableProps with realistic-shape declarations (Day Strip: daysVisible/highlightToday/weekStartsOn; Today Pin: showCount/compact/label; Map: zoom/showLegend/aspect). (d) Drop position resolution — F-3 ships the SIMPLE path: addWidget appends to last row or creates a new row when no position is provided; conflict resolution per Q-26/27 (sliding inherited core to non-overlapping cells) is NOT ACTIVELY EXERCISED because the inherited core renders OUTSIDE the rows-grid (it sits ABOVE the WidgetRowsLayer per the canvas's flex-col layout). Inherited core's row immutability is therefore preserved structurally rather than enforced by overlap logic. The conflict-resolution helper is reserved for a later phase if the visual model evolves to interleave core + widgets in one grid. (e) Type compatibility — `RowsBlob` (typed FocusRow array) is structurally compatible with `Array<Record<string, unknown>>` (the service contract shape) but TypeScript can't see through named-interface vs index-signature; resolved with cast-through-unknown at the boundary (load + save snapshot + payload). Service contract not changed (no backend churn). (f) Bundle delta — F-3 added @dnd-kit usage that was ALREADY part of the bundle (used by scheduling dispatch); marginal cost was the new components + 3 placeholder widget registrations. +15,520 bytes is comparable to F-1's +15,159 and F-2's +11,340. **Visual fidelity of placed widgets**: the placeholder widgets render with frosted-elevated chrome (surface-elevated bg + border-subtle + shadow-level-1) per the canonical mockup at `docs/design/canonical-mockups/funeral_scheduling_apple_pre_liquid_glass.html`. The grid-12-col row layout matches the canonical-mockup row substrate. Real widget renderings ship in vertical-specific arcs. **Next**: F-4 (theme picker — replaces the "Arrives in F-4" placeholder in FocusBuilderRightRail's theme region; surfaces `--space-accent` + tokens.css overrides for live preview against the canvas).

- **2026-05-19 — DECISIONS.md updated (uncommitted): filed multi-hook-mount discovered canon from F-2 build** — Canon-update-only arc; no code changes. One entry appended to `DECISIONS.md`: "Multi-hook-mount pattern for builder UIs surfacing heterogeneous subjects". When a builder page renders one of N subject types (Focus Builder's `?subject=core:<id>` vs `?subject=template:<id>`), all N draft hooks mount unconditionally — each receiving real subject id when active, `null` when inactive. The component resolves `activeHook` from subject kind. Conditional mount would violate React rules-of-hooks; the unconditional-with-nullable-id pattern is canonically correct. Implication: draft hooks for builder consumption must accept `null | id` and short-circuit cleanly when null (no fetch, no save timers, sensible empty defaults). Hook authors must test the null case. Reference implementation: F-2's `FocusBuilderPage`. Pattern carries forward to F-3 (potential third widget hook), Page/Document/Workflow Builder. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+10 lines, format matches the two 2026-05-18 entries verbatim — prose-paragraph, date-prefixed, inline `code`), `STATE.md` (this entry). No code touched. No tests required.

- **2026-05-19 — Sub-arc F-2 shipped (uncommitted) — selection-driven inspector composition + canonical canvas atmosphere** — Second of 5 sub-arcs from the Focus Builder investigation. F-1's read-only shell becomes operator-editable: click core → chrome inspector; click background → substrate + typography inspector (template editing only — cores have no substrate/typography vocabulary, so background-click on a core falls through to chrome editing); Esc OR subject change → deselect. **Canvas atmosphere is canon-matched**: when editing a Tier 1 core, canvas renders the canonical four-layer fallback (three radial gradients + linear base) sourced verbatim from `docs/design/canonical-mockups/funeral_scheduling_apple_pre_liquid_glass.html` and mirroring Tier1CoresEditor's preview pane (E-1.1 canon — single inline-style constant `CANONICAL_FOUR_LAYER_FALLBACK` exported for testing); when editing a Tier 2 template, canvas renders the template's resolved substrate via substrate-resolver. Core placement card composes via chrome-resolver (cascade for templates, direct draft for cores). Single placement only — widget placements + drag-to-canvas land in F-3. **New files**: `FocusBuilderCanvas.tsx` (~250 LOC + ~210 LOC tests; mode prop discriminates 'core'/'template'/'empty', exposes `CANONICAL_FOUR_LAYER_FALLBACK` for reuse + verification, click handlers wire to FocusBuilderSelectionContext, placement carries `data-selected` reflecting `selection.kind === 'core'`); `FocusBuilderInspector.tsx` (~430 LOC + ~225 LOC tests; selection-driven conditional rendering — empty state when selection.kind === 'none', chrome section for 'core' or 'background-on-core', substrate + typography sections for 'background-on-template'; per-row inheritance indicators on template chrome reuse C-2.3's PropertyRowInheritance contract verbatim — resolver `sources.chrome_sources[field]` maps "tier1" → `{tier: "Tier 1 core"}`, "tier2"/"tier3" → "explicit", null → no decoration; substrate + typography rows always "explicit" since cores are substrate/typography-free; **sections kept INLINE** in this file as sub-components rather than extracted to separate files — each is ~50-80 LOC of straight C-1 primitive composition with no shared state, and inline keeps the selection-driven dispatcher visible in one place; "View canonical core" button — eye icon + ghost variant — visible only in template mode AND when `onOpenInheritedCorePanel` supplied; opens the C-2.2c `InheritedCoreInspectorPanel` reused verbatim, including its three-path dirty-state confirm dialog); `FocusBuilderRightRail.tsx` (~55 LOC + ~50 LOC tests; three vertically-stacked sections — inspector top with `flex-[2]`, widget-palette placeholder "Arrives in F-3", theme placeholder "Arrives in F-4"). **Modified files**: `FocusBuilderPage.tsx` rewritten — mounts both `useFocusCoreDraft` AND `useFocusTemplateDraft` (one is bound to a real id, the other receives null and is a no-op per the hook's own contract); top bar gains dirty/last-saved indicators matching FocusEditorPage's "Auto-saved Xs ago" UX verbatim (same `relativeTime()` helper + 5-second force-refresh interval + `beforeunload` browser confirm); resolver-driven `sources` state fetched after every save round-trip (deps on `template.template_slug + template.vertical + template.version`); inherited core fetched from `template.inherits_from_core_id` (template editing only); URL-driven selection reset — `useEffect([subjectKey])` resets selection to `{ kind: 'none' }` on subject change; window Esc keydown handler also resets selection; InheritedCoreInspectorPanel mounted absolutely over the right rail, accepts `saveDraft` + `discardDraft` from the templateHook so its dirty-state confirm dialog Just Works; navigating Tier 1 from the panel rewrites `?subject=core:<id>`. `FocusBuilderPage.test.tsx` extended from 5 to 15 tests covering selection wiring, canvas mode discrimination, View-canonical-core visibility + click flow, palette/theme placeholders, Esc deselect, subject-change selection reset. **Removed files**: `FocusBuilderCanvasPlaceholder.tsx` + `FocusBuilderRightRailPlaceholder.tsx`. **C-2.3 per-row inheritance pattern reused without adaptation**: `PropertyRow` already accepts `inheritanceSource` + `onReset` — F-2 wires them via the same resolver provenance Tier2TemplatesEditor uses, with one tightening: substrate + typography rows always pass `"explicit"` (cores are substrate/typography-free, so no inherited state exists; the reset ↺ button still surfaces correctly when an explicit value is authored). **Background-click on cores ambiguity**: cores have no substrate/typography vocabulary; F-2 resolves by falling through — when `mode === 'core'` AND `selection.kind === 'background'`, the inspector renders the chrome section (chrome is the only editable surface). This means clicking the canvas of a core IS clicking the core for editing purposes; documented inline + in the build report. **InheritedCoreInspectorPanel reused verbatim**: same component, same three-path dirty-state confirm dialog from C-2.2c. Save-and-continue calls `templateHook.save`; Discard-and-continue calls `templateHook.discard`; Cancel keeps panel open. **Dirty-state UX matches FocusEditorPage**: same `relativeTime()` formatter, same `dirty-indicator` + `last-saved-indicator` test ids, same 5-second force-refresh, same `beforeunload` guard. **Tests**: 44 new + updated tests across 4 files in `components/focus-builder/` (15 FocusBuilderPage integration + 8 FocusBuilderCanvas unit + 9 FocusBuilderInspector unit + 2 FocusBuilderRightRail smoke + 10 FocusBuilderPage extended coverage rolled in). Full vitest **204 files / 2778 tests + 1 todo passing** (+4 files / +33 tests vs F-1's 200/2745); 1 failing test is the same pre-existing flaky `Tier2TemplatesEditor.test.tsx` `preview-card chrome preset updates live when chrome pill clicked` (documented in C-2.2b/E-1/F-1 STATE entries — fails identically in isolation; not introduced by F-2). tsc clean (`npx tsc -b` per F-1 lesson); vite build 5.00s; bundle 6,063,290 bytes vs F-1 baseline 6,051,950 = **+11,340 / +0.19% (well within ±3% gate)**. Backend `test_focus_template_inheritance_e1_defaults.py` + `test_verticals.py` 38/38 green (no backend changes — F-2 is pure frontend). **LOC**: production ~735 (FocusBuilderCanvas 250 + FocusBuilderInspector 430 + FocusBuilderRightRail 55) + FocusBuilderPage net delta ~140 + tests ~600 ≈ **~1,475 total**, slightly under the ~1,500–2,200 midpoint target — F-2's heavy reuse of C-1 primitives + C-2.2c InheritedCoreInspectorPanel kept new LOC low. **No backend changes, no migrations, no canon doc edits beyond STATE.md, no C-1 primitives modified, no C-2 hook contracts altered, no resolver internals touched, no Tier 1 / Tier 2 editor surfaces modified, no VerticalGroupedTree or FocusBuilderTree changes, no widget palette (F-3), no theme picker (F-4), no breadcrumb / coexistence banner / tree search (F-5), no multi-select.** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: selection state lives in FocusBuilderSelectionContext (#1, #2, #3); canvas atmospheric canon for cores via CANONICAL_FOUR_LAYER_FALLBACK + substrate-resolver for templates (#7); single core placement only — widgets in F-3 (#8); fixed-fit-to-viewport canvas (#9); C-1 primitives unchanged (#10); three section types — chrome/substrate/typography (#11); per-row inheritance indicators on template chrome reuse C-2.3 (#12); InheritedCoreInspectorPanel reused via "View canonical core" button — only visible for templates (#13, #14, #15); C-2.2c dirty-state confirm carries over (#16); dirty/last-saved UX matches FocusEditorPage (#17); widget palette + theme picker = placeholders only (#18, #19, #20, #21). **Architectural questions surfaced during build**: (a) background-click on cores — cores have no substrate/typography vocab; resolved by falling through to chrome editing (the only editable surface); documented inline; (b) inspector sections inline-vs-extracted choice — kept inline this iteration; each section is ~50-80 LOC and inline keeps the selection dispatcher visible in one place; future F-3 widget palette + F-5 polish may motivate extraction; (c) one-of-both-hooks mounted unconditionally — both `useFocusCoreDraft` AND `useFocusTemplateDraft` mount, each with `null | id` based on subject kind; the unbound hook is a no-op per the hook's own contract; alternative (Suspense or conditional hook mount) would violate React's rules-of-hooks; (d) the inspector's mode/empty + selection-kind dispatch is a 2×4 table; covered by the test matrix but worth keeping a close eye on as F-3 adds widget selection. **Next**: F-3 (widget palette + drag-to-canvas + accessory placement editing).

- **2026-05-18 — DECISIONS.md updated (uncommitted): filed two discovered-canon entries from F-1 + F-1.1** — Canon-update-only arc; no code changes. Two entries appended to `DECISIONS.md`: (1) "Cores are canonical-shared-across-verticals" — `focus_cores` has no `vertical` field; vertical attribution in operator-facing surfaces (Focus Builder tree) is derived from inheriting templates (vertical_default templates pin core to vertical; platform_default makes cross-vertical; zero-template cores fall into "Unclassified"). Implication: future builder UIs (Page/Document/Workflow) must derive vertical placement from inheriting templates, not from non-existent core fields. (2) "Template vertical is design-time-permanent" — `focus_templates.vertical` is immutable through `update_template`; cross-vertical migration requires two-step pattern (deactivate at old vertical + create at new vertical), idempotent on subsequent runs. Implication: future UI surfaces must not expose "change vertical" as inline edit on a template — the operation is structurally a "create new in target + deactivate old" workflow. Reference impl: F-1.1's seed migration code at `backend/scripts/seed_focus_template_inheritance.py`. Active arc → no active arc, canon update only. **Files modified**: `DECISIONS.md` (+38 lines, format-adapted to match existing prose-paragraph + date-prefix style), `STATE.md` (this entry). No code files touched. No tests required.

- **2026-05-18 — Sub-arc F-1 shipped (uncommitted) — read-only Focus Builder shell + vertical-grouped tree + URL state** — First of 5 sub-arcs from the 2026-05-18 Focus Builder investigation. New route `/studio/builder/focuses` mounts a three-region layout (280px tree / flex canvas / 320px right rail) that hides the "Tier 1 vs Tier 2" mechanics from operators. Tree groups verticals → focus-types → cores → templates with a "+ New <Core>-based template" pseudo-child under each core (F-1 click handler is `console.log` stub; F-3 wires the create modal). Selection updates URL via `?subject=core:<id>` / `?subject=template:<id>` per Q-40 LOCKED (b); preserves `?return_to=`. Canvas + right rail are placeholders pointing at F-2's editing layer + F-2's inspector. **F-1 is READ-ONLY — no editing of any kind.** **New files**: `frontend/src/lib/visual-editor/focus-types.ts` (FOCUS_TYPES + CORE_SLUG_TO_FOCUS_TYPE + focusTypeForCore + focusTypeLabel; 5 unit tests); `bridgeable-admin/components/builder-primitives/VerticalGroupedTree.tsx` (consumer-opaque tree primitive — knows nothing about cores/templates/focus-types/scope chips; `metadata` field opaque; recursive children with CSS depth indentation; 8 tests); `bridgeable-admin/components/focus-builder/FocusBuilderPage.tsx` (root, mounts FocusBuilderSelectionProvider + reads `?subject=` + handles URL writes on tree selection; 5 integration tests); `FocusBuilderTree.tsx` (Focus-specific consumer; fetches verticals + cores + templates in parallel; builds tree via pure `buildFocusBuilderTree`; localStorage expansion persistence under `bridgeable.focus-builder.tree-expanded`; auto-expansion of subject trail on load; 11 tests covering pure tree-build + default-expansion + integration); `FocusBuilderSelectionContext.tsx` (Selection = none/background/core/widget(id); F-1 wires provider but selection stays `{kind: 'none'}` — F-2 USES it); `FocusBuilderCanvasPlaceholder.tsx` (loads selected subject via service GETs, renders identity card with slug + version + chrome preset for core, plus inherited-core info for template); `FocusBuilderRightRailPlaceholder.tsx` (empty state "Editing arrives in F-2"). **Modified files**: `bridgeable-admin/pages/studio/StudioShell.tsx` — added `<Route path="builder/focuses" element={<FocusBuilderPage />} />` BEFORE the EditModeDispatcher catch-all so `parseStudioPath` doesn't misclassify "builder" as a vertical slug. **Verticals endpoint + service**: BOTH already existed pre-F-1 — `backend/app/api/routes/admin/verticals.py` (GET /api/platform/admin/verticals returning slug-PK rows sorted by sort_order, status filter via `?include_archived=`) and `frontend/src/bridgeable-admin/services/verticals-service.ts`. Step 1+2 of the prompt were no-ops. **Spec-Override surfaced + resolved (focus_cores has no `vertical` field)**: the build prompt asked to "verify cores have a vertical field — likely it does." Verification: `backend/app/models/focus_core.py` exposes NO `vertical` column; `focus_templates.vertical` is the only vertical association (FK to verticals.slug, nullable). Resolution: F-1's `buildFocusBuilderTree` derives a core's vertical from its inheriting templates — vertical_default templates pin the core to their vertical; platform_default templates make the core "cross-vertical" (appears under every published vertical); cores with zero templates fall into the "Unclassified" pseudo-vertical at the bottom of the rail (Q-7 LOCKED behavior). This is the right F-1 behavior; F-2+ can refine if core-level `vertical` becomes a DB field. **Default expansion (Q-5)**: Studio active vertical comes from `readLastVertical()` (localStorage key `studio.lastVertical`) OR an explicit `studioActiveVertical` prop. Fallback: alphabetical-first published vertical (excluding "Unclassified"). Inside the active vertical, all focus-types start expanded; cores stay collapsed (per investigation lock). **URL contract**: `?subject=core:<id>` or `?subject=template:<id>` round-trips cleanly — page refresh loads the subject, auto-expands the parent trail in the tree, fires the service GET to populate the canvas placeholder. Empty subject → "Select a focus from the tree to preview." `?return_to=` preserved. **VerticalGroupedTree primitive contract verified consumer-opaque**: zero Focus-specific concepts in the primitive — no core/template/focus-type/scope chip references. Tree consumers (Page Builder, Document Builder, Workflow Builder per investigation §Pattern abstractions) can use the primitive directly with their own `groups: TreeNode[]` shape. `metadata` field is `Record<string, unknown>` — Focus Builder stuffs `{kind, subjectId, scope, parentCoreId}` there; primitive doesn't inspect. **FocusEditorPage coexistence verified**: 19/19 FocusEditorPage tests pass; route `/studio/focuses` still serves the tier-aware editor. **localStorage persistence**: write fires inside the React state updater function so reads after `fireEvent.click(chevron)` see the new value synchronously. **Tests**: 29 new tests (5 focus-types + 8 VerticalGroupedTree + 11 FocusBuilderTree (5 pure + 2 default-expansion + 4 integration) + 5 FocusBuilderPage). Full vitest **200 files / 2745 tests + 1 todo passing** (+5 files / +30 tests vs E-1.1 baseline 196/2715); 1 failing test is pre-existing flaky `Tier2TemplatesEditor.test.tsx` `preview-card chrome preset updates live when chrome pill clicked` (documented in C-2.2b STATE entry as known flaky — passes in isolation, fails under parallel load). tsc clean; vite build 5.22s; bundle 6,051,950 bytes vs E-1.1 baseline 6,036,791 = **+15,159 / +0.25% (well within ±3% gate)**. Backend `test_verticals.py` 25/25 green (no backend changes — F-1 is pure frontend). **LOC**: production ~840 (focus-types 50 + VerticalGroupedTree 215 + FocusBuilderTree 410 + FocusBuilderPage 110 + FocusBuilderSelectionContext 40 + FocusBuilderCanvasPlaceholder 145 + FocusBuilderRightRailPlaceholder 20 + StudioShell modifications 10) + tests ~970 (focus-types 50 + VerticalGroupedTree 140 + FocusBuilderTree 365 + FocusBuilderPage 195 + misc 220) ≈ **~1,810 total**, squarely in the ~1,500–2,000 target midpoint, comfortably under worst-case 2,500. **No backend changes, no migrations, no canon doc edits beyond STATE.md, no tier-aware editor surfaces touched, no C-1 primitives modified, no C-2 hook contracts altered, no editing functionality of any kind (F-2 territory), no widget palette (F-3), no theme picker (F-4), no breadcrumb / coexistence banner / tree search (F-5).** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: DB-backed verticals (Q-1 ✓ pre-existing), curated focus-type taxonomy (Q-2 ✓), frontend mapping table (Q-3 ✓), localStorage expansion (Q-4 ✓), Studio-active-vertical default expansion (Q-5 ✓), no tree search (Q-6 ✓ deferred), Other / Unclassified orphan handling (Q-7 ✓ via template-derived vertical), flat template list + scope chip (Q-8 ✓), Square / SquareDashed leaf icons + depth indent (Q-9 ✓), FocusBuilderSelectionContext colocated with root (Q-14 ✓), `builder-primitives/` + `focus-builder/` directory split (Q-38 ✓), `/studio/builder/focuses` route (Q-39 ✓), `?subject=` URL contract (Q-40 ✓), coexistence with FocusEditorPage (Q-41 ✓). **Architectural questions surfaced during build**: (a) focus_cores has no `vertical` column — resolved by template-derived vertical association; clean long-term path is adding the column if cores genuinely belong to a single vertical, but ambiguous since cores are designed to be canonical-shared-across-verticals chrome (the kanban core is reused by both manufacturing + funeral_home); (b) parseStudioPath classifier doesn't natively support `/studio/builder/focuses` — would misclassify "builder" as a vertical slug; resolved by explicit `<Route path="builder/focuses">` placed BEFORE the EditModeDispatcher catch-all so React Router's static-path match wins; (c) vitest module mock contamination across `FocusBuilderTree.test.tsx` + `FocusBuilderPage.test.tsx` running in parallel — resolved by making the localStorage persistence test seed its expected state explicitly rather than relying on default-expansion behavior. **Next**: F-2 (selection-driven inspector composition via SelectionDrivenInspector + RightRailWithSections primitives + per-row inheritance indicators).

- **2026-05-18 — Focus Builder investigation closed (uncommitted) — F-series sub-arc decomposition recommended** — Investigation deliverable at `docs/investigations/2026-05-18-focus-builder.md` surfaces 42 architectural questions across navigation (vertical-grouped tree, focus-type taxonomy, expansion persistence), selection-driven inspector (context provider for selection state, hook-layer reuse via `useFocusCoreDraft` + `useFocusTemplateDraft` unchanged), widget palette (registry-driven, @dnd-kit boundary at canvas edge), theme picker (substrate + typography combined, immediate auto-save, custom-preset save deferred), breadcrumb (full four-level hierarchical, clickable, dirty/saved chrome in right slot), and component decomposition (`builder-primitives/` directory for reusable bits + `focus-builder/` for Focus-specific composition). **40 of 42 Qs LOCKED** by canon or substrate-compatibility reasoning; **2 TBD with James**: Q-11 (canonical/variant caption copy + placement) and Q-33 (dark mode preview model). Five-sub-arc decomposition recommended: F-1 layout shell + vertical-grouped tree + URL state (~1,500–2,000 LOC) → F-2 selection-driven inspector composition (~1,500–2,200) → F-3 widget palette + drag-to-canvas + accessory placement editing (~1,500–2,500) → F-4 theme picker + preset application (~600–1,000) → F-5 breadcrumb + dirty-state polish + coexistence deprecation banner (~800–1,200). F-series midpoint ~6,500 LOC, worst case ~9,500. Focus Builder mounts at new route `/studio/builder/focuses`; FocusEditorPage stays at `/studio/focuses` for the coexistence period with deprecation banner. Pattern abstractions extracted into `builder-primitives/` (VerticalGroupedTree, SelectionDrivenInspector, RightRailWithSections, BreadcrumbContext, WidgetPalette, BuilderDragContext) future-proof Page Builder / Document Builder / Workflow Builder. Architectural conflict surfaced: hook layer exposes draft state but lacks `updateWidget(widgetId, partialChrome)` helper for selection-driven widget chrome editing — F-2 adds ~30 LOC surface. Out-of-scope (explicitly deferred): Tier 3 in-place editor (sub-arc D), Page/Document/Workflow Builders, standalone Theme editor refinements, custom-preset persistence, audit substrate, multi-select, zoom/pan, mobile responsive, cross-tenant publishing. F-1 can dispatch immediately once Q-11 + Q-33 lock; remaining sub-arcs gated only by sequential ordering.

- **2026-05-18 — Sub-arc E-1.1 shipped (uncommitted) — E-1 verification follow-ups closed** — Post-E-1 visual verification surfaced two real findings the E-1 build missed. **Finding 1: scheduling-kanban-core chrome only partially canonical.** E-1 shipped delta updates (corner_radius 100→70, padding_token space-6→space-3, backdrop_blur 42→44) but preserved pre-E-1 values on the other four chrome fields: preset `card` (canonical: `frosted`), elevation 37 (canonical: 50), background_token `surface-elevated` (canonical: `surface-frosted`), border_token `None` (canonical: `border-subtle`). E-1.1 re-seeds with FULL canonical chrome state — all 7 fields explicit — bumping core to v9. **Finding 2: Tier 1 editor preview pane atmosphere not mockup-canonical.** Pre-E-1.1 the canvas painted a warm-amber/copper diagonal gradient (`linear-gradient(135deg, #f9d9a6 0%, #e9b27e 30%, #c97d52 60%, #9C5640 100%)`) plus a 30%-opacity 45° repeating stripe overlay — Tier 1 editor chrome that diverged from the canonical mockup. Replaced with the four-layer canonical mockup composition: three radial gradients (warm cream rgba(252,220,180,0.55) at 15%/10%, soft pink rgba(220,170,200,0.40) at 85%/15%, cool blue rgba(180,200,220,0.45) at 50%/90%) over a vertical linear-gradient base (`#f7ebe0 → #f0dfd0`). Hardcoded directly on the editor's preview pane — NOT routed through substrate-resolver (locked decision 2: Tier 1 cores have no substrate vocabulary). Values mirror substrate-resolver's `morning-warm` preset at intensity 100 so operator-facing + admin-facing surfaces feel canon-matched; diagonal-stripe overlay removed for canonical fidelity. **(1) Seed re-shipped with full canonical chrome state** — `backend/scripts/seed_focus_template_inheritance.py`: SCHEDULING_KANBAN_CORE.chrome now specifies all 7 fields explicitly (`preset: "frosted", elevation: 50, corner_radius: 70, backdrop_blur: 44, background_token: "surface-frosted", border_token: "border-subtle", padding_token: "space-3"`). Drift-tolerant `_seed_core` detects content divergence and calls `update_core` to version-bump existing rows; previous v8 record preserved as `is_active=false`. **(2) Tier-2 template version-bump on stale core pin** — `_seed_template` extended with a new "force version-bump when `inherits_from_core_version` lags live core version" path. Rationale: `update_template` preserves the prior `inherits_from_core_version` (immutable through that surface — service-layer captures from the live active core only on `create_template`). The resolver always uses the active core regardless (locked decision 2), but C-2.3's lineage chrome reads `template.inherits_from_core_version` directly, so a stale stamp would display as "v1" in the editor even after the core moves to v9. The new branch calls `create_template` to trigger a version-bump that restamps `inherits_from_core_version` to live core.version on the deactivate-and-insert path. Idempotent: subsequent reruns short-circuit because the stamp matches. **(3) Tier 1 editor canvas atmosphere canonicalized** — `frontend/src/bridgeable-admin/components/visual-editor/Tier1CoresEditor.tsx`: preview-pane `<section data-testid="tier1-preview">` `background` style replaced with the four-layer canonical composition; diagonal-stripe `<div aria-hidden>` overlay deleted; `isolation: isolate` added so the canvas establishes its own stacking context. Inline-style approach is deliberate per locked decision 2 (Tier 1 has no substrate vocabulary). **(4) Canonical mockup committed** — new `docs/design/canonical-mockups/funeral_scheduling_apple_pre_liquid_glass.html` (a self-contained four-column kanban skeleton with frosted cards demonstrating the substrate + chrome + typography canon, no build step, opens directly in a browser) + `docs/design/canonical-mockups/README.md` explaining the canonical-mockup convention. Authoritative reference for the morning-warm substrate composition, frosted chrome preset values, and frosted-text typography preset values — referenced by E-1 and E-1.1. **Tests**: new `frontend/src/bridgeable-admin/components/visual-editor/Tier1CoresEditor.test.tsx` (2 tests) asserts the four-layer canonical composition is present on the canvas + the pre-E-1.1 warm-amber gradient is absent (JSDom normalizes hex to rgb so assertions check both normalized stops `rgb(247, 235, 224)` / `rgb(240, 223, 208)` and explicit hex absence). **Gates**: full vitest **196 files / 2715 tests + 1 todo passing** (+1 file / +2 tests vs E-1's 195/2713); tsc clean; vite build 4.98s, bundle unchanged within ±2%. Backend 264/264 across 9 focus_template_inheritance test files (full backend suite green). **LOC**: ~115 changed (seed ~50 net delta with new version-bump branch + comment block; Tier1CoresEditor ~25 (canvas style replace + overlay delete); new test file ~80; mockup HTML ~210 + README ~22 = docs ~232) — well under the ~300-600 midpoint, comfortably under 900 worst case. **No backend service changes, no schema changes, no migrations, no resolver module changes, no Tier 2 editor surfaces touched, no canon doc edits beyond STATE.md, Frosted Scribe not touched per locked decision 7.** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: full canonical chrome state replacement (#1); editor preview pane canonical composition hardcoded, NOT routed through substrate-resolver (#2); scheduling-fh `inherits_from_core_version` auto-bumps to new active version via the new seed branch (#3); substrate-resolver morning-warm unchanged (#4); no backend schema or migration (#5); resolver modules untouched (#6); Frosted Scribe not touched (#7); canonical mockup HTML committed (#8). **Open architectural question surfaced**: the `_seed_template` version-bump branch is a workaround for `update_template` preserving the prior `inherits_from_core_version` — clean long-term path is either (a) extending `update_template` to optionally restamp from live core, or (b) adding a dedicated `restamp_core_version_pin` service. Not blocking; the seed-only workaround is correct for E-1.1's surface but if multiple templates inherit from the same core in future, each will need a similar code path. **Next**: full Focus Builder UI restructuring (vertical-grouped left rail nav, selection-driven inspector, widget palette, theme preset switcher in right rail, breadcrumb context, right-bar cleanup, drag widgets into Tier 2, Tier 3 work) — explicitly out of scope for E-1.1.

- **2026-05-18 — Sub-arc E-1 shipped (uncommitted) — mockup-canonical-defaults arc closed** — Post-C-2 verification revealed three findings: (1) Tier 2 templates didn't inherit Tier 1's exact look on creation — substrate-resolver's `morning-warm` preset at intensity 70 produced a flat cream wash rather than the canonical warm sunrise gradient; (2) Tier 1 scheduling-kanban-core chrome diverged from mockup card chrome (corner_radius 37 / padding_token space-6 / no backdrop_blur vs. mockup 70 / space-3 / 44); (3) new Tier 2 templates via the C-2.2c modal started with empty substrate/typography rather than canonical mockup defaults. E-1 closes all three. **(1) Substrate-resolver morning-warm canonical** — `frontend/src/bridgeable-admin/lib/visual-editor/substrate-resolver.ts`: `morning-warm` preset rebuilt as a four-layer composition. Three radial gradients (warm cream `rgba(252, 220, 180, α)` ellipse-at-15%-10%; pink `rgba(220, 170, 200, α)` ellipse-at-85%-15%; cool blue `rgba(180, 200, 220, α)` ellipse-at-50%-90%) over a token-bindable linear-gradient base (`accent_token_1` top stop → `base_token` bottom stop; default `surface-elevated` → `surface-base`). Radial alphas at intensity 100 are canonical 0.55 / 0.40 / 0.45; intensity scales linearly to 0 (no radials). Radial colors, positions, ellipse sizes are HARDCODED — not operator-tunable. `accent_token_2` is unused for morning-warm. SUBSTRATE_PRESETS entry updated: `intensity: 100` default + `base_token: surface-base` + `accent_token_1: surface-elevated` + `accent_token_2: null`. Other presets preserve the legacy two-stop diagonal composition via fall-through branch. Resolver function signature unchanged (`resolveSubstrateStyle(view, tokens) → CSSProperties`). **(2) Backend create-time defaults** — new `backend/app/services/focus_template_inheritance/defaults.py` canonicalizes `DEFAULT_CHROME_OVERRIDES = {}` (cascade chrome from core unchanged), `DEFAULT_SUBSTRATE = {preset: "morning-warm", intensity: 100, base_token: "surface-base", accent_token_1: "surface-elevated", accent_token_2: None}`, `DEFAULT_TYPOGRAPHY = {preset: "frosted-text", heading_weight: 600, body_weight: 500, heading_color_token: "content-strong", body_color_token: "content-base"}`. `focus_templates_service.create_template` calls `is_empty_blob(blob)` (True for None or `{}`) on each — empty-or-null populates the canonical default; any key-present mapping (even `{preset: None}`) is treated as explicit operator intent and respected verbatim. `update_template` semantics unchanged. **(3) Seed alignment** — `backend/scripts/seed_focus_template_inheritance.py`: scheduling-kanban-core chrome bumped from `{"preset": "card"}` to `{"preset": "card", "corner_radius": 70, "padding_token": "space-3", "backdrop_blur": 44}` — explicit overrides on top of the `card` preset stamp the mockup-canonical values at the row level. scheduling-fh template bumped to fully-explicit canonical mockup defaults matching DEFAULT_SUBSTRATE + DEFAULT_TYPOGRAPHY; chrome_overrides explicitly `{}` (cascade from core unchanged). Seed is drift-tolerant: `_seed_core` + `_seed_template` detect content divergence on rerun and call `update_core` / `update_template` to version-bump existing rows; previous version preserved as `is_active=false`. Re-running on unmodified seed is a clean no-op. **Frosted Scribe template intentionally NOT seeded** — no such template exists in the codebase today; the prompt's reference appears aspirational. Surfaced. **Tests**: new `backend/tests/test_focus_template_inheritance_e1_defaults.py` (12 tests across 2 classes) covers defaults.py shape + create_template wiring. Five existing tests updated for E-1 contract change: `TestTemplateServiceSubstrate::test_create_default_empty_substrate` + `TestTemplateServiceTypography::test_create_default_empty_typography` now expect canonical defaults; admin API `test_template_response_includes_all_substrate_fields` + `test_template_response_includes_all_typography_fields` updated to expect canonical mockup default values; resolver "empty at both tiers → None" tests (substrate + typography) updated to null the column out directly post-create (simulates pre-E1 seeded rows). Substrate-resolver vitest expanded from 6 to 13 tests: morning-warm intensity 100/50/0; token-binding to linear base stops; other-presets-unchanged regression. **Gates**: backend pytest 241/241 green across 8 focus template inheritance test files; frontend tsc clean; vitest 2712/2714 passing (1 failure is pre-existing flaky `Tier2TemplatesEditor.test.tsx` `preview-card chrome preset updates live when chrome pill clicked` — fails identically with E-1 absent, passes when isolated; not introduced by E-1); vite build clean 5.06s, bundle unchanged within ±2%. **Out of scope per spec**: card content, Tier 1 editor preview CSS, other substrate/typography/chrome presets, right-bar cleanup, widget palette, Tier 3 work. **Migration head unchanged** — pure JSONB content alignment via service-layer versioning.

- **2026-05-18 — Sub-arc C-2.3 shipped (uncommitted) — C-2 sub-arc canonically complete** — Final sub-sub-arc of C-2. Ships the inheritance-surfacing layer + URL consolidation polish that closes C-2 end-to-end. **(1) Per-row inheritance indicators in the Tier 2 inspector** — `PropertyRow` (in `PropertyPanel.tsx`) extended with optional `inheritanceSource?: "explicit" | { tier: string } | null` and `onReset?: () => void` props. Inherited rows render dimmed value (opacity 60) + a brass-toned "↑ inherited from <tier>" mono caption below the row; explicit rows render full-opacity. Driven ENTIRELY by the resolver's `sources.*_sources` provenance (locked decision #4) — Tier2TemplatesEditor calls a new `focusTemplatesService.resolve({template_slug, vertical})` and maps each tier string ("tier1"/"tier2"/"tier3"/null) to a `PropertyRowInheritance`: `"tier1"` → `{tier: "Tier 1 core"}` (chrome only — substrate + typography are tier-1-free by design per resolver); `"tier2"/"tier3"` → `"explicit"`; `null` → no decoration. Existing PropertyRow consumers (Tier1CoresEditor, InheritedCoreInspectorPanel) untouched — neutral rows render exactly as before when no `inheritanceSource` is supplied. **(2) Reset-to-inherited ↺ affordance** — hover-only RotateCcw icon button rendered to the right of the value block, opacity 0 at rest → 100 on row hover or button focus; only renders when row is "explicit" AND `onReset` is wired. New hook methods: `resetChromeOverridesField(fieldName)` / `resetSubstrateField(fieldName)` / `resetTypographyField(fieldName)` on `useFocusTemplateDraft`. Each removes the named key via `delete next[fieldName]` (object identity preserved when key absent — short-circuit prevents needless save trigger), then calls `queueSave` to fire the standard 300ms debounced PUT. Reset is dirty-positive: `isDirty=true` momentarily, snaps back to `false` after the round-trip. Per locked decisions #5/#8 Tier 1 cores editor gets NO inheritance indicators or reset (cascade root). **(3) Tier indicator pill** — brass-toned chip ("Tier 1" / "Tier 2") rendered in FocusEditorPage's top bar adjacent to the "Focuses" title, gated on `(tier===1 && selectedCoreId) || (tier===2 && selectedTemplateId)` so the chip only appears when there's a target to attribute the tier to. `bg-accent-subtle text-accent` with mono font and `data-tier` discriminator for tests. **(4) Interactive lineage chrome** — the existing "Inherits from: X (Tier 1 v<N>)" text in FocusEditorPage's top bar is now a button. Click opens InheritedCoreInspectorPanel (same panel as canvas placement click per locked decision #15); visual unchanged at rest, subtle accent-subtle hover treatment. Dirty-state confirm-before-leave flow inherits from C-2.2c automatically — the panel handles it. Required lifting `inheritedCorePanelOpen` state from Tier2TemplatesEditor up to FocusEditorPage: new optional `inheritedCorePanelOpen` + `onInheritedCorePanelOpenChange` props on the editor; falls back to local uncontrolled state when no controller wired (preserves C-2.2c canvas-placement-click behavior in isolation). Same panel, single source of truth — top-bar lineage and canvas placement are two trigger sites for the same UI. **(5) useSearchParams consolidation cleanup** — Tier2TemplatesEditor's local `useSearchParams` + `useNavigate` (used only to compose the `?tier=1&core=<id>` URL when navigating to Tier 1 from the inherited-core inspector panel) lifted up to FocusEditorPage. The editor now accepts `onNavigateToTier1Core?: (coreId: string) => void` and invokes the parent's handler; FocusEditorPage owns the URL rewrite via its existing setSearchParams. Mechanical refactor — behavior unchanged (deep-linking via ?core / ?template / ?tier / ?return_to still works; navigation between Tier 1 ↔ Tier 2 preserved). Removed unused imports from Tier2TemplatesEditor (`useNavigate`, `useSearchParams`). **Step 1 backend verification**: confirmed resolver `sources.*_sources` shape — flat dicts mapping field name → `"tier1" | "tier2" | "tier3" | null`. Substrate/typography never produce `"tier1"` (cores substrate/typography-free per resolver). `ResolveResponse` already exposes `sources` cleanly. The resolver endpoint exists at `GET /api/platform/admin/focus-template-inheritance/resolve?template_slug=&vertical=&tenant_id=` (NOT keyed by template id) — frontend now consumes via new `focusTemplatesService.resolve(params)` method + new typed `ResolveResponse` + `ResolveSources` exports. No backend changes required, no Spec-Override invoked. **Resolver refetch trigger**: `useEffect` deps `[template.template_slug, template.vertical, template.version]` — refetch after each save round-trip so explicit overrides flip from "tier1 inherited" → "tier2 explicit" without operator action. **Defensive guard**: editor checks `typeof focusTemplatesService.resolve === "function"` before calling — graceful degradation for any consumer whose mock omits the method (no test churn for unrelated suites; the C-2.3 test mocks include it). **Tests**: PropertyPanel +5 new (neutral row, dimmed-inherited + caption, full-opacity explicit, hover ↺ wires onReset + fires, no-onReset hides button) → 11/11. useFocusTemplateDraft +5 new (resetChrome removes field + marks dirty, resetSubstrate, resetTypography, no-op when field absent preserves object identity AND doesn't mark dirty, reset triggers debounced save with the field absent from payload — uses `vi.useFakeTimers` + `advanceTimersByTimeAsync` to confirm the PUT payload shape) → 25/25. Tier2TemplatesEditor +6 new integration (inherited-from caption renders for cascading rows, exactly-one reset ↺ for the single explicit row in fixture, click reset persists `chrome_overrides: {}` via update, controlled panel open prop mounts panel, canvas placement click invokes `onInheritedCorePanelOpenChange(true)`, `onNavigateToTier1Core` fires on "Edit core in Tier 1" with correct coreId) → 22/22. FocusEditorPage +5 new (no pill at Tier 1 no selection, "Tier 1" pill when selected, no pill at Tier 2 no template, "Tier 2" pill when template selected, lineage chrome hidden at Tier 1) → 19/19. **Full vitest 195 files / 2708 tests + 1 todo passing** (+21 vs C-2.2c baseline 2687, all new tests green); tsc clean; eslint clean; vite build 4.74s; bundle 6,036,791 bytes (+4,641 / +0.077% vs C-2.2c 6,032,150 — well within ±2% gate). **LOC**: production ~280 (PropertyPanel +90 + useFocusTemplateDraft +45 + Tier2TemplatesEditor net +75 inherited helper + sources state + reset wiring on 17 rows + lifted panel state + FocusEditorPage +60 tier-pill + interactive lineage + navigateToTier1Core + panel state + service +45 resolve method + types) + tests ~480 (PropertyPanel +95 + useFocusTemplateDraft +110 + Tier2TemplatesEditor +220 + FocusEditorPage +55) ≈ **~760 total — well within the ~1,000-1,500 midpoint target, comfortably under worst-case 2,000**. **No backend changes, no migrations, no canon doc edits, no resolver internals touched, no Tier 1 surfaces touched** beyond the (no-op for them) PropertyRow extension. Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: dimmed-value + caption indicator shape (#1, #2, #3); resolver-driven only (#4); no Tier 1 indicators (#5, #8); hover-only reset ↺ (#6); no reset on inherited (#7); reset removes field per C-2.1.3 contract (#9); reset dirty-positive (#10); brass tier pill (#11, #12, #13); interactive lineage as button → same panel (#14, #15, #16, #17); useSearchParams consolidation mechanical (#18, #19, #20); explicit scope guards honored (#21-25 — no canvas trigger change, no animation polish, no Tier 3, no widget palette). **C-2 sub-arc operationally + canonically complete after this lands.** Coverage from Tier 1 cores authoring (C-2.1) through Tier 2 templates authoring (C-2.2a/a.1/b/c) through inheritance surfacing + URL consolidation (C-2.3) — Studio Focus authoring is end-to-end usable across both tiers. **Next major sub-arc**: deferred work — Tier 3 composition editor, widget palette, theme editor refinements, animation polish, inherited-core trigger surface relocation (possible C-2.3.1 if signal warrants).

- **2026-05-18 — Sub-arc C-2.2c shipped (uncommitted)** — Third and final sub-sub-arc of C-2.2 (Tier 2 templates editor). Ships the operator-authoring entry points that make the editor end-to-end usable: (1) `PropertyPanel` className + `data-testid` passthrough so consumers can mount a second `PropertyPanel` in the same tree without testid collision (closes C-2.2b's known follow-up; prerequisite for the side panel below); (2) `CreateTierTwoTemplateModal.tsx` — mirrors `CreateTierOneCoreModal` shape with Tier 2 adaptations: required "Inherit from core" dropdown (populated from `focusCoresService.list()` filtered to active), `template_slug` immutable post-create with client-side `^[a-z0-9][a-z0-9-]*$` pattern validation (≤96 chars), `display_name` required, `description` optional, scope radio (`platform_default` / `vertical_default`) with conditional `vertical` dropdown populated from `verticalsService.list()` filtered to `status="published"`; pre-selects scope=vertical_default + the supplied `defaultVertical` slug when the host editor passes one from the Studio scope; POSTs `chrome_overrides: {}` + `substrate: {}` + `typography: {}` explicit empties so cascade-from-core intent is visible at create time; 422 slug collision sentinel routes to slug-field error per CreateTierOneCoreModal precedent, all other errors surface in `_form` banner with form state preserved for retry; (3) `Tier2TemplatesEditor.tsx` lineage chrome — inherited core's `display_name (Tier 1 v<version>)` surfaced via new `onInheritedCoreChange` callback prop, rendered as a thin mono caption alongside the dirty / last-saved indicators in `FocusEditorPage`'s top bar (test-marked `tier2-lineage-chrome`); subtle, no click behavior per locked decision #4; (4) `InheritedCoreInspectorPanel.tsx` — read-only slide-in side panel mounted absolutely over the inspector area (340px from right edge, surface-sunken bg, accent left border) when operator clicks the inherited-core placement on the canvas (the previously-static `inherits:` caption is now a button — `data-testid="inherited-core-placement"`, disabled until the core fetch resolves); panel surfaces display_name + slug + version in the header, three property sections (Registered component / Chrome read-only / Default geometry) composed against the C-1 `PropertyPanel` substrate via the new data-testid override `inherited-core-properties-panel`, footer with Close + "Edit core in Tier 1" CTA; (5) click-to-edit-core flow — when isDirty=false the CTA navigates immediately to `?tier=1&core=<inherited_core_id>`; when isDirty=true a confirm-before-leave dialog opens with three paths (Save & continue calls `saveDraft` then navigates; Discard & continue calls `discardDraft` then navigates; Cancel dismisses dialog without navigating, side panel stays open, no save/discard fires). **Architectural choices surfaced**: side panel chosen as an absolutely-positioned slide-in OVER the inspector area (not unmounting the underneath PropertyPanel) so Tier 2 unsaved state in `useFocusTemplateDraft` is preserved across panel dismiss + the canvas remains visible alongside core properties — matches Sketch's right-panel "show me the source" rhythm the C-1 inspector substrate borrowed from. Lineage chrome lives in `FocusEditorPage`'s top bar (NOT inside Tier2TemplatesEditor's own area) — Tier 2 surfaces inherited-core info upward via `onInheritedCoreChange` (extending the existing `onDirtyChange` + `onLastSavedChange` parent-callback pattern) and FocusEditorPage tier-gates the rendering so it disappears on tier-1; rationale: lineage is editor-page metadata for the active template, semantically aligned with the dirty / last-saved indicators already in the top bar. Backend Step 2 verification: `TemplateCreateRequest` schema (backend/app/services/focus_template_inheritance/schemas.py:176-189) matches the frontend `TemplateCreatePayload` shape verbatim — all required fields (`scope`, `template_slug`, `display_name`, `inherits_from_core_id`) plus optional `vertical` + `description` + `chrome_overrides` + `substrate` + `typography` defaulting to factory-empty; POST route at `/api/platform/admin/focus-template-inheritance/templates` was already shipped from C-2.2a; `focusCoresService.list()` returns active cores with all fields the modal + side panel use (`id`, `core_slug`, `display_name`, `version`, `is_active`, `chrome`, `registered_component_kind`, `registered_component_name`, default geometry); no backend changes required, no migration, no resolver-internals touched. PropertyPanel passthrough (15 LOC code + 50 LOC tests): optional `className?` + `'data-testid'?` props with `??` fallback to canonical `"property-panel"` id; 3 new vitest cases assert classname pass-through, custom testid presence (and absence of default), default fallback when omitted; existing 3 tests stay green. **Click-to-edit-core dirty-state confirm dialog has all 3 paths under coverage**: Save & continue calls saveDraft + navigates (assertion both onNavigate and saveDraft fired, discardDraft did NOT); Discard & continue calls discardDraft + navigates (assertion onNavigate + discardDraft fired, saveDraft did NOT); Cancel keeps dialog dismissed + no side effects (assertion no save/discard/navigate fired, side panel stays mounted via onClose NOT called). Tier2TemplatesEditor.test.tsx required wrapping all 11 existing `render()` calls in `<MemoryRouter>` because the editor now uses `useNavigate` + `useSearchParams` for the click-to-edit-core flow; the wrap is mechanical (no test semantics change). Tier2TemplatesEditor.test.tsx also gains a `verticalsService` mock (returning a single FH vertical) since the create modal mounted inside the editor calls `verticalsService.list()` on open. **Tests**: PropertyPanel +3 / 6 total → 6/6; CreateTierTwoTemplateModal +10 new (`open=false` no render, full chrome mount, Cancel, empty-fields validation, malformed slug, successful platform_default submit with payload shape assertion, slug collision via "already" error, _form banner for non-slug failures, defaultVertical pre-selects vertical_default scope + vertical dropdown, vertical_default scope without vertical errors, vertical_default + vertical slug submits) → 10/10; InheritedCoreInspectorPanel +10 new (display name/slug/version render, chrome read-only render, registered component render, null-core loading state, Close button, Edit-core immediate navigation when clean, Edit-core raises dialog when dirty, Save & continue saves+navigates, Discard & continue discards+navigates, Cancel keeps panel open + no side effects, empty-chrome notice) → 10/10; Tier2TemplatesEditor +5 new integration (New template button opens modal, inherited-core placement opens side panel, side panel close preserves inspector mount, create-flow closes modal + selects new template, onInheritedCoreChange callback surfaces lineage). Full vitest **195 files / 2687 tests + 1 todo passing** (+30 vs C-2.2b's 2657, all new tests green); tsc clean; eslint clean; vite build 5.12s; **bundle 6,032,150 bytes (+15,461 / +0.26% vs C-2.2b's 6,016,689 — well within ±2% gate)**. LOC: production ~520 (CreateTierTwoTemplateModal 360 + InheritedCoreInspectorPanel 270 + Tier2TemplatesEditor wire-up ~85 net delta + FocusEditorPage lineage chrome ~25 + PropertyPanel +15) + tests ~810 (CreateTierTwoTemplateModal.test 310 + InheritedCoreInspectorPanel.test 230 + Tier2 +220 + PropertyPanel +50) ≈ **~1,330 total, squarely in the ~1,200-1,800 midpoint**, comfortably under worst-case 2,400. **No backend changes, no migrations, no canon doc edits, no resolver internals touched, no Tier 1 surfaces touched, no C-2.2a/b shipped behavior altered.** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: create modal vertical defaults from Studio scope when supplied (#1); side panel inspects without jumping (#2); click-to-edit-core completes side panel UX with 3-path dirty confirm dialog (#3); lineage chrome is simple text only, no interactivity in C-2.2c (#4); no per-row inheritance indicators or tier breadcrumb (#5, #6 — C-2.3); PropertyPanel className/data-testid passthrough minimal (#7); CreateTierTwoTemplateModal mirrors CreateTierOneCoreModal (#8); side panel slide-in over inspector (#9, surfaced + rationale documented); confirm-before-leave inline minimal — no existing shadcn dialog primitive used (#10); integration test for click-to-edit-core mandatory (#11); no backend changes (#12 → Step 2 verified clean). **C-2.2 sub-arc operationally complete**. **Next**: C-2.3 (per-row inheritance indicators + reset-to-inherited affordance + polished tier breadcrumb + interactive lineage chrome + any deferred polish).

- **2026-05-15 — Sub-arc C-2.2b shipped (uncommitted)** — Second of three sub-sub-arcs composing C-2.2 (Tier 2 templates editor). Ships the **editable three-section inspector** on top of C-2.2a's read-only canvas seam. After C-2.2b, operators can author Tier 2 templates' `chrome_overrides` + `substrate` + `typography` end-to-end with 300ms debounced auto-save and the Focus frame canvas updating live as values are scrubbed. New: `frontend/src/bridgeable-admin/hooks/useFocusTemplateDraft.ts` (~370 LOC + ~580 LOC tests; mirrors `useFocusCoreDraft` for the three-blob Tier 2 shape — exposes `chromeOverridesDraft` / `substrateDraft` / `typographyDraft` + three updaters + single `save` / `discard` / `isDirty` / `editSessionId`; uses **three refs** (`chromeOverridesRef` / `substrateRef` / `typographyRef`) that `save` reads from instead of closure-captured state — C-2.1.4 stale-closure discipline applied symmetrically to all three blobs; save's useCallback deps strictly `[templateId, editSessionId]`; 410-retry path swaps to `active_template_id` from the error body and retries once under the same session token), `frontend/src/bridgeable-admin/components/visual-authoring/SubstratePresetPicker.tsx` (5 preset pills: morning-warm / morning-cool / evening-lounge / neutral / custom; mirrors ChromePresetPicker visual treatment), `TypographyPresetPicker.tsx` (4 preset pills: card-text / frosted-text / headline / custom), both with 4 unit tests each. Modified: `Tier2TemplatesEditor.tsx` rewritten to consume `useFocusTemplateDraft` — the inspector right rail now mounts a `PropertyPanel` with three `PropertySection`s (Chrome / Substrate / Typography, all default-expanded, all with optional `lineageHint` captions). Each section composes the canonical C-1 primitives: preset picker + 1-3 scrubbable sliders + 2-5 token swatch pickers. Canvas reads chrome/substrate/typography views from the DRAFT state (not from `template.<blob>`) — same hook membership means live cascade is automatic, no extra wiring. Surface `isDirty` + `lastSavedAt` upward via the existing `onDirtyChange` + `onLastSavedChange` callbacks (replaces C-2.2a's static-false stub). `PropertyPanel.tsx` minimally extended with optional `lineageHint?: string` prop on `PropertySection` (renders as thin mono caption below the section title; test-marked via `property-section-lineage`). `index.ts` barrel exports both new pickers + their slug types. `FocusEditorPage.test.tsx` updated for the new editable inspector contract (3 tier-2 tests aligned: `tier2-inspector-placeholder` → `tier2-inspector-empty` for the no-selection path, `tier2-inspector` testid for the loaded path). **Stale-closure regression test verified against pre-fix code** (per C-2.1.4 discipline): temporarily injected the bug two ways — first added `substrateDraft` to `save`'s deps (test still passed because the refs short-circuit the closure issue); then ALSO swapped the ref reads in `save` for closure reads — 4 tests immediately failed with stale-value assertion mismatches (the substrate / typography / cross-blob C-2.1.4 tests + the explicit save-payload test), confirming the tests genuinely cover the stale-closure surface. Restored fix → 20/20 hook tests pass. **Inspector → canvas integration test verified against pre-fix wiring**: temporarily rewired the editor's `chromeView` / `substrateView` / `typographyView` memos to read from `template?.<blob>` instead of draft variables — 3 live-cascade tests immediately failed (substrate-pill-click didn't update canvas background, typography-pill didn't update heading weight, chrome-pill didn't update preset caption), confirming the wiring contract is genuinely under test. Restored draft-reading wiring → 11/11 editor tests pass. Tests: 8 new picker (4 substrate + 4 typography) + 20 new hook + 4 new editor integration (3 live-cascade + 1 lineage-hint) + 3 updated tier-2 wiring assertions in FocusEditorPage = **35 new + 5 updated tests**. Full vitest 193 files / 2657 tests + 1 todo passing; tsc clean; vite build 5.37s; bundle 6,016,689 bytes (+18,749 / +0.31% vs C-2.2a.1 baseline 5,997,940 — well within ±2% gate); eslint clean. LOC: production ~890 (hook 370 + SubstratePresetPicker 80 + TypographyPresetPicker 80 + Tier2TemplatesEditor refactor +315 net delta + PropertyPanel +18 + index.ts +18 = ~890) + tests ~1,180 (useFocusTemplateDraft 580 + Substrate test 50 + Typography test 50 + Tier2 test additions ~390 + FocusEditorPage updates ~10 + ~100 misc) = ~2,070 total, squarely in the ~1,500-2,200 midpoint, comfortably under worst-case 2,800. **No backend changes, no migrations, no canon doc edits, no resolver internals touched, no Tier 1 surfaces touched.** Migration head unchanged: `r103_focus_templates_edit_session`. **Locked decisions honored**: section order Chrome → Substrate → Typography (#1); default expanded with component-local useState (#2); no placement-selection behavior — chrome inspector targets template chrome_overrides (#3); substrate + typography are Focus-scope (#4); live cascade via shared hook state (#5); minimal lineage hint text only — no polished UI (#6); draftRef discipline on all three blobs (#7, CRITICAL); 410-retry mirrors C-2.1.1 (#8); deepEqualBlob handles sparse responses (#9); session-aware semantics from C-2.1.2 (#10); inspector → canvas integration test ships (#11); test-fails-against-pre-fix verified (#12). **Next**: C-2.2c (`CreateTierTwoTemplateModal` for the create-from-Core flow + `InheritedCoreInspectorPanel` + polished lineage chrome).

- **2026-05-15 — Sub-arc C-2.2a.1 shipped (uncommitted)** — Closes the C-2.2a integration gap: substrate-resolver + typography-resolver were correctly imported and called in `Tier2TemplatesEditor.tsx` (resolved-style memos wiring substrate → CENTER section's inline `style` prop, heading/body typography → preview-card text nodes), but no integration test asserted "editor mounts → substrate appears in DOM" — same class of gap as C-2.1.4. Production diagnosis missed the wiring because (a) it grepped for `data-testid="tier2-canvas"` which didn't exist (actual was `tier2-preview`) and (b) it searched for class-based gradient utilities, not inline `background: linear-gradient(...)` styles. Fix: (1) renamed canvas wrapper testid from `tier2-preview` → `tier2-canvas` (canonical C-2.2a.1 target; `data-tier2-preview="true"` attribute retained for transition-period continuity, no callers depend on it), (2) extended the canvas inline `style` to compose substrate output PLUS four typography CSS custom properties (`--tier2-heading-weight`, `--tier2-body-weight`, `--tier2-heading-color`, `--tier2-body-color`) so canvas-wide typography cascades to inner content without each surface re-resolving the view (locked decision #2), (3) added 2 integration tests in `Tier2TemplatesEditor.test.tsx` asserting actual rendered DOM — first test mocks a `morning-warm`/`intensity:70`/`frosted-text` template and asserts the `tier2-canvas` element's inline `background` matches `/gradient/i`; second test asserts heading `fontWeight === "600"` and body `fontWeight === "500"` on the rendered preview-card text nodes. **Test-fails-against-pre-fix verification**: ran the new tests against pre-fix code → 2 failed (`getByTestId("tier2-canvas")` throws because testid didn't exist yet) → applied the wiring fix → 7/7 pass. **`ResolvedFocus` shape**: editor consumes `TemplateRecord` directly via `focusTemplatesService.get(id)` (no separate `resolve` endpoint call — chrome cascade happens client-side via `mergeChromeWithOverrides(coreChrome, template.chrome_overrides)`). Shape fields confirmed: `chrome_overrides`, `substrate`, `typography` (all `Record<string, unknown>`, default `{}`); `inherits_from_core_id` + `inherits_from_core_version` for the inherited-core fetch. **Chrome cascade**: working correctly — `mergeChromeWithOverrides(core.chrome, template.chrome_overrides)` composes; result feeds `expandPreset` → `resolveChromeStyle` → applied to `tier2-preview-card`. There is no separate "inherited-core placement" rendered yet (that's a C-2.2c concern with the lineage panel + actual core placement); the current preview card is the surface chrome cascade applies to. **`themeTokens` + `mode`**: already in scope; `themeTokens` state lives at the editor's top level, `mode` is hardcoded to `"light"` for the resolve-theme fetch (dark-mode preview deferred). **Gates**: `npx vitest run Tier2TemplatesEditor.test.tsx` → 7/7 passing (5 pre-existing + 2 new); full vitest 190 files / 2625 tests + 1 todo passing; tsc clean; vite build 5.16s; eslint clean. **LOC**: ~25 production-code (canvasStyle memo +20, testid rename + comment +5) + ~165 test-code (2 new integration tests) = ~190 total, squarely in the ~80-200 target midpoint. **No backend changes, no migrations, no canon doc edits, no resolver internals touched, no new visual primitives.** Migration head unchanged: `r103_focus_templates_edit_session`. **Next**: C-2.2b (three-section inspector + useFocusTemplateDraft hook + SubstratePresetPicker + TypographyPresetPicker primitives).

- **2026-05-15 — Sub-arc C-2.2a shipped (uncommitted)** — First of three sub-sub-arcs decomposing C-2.2 (Tier 2 templates editor in Studio). Ships the READ-ONLY canvas seam: a 3-column Tier 2 editor mirroring Tier1CoresEditor's structure (320px templates browser / preview canvas / 340px inspector). Inspector pane is a named placeholder pointing to C-2.2b's three-section editor; create flow + lineage chrome + edit hooks all deferred to C-2.2b/c per scope boundary. New files: `lib/visual-editor/chrome-resolver.ts` (extracts the PRESETS table + expandPreset + slider-bucket helpers + new `mergeChromeWithOverrides` + `resolveChromeStyle` into a shared pure module that both Tier 1 and Tier 2 editors consume — eliminates the duplicate copy that lived in `Tier1CoresEditor.tsx` and `ChromePrimitivesDemoPage.tsx`), `lib/visual-editor/substrate-resolver.ts` (B-4 SUBSTRATE_PRESETS frontend mirror + expand + CSS gradient composition), `lib/visual-editor/typography-resolver.ts` (B-5 TYPOGRAPHY_PRESETS frontend mirror + expand + heading/body style resolvers), `services/focus-templates-service.ts` (admin API client for `/api/platform/admin/focus-template-inheritance/templates` mirroring focus-cores-service shape, full CRUD shipped — C-2.2b's draft hook + C-2.2c's create modal will wire on top), `components/visual-editor/Tier2TemplatesEditor.tsx` (the 3-column editor; selecting a template fetches the template + its inherited core in parallel, then composes chrome via `mergeChromeWithOverrides(core.chrome, template.chrome_overrides)` + substrate via `expandSubstratePreset` + typography via `expandTypographyPreset`, applied to the preview card). `FocusEditorPage` wired: imports `Tier2TemplatesEditor`, adds `setSelectedTemplate` callback and `?template=<id>` URL param, switching tiers now strips the OTHER tier's selection symmetrically (`tier=2` deletes `core`, `tier=1` deletes `template`), `Tier2Placeholder` function deleted. C-1 demo cleanup: `ChromePrimitivesDemoPage.tsx` floating preset's `border-brass` → `border-accent` (legacy token retired in Aesthetic Arc Session 2). Tests: **47 new** — chrome-resolver (16 across slider scrubs / expandPreset / mergeChromeWithOverrides / resolveChromeStyle), substrate-resolver (11), typography-resolver (10), Tier2TemplatesEditor smoke test (5 across empty state + browser + preview + error surfacing) + 5 updates to `FocusEditorPage.test.tsx` (placeholder → editor: tier-2 testid swapped from `tier2-placeholder` to `tier2-inspector-placeholder`, focus-templates-service mocked). Full vitest 190 files / 2623 tests passing; tsc clean; eslint clean. LOC: ~1,535 across new files (chrome-resolver 204 + substrate-resolver 145 + typography-resolver 119 + 3 test files 309 + Tier2TemplatesEditor 413 + smoke test 230 + service 115), squarely in the ~1,000-1,500 target range. **No backend changes, no migrations, no canon doc edits.** Migration head unchanged: `r103_focus_templates_edit_session`. **Current phase**: C-2.2b (three-section inspector + useFocusTemplateDraft hook + SubstratePresetPicker + TypographyPresetPicker primitives) next.

- **2026-05-15 — Sub-arc C-2.1.3 shipped (uncommitted)** — Closes the misdiagnosed dirty-state-clear bug from C-2.1.2. The real root cause was not JSONB key-order (C-2.1.2's hypothesis) but null-stripping in the backend serializer: `_core_to_response` did `dict(row.chrome or {})` which carries only the keys actually present on the row, while the frontend draft populates the full 7-field chrome canonical shape (some values null) as the user touches sliders. Response had 4 keys, draft had 7 → C-2.1.2's `deepEqualChrome` (key-order-tolerant but key-set-strict) returned `!equal` forever → "Unsaved" indicator stuck post-save. Production DevTools inspection confirmed: PUT response chrome blob carried `{preset, elevation, corner_radius, backdrop_blur}` — missing `background_token`, `border_token`, `padding_token`. C-2.1.2's regression test passed for the wrong reason: the mock returned chrome with the same key set as the draft (key-order swapped only), never reproducing production's sparse-key shape. **Backend fix (Option B — clean contract)**: `_core_to_response` + `_template_to_response` (chrome_overrides + substrate + typography) now normalize via a new `_normalize_blob(stored, canonical_fields)` helper. Canonical field sets imported from `chrome_validation.CHROME_FIELDS` (7), `substrate_validation.SUBSTRATE_FIELDS` (5), `typography_validation.TYPOGRAPHY_FIELDS` (5) — already cleanly exported by B-3.5 / B-4 / B-5 validators, no inlining needed. Every response carries the full canonical key set with explicit `null` for unset fields. Database storage stays as-is (null/missing fields stay however JSONB writes them); only the API response is normalized. NO migration. **Frontend fix (Option A — defensive)**: `deepEqualChrome` in `useFocusCoreDraft.ts` rewritten to treat missing-key and explicit-null/undefined-value as semantically equivalent — `{a:1, b:null}` deep-equals `{a:1}`. Union-of-keys iteration; both null/undefined branches short-circuit to `true`. Defensive against any current or future backend serializer that drops null fields. **C-2.1.2's helper kept** (per scope rule "do not remove or revert"); comment updated to reflect C-2.1.3 semantics (key-order-AND-key-set-tolerant). **C-2.1.2's regression test kept-and-improved**: original "key-order swap" test still passes (correct in spirit); 3 new C-2.1.3 tests added that REPRODUCE the production bug (mock response chrome has FEWER keys than draft) — pre-C-2.1.3 implementation would FAIL on those; post-fix all pass. Plus a "missing key == explicit null" equivalence test and a "normalized full-shape response" test (post-backend-fix path). **Tests: 7 new backend** in `test_focus_template_inheritance_admin_api.py` covering core empty-chrome → 7 explicit nulls, core partial update → 7 keys with 1 valued + 6 null, GET round-trip preserves full shape, template chrome_overrides normalized, template substrate normalized (5 keys), template typography normalized (5 keys), list endpoint normalizes every row. **5 pre-existing backend tests updated** in `test_focus_template_inheritance_chrome.py` (1) + `_substrate.py` (2) + `_typography.py` (2) — exact-equality assertions on response blobs aligned to the new normalized shape; these tests were the contract for the pre-C-2.1.3 sparse shape, now updated with explanatory comments. **3 new frontend tests** in `useFocusCoreDraft.test.tsx` (1) sparse-response-vs-7-field-draft regression that reproduces production bug, (2) missing-key/explicit-null equivalence for dirty check, (3) normalized full-shape response path after backend fix. Backend regression: 228/228 focus_template_inheritance suite (chrome 54 + substrate 47 + typography 49 + admin_api 17 + service 50 + session_aware 9 + session_aware_templates 13 — including 7 new C-2.1.3 admin_api tests + 5 pre-existing aligned). Frontend regression: vitest 2589/2589 + 1 todo passing (20/20 in useFocusCoreDraft.test.tsx); tsc clean; vite build 5.89s. LOC: ~50 backend production-code (route serializer +25 incl. new `_normalize_blob` helper + 3 imports + 2 call sites; existing validators untouched) + ~190 backend tests (7 new + 5 modified) + ~30 frontend production-code (hook deepEqualChrome rewrite) + ~110 frontend tests = ~380 total (within ~250-400 midpoint). No migration. No schema changes. No canon doc edits. **Diagnostic root-cause confirmation**: the build prompt's diagnosis stood up — the production response WAS missing 3 chrome keys, draft DID carry all 7, and C-2.1.2's helper DID return `!equal` on sparse-key response. Hypothesis confirmed end-to-end. **Deferred per scope**: C-2.2 (next dispatch — Tier 2 templates editor); edge-panel templates response normalization (apply same pattern when edge-panel session-aware ships); new session-aware semantics; new substrate vocabularies; audit log substrate; resolver changes. Migration head unchanged: `r103_focus_templates_edit_session`.

- **2026-05-15 — Sub-arc C-2.1.2 shipped (uncommitted)** — Mixed backend + frontend fix closing three issues before C-2.2 dispatches: (1) session-aware update semantics for `focus_templates` (Tier 2 — mirrors C-2.1.1 verbatim), (2) resolver slug-vs-UUID bug — live cascade restored by following active core BY SLUG instead of the template's stored `inherits_from_core_id`, (3) `useFocusCoreDraft` dirty-state-clear bug — "Unsaved" indicator was sticking after successful save because `JSON.stringify` equality is key-order-sensitive and PostgreSQL JSONB does not preserve insertion order on round-trip. **Migration `r103_focus_templates_edit_session`** adds `last_edit_session_id UUID NULL` + `last_edit_session_at TIMESTAMPTZ NULL` to focus_templates; reversible; mirrors r102's columns on focus_cores. **`update_template` becomes session-aware**: in-place mutation within 5-min window matching session_id (or first-touch on never-stamped row); version-bump for no-token / expired / different-token / no-session paths; INACTIVE target → new `StaleTemplateVersionError(inactive_id, slug, scope, vertical, active_id)` → **HTTP 410 Gone** with structured `detail: {message, inactive_template_id, active_template_id, slug, scope, vertical}` so frontend swaps its local id + retries under the SAME session token. `TemplateUpdateRequest.edit_session_id` added; `TemplateResponse.last_edit_session_id` + `last_edit_session_at` added. **Resolver slug-based active core lookup** (locked Option A, silent transparent resolve per α): two new helpers `get_core_slug_by_id` + `get_active_core_by_slug` in focus_cores_service; resolver does `slug_by_id(template.inherits_from_core_id) → active_core_by_slug(slug)` rather than direct PK lookup of stored id. When stored UUID is still active, slug lookup returns same row (regression-safe); when bumped, slug lookup returns NEW active row (closes live-cascade bug). Both lookups indexed (PK + partial-unique `ix_focus_cores_active_slug` from r96) — perceptible-impact zero. Orphan slug (would only happen via manual DB intervention bypassing partial-unique) raises `FocusTemplateNotFound` with descriptive message. **`EDIT_SESSION_WINDOW_SECONDS` lifted to shared `app/services/focus_template_inheritance/constants.py`** — single canonical source; cores + templates + future edge-panel session-aware semantics all import from there. `__init__.py` re-exports preserved public surface unchanged. **Frontend dirty-state-clear fix**: introduced narrow recursive `deepEqualChrome(a, b)` (key-order-independent, handles primitives + plain objects + arrays) in `useFocusCoreDraft.ts`; `isDirty` switched from `JSON.stringify(...)` inequality to `useMemo(() => !deepEqualChrome(draft, savedSnapshot), [draft, savedSnapshot])`. Root cause was hypothesis (b) variant — PostgreSQL JSONB round-trip reorders object keys; backend returned `{ elevation: 75, preset: "card" }` when draft was `{ preset: "card", elevation: 75 }` — JSON.stringify produced different strings → `isDirty = true` forever → "Auto-saved Xs ago" never appeared. **Tests: 13 new backend** in `test_focus_template_inheritance_session_aware_templates.py` across 3 classes (TestTemplateSessionAwareUpdate × 10, TestTemplateApiStaleReturns410 × 2, TestMigrationR103 × 1) + **4 new backend** in `test_focus_template_inheritance_service.py::TestResolverSlugBasedLookup` (regression slug-when-active, new behavior slug-after-bump, orphan raises, in-place propagation regression) + **2 new frontend** in `useFocusCoreDraft.test.tsx` (dirty clears on key-order-mismatch round-trip; dirty re-appears on subsequent update). Backend prior-arc regression: 70/70 across session_aware (29) + service (37 + appended 4) + admin_api (4). Adjacent focus_* tests: 164/164 across chrome + substrate + typography + schema + compositions. Frontend regression: vitest 2586/2586 + 1 todo passing; tsc clean; vite build 5.39s; main bundle 5,998,380 bytes = +440 bytes / +0.007% vs C-2.1.1 baseline 5,997,940 (well within ±2% gate). LOC: ~210 backend production-code (service +154, schema +8, migration +70, route +27, model +15, __init__ +7, constants +25, resolver +35) + ~610 backend tests (new file 489 + appended 121) + ~60 frontend production-code (hook) + ~63 frontend tests = ~1,150 total (within ~700-1,000 midpoint, comfortably under worst-case 1,300). **Resolver performance**: 2 indexed B-tree hits vs 1 PK hit — ~50-200µs added latency in unmeasured-but-typical territory; the partial-unique `ix_focus_cores_active_slug` makes the second lookup as cheap as the PK; no measurement script added (over-engineered for the change). **Deferred per Out-of-Scope**: C-2.2 Tier 2 templates editor (next dispatch); edge-panel session-aware (deferred); ResolvedFocus drift signaling (Option β deferred); audit log infrastructure; explicit "Publish v2" UI; multi-tab conflict detection beyond 410-retry; background cleanup of stale session_id rows; resolver caching. **Recommendation**: C-2.2 can now safely build the Tier 2 templates editor knowing scrub-session auto-save will mutate in place + the resolver will surface in-place mutations across all dependent compositions without staleness. Migration head: `r102_focus_cores_edit_session` → `r103_focus_templates_edit_session`.

- **2026-05-15 — Sub-arc C-2.1.1 shipped (uncommitted)** — Session-aware update semantics for `focus_cores` closes the version-bump-on-every-scrub bug from C-2.1. C-2.1's `useFocusCoreDraft` 300ms debounced auto-save was producing ~30+ version-bumps during a single elevation-slider drag because B-1's `update_core` versioned on every successful write; after the first save the frontend held a now-inactive UUID and subsequent saves returned 409. The fix: updates carrying an `edit_session_id` UUID v4 token that match the row's `last_edit_session_id` within 5 minutes (`EDIT_SESSION_WINDOW_SECONDS=300`) mutate in place — version unchanged, `updated_at` bumps, `last_edit_session_at` advances; row id stays the same so the frontend never goes stale. Updates outside the window OR with a different token version-bump per the B-1 behavior. Updates without any token version-bump (B-1 backward-compat preserved verbatim). **First-touch semantics**: when an update arrives with a token AND the row has never been session-stamped (`last_edit_session_id IS NULL`), the update mutates in place + sets the pointer — without this branch the FIRST update of every editor session would version-bump and the scrub-session would still produce one stray version per session. Migration `r102_focus_cores_edit_session` adds two columns to `focus_cores`: `last_edit_session_id UUID NULL` + `last_edit_session_at TIMESTAMPTZ NULL` (reversible; downgrade drops both, no data backfill needed since NULL is the correct "no active session" state). New `StaleCoreVersionError` exception carries `inactive_id` + `slug` + `active_id`; route layer translates to **HTTP 410 Gone** with structured `detail: {message, inactive_core_id, active_core_id, slug}` per **Spec-Override decision (Sonnet locked) option (i)** — preserves the addressing contract and surfaces staleness explicitly rather than silently redirecting. Frontend hook generates a UUID v4 token via `crypto.randomUUID()` (with Math.random fallback for test envs) when `coreId` transitions null → real OR switches between two real values; preserved across re-renders for the same coreId; cleared when coreId returns to null; intentionally NOT persisted across page reloads (page reload = fresh session). On 410 Gone the hook reads `active_core_id` from the error body, swaps its internal `activeCoreIdRef`, and retries the save once under the SAME session token — the scrub keeps going without visible interruption. `CoreUpdateRequest` gains `edit_session_id: str | None` (≤36 chars, optional). `CoreResponse` gains `last_edit_session_id` + `last_edit_session_at` for client telemetry. Tests: **16 new backend** in `test_focus_template_inheritance_session_aware.py` across 4 classes — `TestSessionAwareUpdate` × 10 (in-place mutate, consecutive same-session, cross-session bump, stale-window bump, no-token bump, unset-field preservation, session_at advance, stale-inactive raises, no-new-row, chrome persistence), `TestSessionAwareCascade` × 2 (in-place mutation flows through resolver without re-versioning templates; many in-place edits during one session keep template row at version 1), `TestApiStaleCoreReturns410` × 2 (410 body shape; session round-trip), `TestMigrationR102` × 1 (columns exist + populate). **6 new frontend** in `useFocusCoreDraft.test.tsx` (token generation, preserved across re-renders, refreshed on coreId switch, cleared on null, included in PUT payload, 410 retry path). Backend regression: 241/241 across focus_template_inheritance (188) + edge_panel_inheritance (24) + focus_compositions (29). Pre-existing B-1 version-bump tests (`test_update_versions`, `test_update_cannot_target_inactive`) preserved verbatim because they don't supply session tokens — version-bump path unchanged. Frontend: 15/15 useFocusCoreDraft tests pass (9 original + 6 new); tsc clean; vite build 5.46s; main bundle 5,997,940 bytes = +870 / +0.014% vs C-2.1 baseline 5,997,070 (well within ±2% gate). LOC: ~290 backend (service +130, schema +12, migration +50, route +30, model +15, __init__ +3, tests ~480 raw but ~280 effective service-test) + ~280 frontend (hook +120, service +30, tests +130) = ~570 production + ~610 tests = ~1,180 total (within ~600-900 midpoint, just under worst-case 1,200). **Deferred items**: (a) extending the same semantics to `focus_templates` (Tier 2) — likely the right call once C-2.2 ships a Tier 2 editor with auto-save, but scope-deferred to a follow-up sub-arc (call it C-2.1.2 or fold into C-2.2's slider-scrub work); (b) same for `edge_panel_templates` (B-1.5 territory); (c) explicit "Publish v2" UI affordance for deliberate version creation; (d) audit log capture of session events (no audit substrate for focus_cores exists yet); (e) session-token persistence across page reloads (intentional fresh-session contract); (f) multi-tab editing conflict detection; (g) telemetry on session-reuse rate. **Recommendation**: extend to `focus_templates` BEFORE C-2.2's Tier 2 editor ships — same auto-save pattern will produce the same bloat. Migration head: `r101_typography_substrate` → `r102_focus_cores_edit_session`.

- **2026-05-15 — Sub-arc D-1 shipped (uncommitted)** — Deployment substrate, sub-arc 1. Canonical platform-seed orchestration. Closes the deploy-pipeline gap that left B-1's `scheduling-kanban-core` (seeded by `seed_focus_template_inheritance.py`) unapplied on staging — no upstream invocation existed in `railway-start.sh`. New `backend/scripts/run_canonical_seeds.sh` discovers all `seed_*.py` in `backend/scripts/` via `find -print0 | sort -z` (space-safe under repo paths containing "My Drive"/"Claude Code"), iterates alphabetically, runs each via `python -m scripts.<name>` cd'd into `BACKEND_DIR` (overridable for tests). Skips the four already-invoked-upstream-with-fail-loud seeds (`seed_staging`, `seed_fh_demo`, `seed_dispatch_demo`, `seed_edge_panel_inheritance`) via `SKIP_SEEDS` array to avoid double-execution + preserve R-1.6.3 fail-loud discipline for those four. Auto-detects `python` vs `python3` binary (Railway has bare `python`; macOS dev only `python3`). Non-fatal contract per locked decision 2: seed failure logs `WARN: <name> failed with exit code N — continuing`, loop continues, runner always exits 0. `railway-start.sh` modified additively: new "Running canonical platform seeds (D-1)..." block inserted between the existing fail-loud staging-seed block and `exec uvicorn`. **Spec-Override surfaced**: locked decision 1 says "All `seed_*.py` are canonical" but discovery returned 36 files mixing inheritance seeds, demo seeds (require `--apply`), phase-migration seeds, and dev-only seeds. Resolved by combining locked decision 1 (glob everything) with SKIP_SEEDS for upstream-invoked entries, relying on per-seed self-protection (argparse `--apply` defaults, ENVIRONMENT=production guards) for safety. Local two-run idempotency proof: both runs report 32 attempted / 28 succeeded / 4 failed / 4 skipped, identical totals — 4 expected failures are demo seeds (`seed_email_quote_approval_demo`, `seed_intelligence_dev_executions`, `seed_quotes`, `seed_saved_orders`) refusing to run without `--apply`; they self-protect correctly. Failure-mode proof: synthetic `seed_beta_broken.py exit(7)` between two ok seeds → broken seed warns, next seed runs, runner exits 0. Tests: new `backend/tests/test_canonical_seeds_runner.sh` (3 tests: alphabetical-discovery + non-fatal-failure + summary; empty-dir no-op; SKIP_SEEDS bypass) + new `backend/tests/test_railway_start_structure.py` (7 pytest assertions: alembic precedes runner, runner precedes uvicorn, fail-loud seeds precede runner, runner invocation not set-e-gated, runner ends `exit 0`, runner does not `set -e`, SKIP_SEEDS contains the four upstream entries). All 7 structure tests + 3 shell tests green. Backend regression: 81/81 across structure (7) + focus_template_inheritance_service (50) + edge_panel_inheritance_service (24). Frontend tsc clean (zero changes to frontend). LOC: ~120 runner + ~135 shell test + ~115 structure test + ~25 railway-start.sh diff = ~395 total, within ~150-300 midpoint stretching toward worst-case 500. **No migrations, no business logic, no frontend.** Sub-arc C-2.2 (Tier 2 templates editor) next per arc plan.

- **2026-05-15 — Sub-arc C-2.1 shipped (uncommitted)** — Tier 1 cores editor + create-from-Core (Tier 1 variant) flow + single-section chrome inspector + tier toggle wiring + shared BASE_TOKENS substrate. Foundational pattern-establisher consumed by C-2.2 (Tier 2 templates editor) and sub-arc D (Tier 3 in-place editor). New: `frontend/src/lib/visual-editor/themes/base-tokens.ts` (canonical platform-default substrate mirrored from tokens.css; 30-token catalog across surface/content/border/accent/status/shadow families, light+dark), `resolve-effective-tokens.ts` (composes BASE_TOKENS + platform_themes overrides per Q7 lean option b), `focus-cores-service.ts` (admin API client for `/api/platform/admin/focus-template-inheritance/cores`), `useFocusCoreDraft` hook (load/dirty/300ms-debounced-auto-save lifecycle), `CreateTierOneCoreModal` (deliberate-authoring flow with registered-component picker, slug validation, geometry fieldset), `Tier1CoresEditor` (three-column: cores browser, warm-gradient preview, chrome inspector composing C-1 primitives). `FocusEditorPage` rewritten as top-level dispatcher with tier toggle (`?tier=1|2` URL deep-link) + Tier 2 named placeholder ("Tier 2 editor coming in C-2.2") + dirty pulse + auto-save readout. `ChromePrimitivesDemoPage.FALLBACK_TOKENS` retired (now `{ ...BASE_TOKENS.light }`). New vitest drift-gate parses tokens.css and asserts BASE_TOKENS tracks the source — drift = build break. Auto-register registry filtered to `componentKind === "focus-template"` in modal's picker. Frontend tests: 41 theme tests + 9 hook tests + 8 modal tests + 14 page tests (rewritten — legacy `?focus_type` / `?return_to` assertions against `focus_compositions` retired; new tests cover tier toggle, browser, placeholder, dirty indicator, URL sync). Full vitest 2578/2579 passing (1 pre-existing todo); tsc clean; vite build 5.27s; main bundle 5,997,070 bytes = −11,640 / −0.19% vs C-1 baseline 6,008,710 (well within ±2% gate). Sub-arc C-2.2 (Tier 2 templates editor + three-section inspector + canvas adaptation + inheritance surfacing for chrome) next.

## Production

- Live tenant: Sunnycrest Precast at `sunnycrest.getbridgeable.com` (first tenant: James Atkinson)
- Go-live date: April 7, 2026
- Migration head: `r103_focus_templates_edit_session`

## Build status

| Metric | Count |
|--------|-------|
| Database tables | ~260 |
| ORM model files | 165+ |
| ORM model exports (`__init__.py`) | 178+ |
| API route files | 105+ |
| API endpoints | 955+ |
| Route modules registered in v1.py | 95+ |
| Frontend routes | 150+ |
| Backend service files | 117+ |
| Migration files | 135 |
| Documents test suite (D-1 → D-9) | 224 passing |
| Agent jobs (scheduled) | 14 |
| Accounting agents (registered) | 12/12 (complete) |
| Accounting agent tests | 105 passing |
| Urn Sales E2E tests | 39/39 passing |
| Safety Program Generation E2E tests | 12/12 passing |
| Agent jobs (not yet built) | ~30 |
| TypeScript errors | 0 |
| Backend import errors | 0 |
| Migration chain | Single head, no broken links |

## Active arc

- Arc name: Focus Template Inheritance (sub-arcs A → B → C → D → E → F)
- Current phase: **Sub-arc F-4 closed (this commit, 2026-05-19) — Theme picker shipped in right-rail bottom section. New `FocusBuilderThemePicker` component renders parallel substrate + typography preset chip strips when subject is a template. Clicks fire `templateHook.updateSubstrate({ preset })` / `updateTypography({ preset })` (preset-only partial, preserving intensity / base_token / accent tokens / weights / colors). Existing debounced save path persists; existing F-2 canvas substrate/typography wiring re-renders live. **Disabled state** with mode-specific hint when subject is a core ("Themes apply to templates, not cores") or empty ("Load a template to edit theme") — matches F-3 widget palette disabled-on-core pattern. **F-2 inspector substrate/typography sections coexist unchanged** — both paths write through the same hook methods, single source of truth, single backend persistence path, single canvas re-render. C-1 SubstratePresetPicker + TypographyPresetPicker primitives reused directly (already chip-strip shape with toggle-to-clear semantics); F-4 wraps them in a labeled section. ~140 LOC production + ~200 LOC tests. 7 unit tests + 3 integration tests. **Cross-side data↔render integration test per DECISIONS.md 2026-05-19 (evening) canon**: substrate chip click + typography chip click each assert BOTH save side (PUT body carries `substrate.preset` / `typography.preset`) AND render side (canvas inline style attribute changes). Verify-against-pre-fix discipline applied for BOTH variants — Variant A (chip onClick noop) failed save-side AND render-side; Variant B (canvas-resolver substrate-spread removed) passed save-side AND failed render-side, proving the test catches render-pipeline regressions independent of save-pipeline regressions. Two pre-existing tests adjusted for testid collision (theme picker reuses inspector's substrate-pill testids by primitive reuse): F-3.1a.2 substrate-pill click scoped to inspector via `within(...)`; core-placement-click test's "Substrate"-not-visible assertion scoped to inspector. tsc clean, vitest 86/86 focus-builder passing (was 75; +11 net new), full vitest 2869/2869 substantive passing (one flaky teardown-timing failure in unrelated `r21-outer-tabs.test.tsx` confirmed pre-existing — passes in isolation), vite build clean 5.56s. Sub-arc F-3.1a.2 closed (2026-05-19) — URL recovery on 410-retry. Hook exposes `onActiveTemplateIdChange?: (newId: string) => void` callback option fired on successful 410-retry when the resolved id differs from the consumer's bound `templateId`. `FocusBuilderPage` wires the callback to rewrite URL via `setSearchParams({ subject: template:${newId} }, { replace: true })`. Widgets AND configurable widget props now persist across refresh per F-3.1a (adapter) + F-3.1a.2 (URL recovery) combination. Subsequent saves within session mutate-in-place via the backend's session-aware fast path; URL stays canonical. ~100 LOC total (~25 production + ~75 tests). 5 new tests (4 hook unit + 1 page integration); verify-against-pre-fix discipline applied successfully. F-3.1a.2 closes the F-3.1 fix split — both halves shipped. Sub-arc F-3.1a closed (2026-05-19) — frontend placement-shape adapter (Path A) resolving the frontend↔backend wire-shape mismatch that caused F-3-placed widgets to persist as backend-validation no-ops. F-series sub-arc decomposition: F-1 → F-1.1 → F-2 → F-3 → F-3.1 (split) → F-3.1a → F-3.1a.2 → F-3.1b → F-3.1c → F-4 (this commit, theme picker) → F-5 (breadcrumb + dirty-state polish + coexistence). Sub-arc F-1.1 closed (this commit, 2026-05-18) — Studio rail Focus Builder navigation entry (Q-41) + seed corrections (Kanban display_name + scheduling-fh manufacturing-vertical migration + CORE_SLUG_TO_FOCUS_TYPE decision mapping). Closes two findings from F-1 visual verification. Focus Builder is now reachable from the Studio rail with a dismissible "New" badge; tree hierarchy renders corrected after deploy (Manufacturing → Decision Focuses → Kanban → Funeral Scheduling). F-series sub-arc decomposition continues at `docs/investigations/2026-05-18-focus-builder.md`: F-1 (shipped) → **F-1.1 (this commit, shipped)** → F-2 (selection-driven inspector) → F-3 (widget palette + drag-to-canvas) → F-4 (theme picker) → F-5 (breadcrumb + dirty-state polish + coexistence). 40/42 architectural questions LOCKED; 2 TBD with James (Q-11 canonical/variant caption + Q-33 dark mode preview model). Focus Builder mounts at new route `/studio/builder/focuses`; FocusEditorPage at `/studio/focuses` stays in coexistence period with deprecation banner. Prior sub-arcs A + B-1 + B-1.5 + B-2 + B-3 + B-3.5 + B-4 + B-5 + C-1 + C-1.1 + C-2.1 + C-2.1.1 + C-2.1.2 + C-2.1.3 + C-2.1.4 + C-2.2a + C-2.2a.1 + C-2.2b + C-2.2c + C-2.3 + D-1 + E-1 + E-1.1 all closed. C-2 sub-arc canonically complete after C-2.3 (per-row inheritance indicators + reset-to-inherited ↺ + tier indicator pill + interactive lineage chrome + useSearchParams consolidation cleanup). Studio Focus authoring is end-to-end operational across Tier 1 (cores) and Tier 2 (templates) with full inheritance surfacing.** Next major sub-arc: deferred work — Tier 3 composition editor + widget palette + theme editor refinements + animation polish + inherited-core trigger surface relocation if signal warrants (possible C-2.3.1). Sub-arc C-2.1.4 (this commit, 2026-05-15) — dirty-state-clear definitive fix via draftRef. Pinned by direct fiber-state inspection in production: `useFocusCoreDraft.save` had `draft` in its `useCallback` deps, so each rapid updateDraft re-created `save` capturing the draft value at that re-render moment; the setTimeout queued in updateDraft captured a save whose closure was already one event behind by the time the timer fired. Result: every PUT persisted a value one scrub-step behind the actual draft; savedSnapshot ⊂ draft permanently → isDirty stuck true. Three prior diagnoses (C-2.1.2 JSONB key-reorder, C-2.1.3 null-stripping) were partial truths (the deep equality + null tolerance work IS still correct + load-bearing) but didn't fix the persistent symptom in production. C-2.1.4 adds `draftRef` synced via useEffect; `save` reads `draftRef.current` for the PUT payload; `draft` removed from save's useCallback deps. Canonical React stale-closure-in-callback-deps fix. Plus regression test that fires three rapid updateDraft({elevation: 70, 72, 75}) and asserts the single resulting save payload contains elevation=75 (not stale 72) + isDirty resolves to false. Test verified to FAIL against pre-fix code (`expected 72 to be 75`) and PASS against the fix. ~50 LOC production + 65 LOC test = 115 LOC total. Substrate phase fully complete (chrome + page-background substrate + typography). Tier 1 production editor shipped in C-2.1; session-aware update semantics for cores shipped in C-2.1.1; session-aware update semantics for templates + resolver slug-based live cascade + dirty-state-clear fix shipped in C-2.1.2; deployment substrate (canonical platform seed orchestration) shipped in D-1. **Sub-arc C-2.2 (Tier 2 templates editor + three-section inspector + canvas adaptation + per-row inheritance surfacing) is the next dispatch** — Tier 2 substrate is now pre-emptively session-aware so the editor's scrub auto-save won't reproduce C-2.1.1's version-bump bloat. Sub-arc D-1 (2026-05-15, uncommitted) — canonical platform-seed orchestration on deploy. New `backend/scripts/run_canonical_seeds.sh` discovers all `seed_*.py` alphabetically and runs each via `python -m scripts.<name>` from `railway-start.sh` AFTER the existing fail-loud staging-seed block and BEFORE `exec uvicorn`. Closes the gap where B-1's `scheduling-kanban-core` (in `seed_focus_template_inheritance.py`) wasn't being applied on staging. Non-fatal contract: WARN + continue + exit 0. Skips four upstream-invoked seeds (`seed_staging` / `seed_fh_demo` / `seed_dispatch_demo` / `seed_edge_panel_inheritance`) via `SKIP_SEEDS` array to preserve R-1.6.3 fail-loud discipline. Auto-detects `python` vs `python3`. Space-safe glob via `find -print0 | sort -z`. Spec-Override surfaced: locked decision 1 says "all `seed_*.py` are canonical" but discovery returns demo/phase-migration/dev-only scripts; resolved by combining glob-everything with SKIP_SEEDS + relying on per-seed self-protection (argparse `--apply` defaults, ENVIRONMENT=production guards). Local two-run idempotency: both 32/28/4/4 (attempted/succeeded/failed/skipped); 4 expected failures are demo seeds refusing without `--apply`. Failure-mode proof via synthetic broken seed: warn + continue + exit 0. Tests: new `tests/test_canonical_seeds_runner.sh` (3 cases) + new `tests/test_railway_start_structure.py` (7 cases). 81/81 backend regression across D-1 + focus_template + edge_panel substrates. ~395 LOC. Zero frontend changes, zero migrations, zero business logic. Sub-arc designation: D-1 (Deployment substrate, sub-arc 1) — future deployment-hygiene work falls under the D-arc. Sub-arc B-5 (2026-05-15, uncommitted) — typography substrate. Mirrors B-4's page-background substrate shape: Tier 2 templates carry default typography via new `focus_templates.typography` JSONB column (migration `r101_typography_substrate`); Tier 3 compositions override via existing `deltas.typography_overrides` JSONB key; Tier 1 cores stay typography-free by design (locked architectural decision). Typography v1 shape: `{preset, heading_weight, heading_color_token, body_weight, body_color_token}`. Four canonical presets (card-text 500/400 / frosted-text 600/500 / headline 700/500 / custom) using DESIGN_LANGUAGE §3 content tokens. New `typography_validation.py` module + `resolver.TYPOGRAPHY_PRESETS` + `resolver.expand_typography_preset` + Tier 2 → Tier 3 field-level cascade with typography_sources per-field provenance. Scheduling-fh template seeded with `{"preset": "frosted-text"}` — grounds against the morning-warm substrate already seeded. **Weight scope = 400-900 CSS scale** (multiples of 100 NOT enforced); **color token validation is permissive** (any non-empty string). **Family / line-height / letter-spacing / size deliberately NOT in v1 vocabulary** — those are DESIGN_LANGUAGE §4 platform-canonical concerns. **NO Tier 1 typography** — typography is a Focus-level concern, not a core concern. **NO new endpoints** — typography rides existing B-1 endpoint shapes. **NO frontend rendering of typography** — C-2 is the consumer. **NO new design tokens.** 42 new tests across 6 test classes (TestTypographyValidation × 12, TestTypographyPresetExpansion × 7, TestTemplateServiceTypography × 5, TestCompositionServiceTypography × 3, TestResolverTypographyCascade × 11, TestApiTypography × 4 + module-constant smoke test). One pre-existing B-1 regression test (`test_reset_composition`) updated to expect `typography_overrides: {}` in the deltas dict (same pattern B-3.5 used for chrome_overrides + B-4 used for substrate_overrides). Migration reversible (downgrade drops the column; Tier 3 typography_overrides inside deltas JSONB survives round-trip per the resolver's "ignore unknown delta keys" gracefulness). Backend regression: 169/169 across new typography (42) + chrome (56) + substrate (40) + B-1 service+admin (50, with 1 reset-test update) + A schema (23 — included in 169 figure approximation) + 88/88 across edge-panel inheritance + focus_compositions + edge_panel_r50 + edge_panel_user_override + vertical_inventory. Frontend tsc clean, vitest 2511/2511 unchanged, vite build clean 5.06s. LOC: ~1,217 (typography_validation 121 + resolver additions 119 + schemas 40 + service additions 44 + route serialize 3 + model 11 + migration 90 + seed 6 + tests 773 + reset test update 2) — within worst-case 1,200 budget, exceeded the 700-900 midpoint by ~30% primarily due to test-fixture verbatim mirror from B-4. Sub-arc C-1.1 (2026-05-15, uncommitted) — small post-C-1 fix sub-arc. Adds `surface-frosted` translucent token (light `oklch(0.965 0.014 82 / 0.60)` / dark `oklch(0.20 0.014 65 / 0.55)`) to DESIGN_LANGUAGE.md §3 surface tokens table + Notes bullet + §6 frosted glass card composition pattern, with same-commit mirror to `frontend/src/styles/tokens.css` per the "Tokens.css is a mirror" canon. Token catalog at `frontend/src/lib/visual-editor/themes/token-catalog.ts` extended to include `surface-frosted` so the existing catalog-completeness vitest gate stays green. Frosted chrome preset's `background_token` swapped `surface-elevated` → `surface-frosted` in `backend/app/services/focus_template_inheritance/resolver.py::PRESETS["frosted"]`; mirrored in `ChromePrimitivesDemoPage.tsx`'s frontend PRESETS mirror + FALLBACK_TOKENS (translucent rgba so the fallback path mirrors the resolved-theme path under backdrop-filter). **Root cause** (static-analysis diagnosis confirmed hypothesis a): `surface-elevated` resolves to opaque `oklch(0.965 0.014 82)` / FALLBACK `#ffffff`, alpha = 1; `backdrop-filter: blur()` was being applied to the card (both `backdrop-filter` AND `-webkit-backdrop-filter` set, stacking context fine) but the opaque background masked the blur of the substrate behind. The alpha channel on the new token is the load-bearing piece. Three test assertions updated in `test_focus_template_inheritance_chrome.py` (preset expansion canonical + backdrop_blur override + cascade resolution) — backend chrome suite 56/56 green, full focus_template_inheritance suite 146/146 green. Frontend tsc clean, vitest 2511/2511 (was 2511 — net 0 new), vite build clean 6.47s. **NO new editor work** — C-2 still next major. **NO chrome vocabulary v2 changes.** **NO new presets** — only frosted touched. LOC: ~30 production + ~6 test assertion updates + DL doc additions ~25 lines = ~60 total, well within ~150-300 midpoint budget. Sub-arc B-4 (2026-05-14, uncommitted) ships the page-background substrate vocabulary — Focus-level atmospheric backdrop distinct from chrome (per-surface composition). Tier 2 templates carry default substrate via new `focus_templates.substrate` JSONB column (migration `r100_substrate_vocabulary`); Tier 3 compositions override via existing `deltas.substrate_overrides` JSONB key; Tier 1 cores stay substrate-free by design (locked architectural decision). Substrate v1 shape: `{preset, intensity, base_token, accent_token_1, accent_token_2}`. Five canonical presets (morning-warm, morning-cool, evening-lounge, neutral, custom) — `morning-warm` is the funeral-scheduling mockup substrate (surface-base base + accent-brass-subtle + status-warning-muted, intensity 70). New `substrate_validation` module + extensions to template service writer (create + version-bump-preserves) + composition service writer (extended `_ALLOWED_DELTA_KEYS` + reset clears substrate_overrides) + resolver field-level cascade (`expand_substrate_preset` runs per-tier BEFORE cross-tier merge, mirroring chrome v2 pattern) + `resolved_substrate` + `sources.substrate_sources` per-field provenance. Scheduling-fh template seeded with `{"preset": "morning-warm"}` — re-runs of the seed log skip when the row exists; the canonical preset persists into the substrate column via the create path on fresh installs. **NO Tier 1 substrate** — substrate is Focus-level atmospheric backdrop, not a core concern. **NO new endpoints** — substrate rides existing B-1 endpoint shapes. **NO frontend rendering of substrate** — C-2 is the consumer. 40 new tests across 6 test classes (TestSubstrateValidation × 10, TestSubstratePresetExpansion × 7, TestTemplateServiceSubstrate × 5, TestCompositionServiceSubstrate × 3, TestResolverSubstrateCascade × 11, TestApiSubstrate × 4 + module-constant smoke test). One pre-existing B-1 regression test (`test_reset_composition`) updated to expect `substrate_overrides: {}` in the deltas dict (same pattern B-3.5 used when adding chrome_overrides). Migration reversible (downgrade drops the column; Tier 3 substrate_overrides inside deltas JSONB survives the round-trip per the resolver's "ignore unknown delta keys" gracefulness). LOC: ~800 (substrate_validation ~115, resolver additions ~75, schemas additions ~30, service additions ~30, route serialize ~1, model ~10, migration ~85, seed ~10, tests ~480) — within midpoint budget. Sub-arc C-1 (2026-05-14, uncommitted) ships the visual-authoring primitives + a substrate extension that adds the `frosted` preset (sixth canonical composition: surface-elevated background, elevation 50, corner_radius 62, padding space-6, backdrop_blur 60, border-subtle) + a `backdrop_blur` int 0-100 field to the chrome v2 vocabulary. Four primitives land at `frontend/src/bridgeable-admin/components/visual-authoring/`: **ScrubbableButton** (button-styled scrubber, pointer-capture drag, Shift modifier slows to 0.25× rate, arrow-key a11y, brass border on active scrub), **TokenSwatchPicker** (unified picker across surface/border/padding token families; padding renders a box-illustration swatch via render-strategy prop rather than a separate primitive — minimizes coupling per the locked single-component approach), **ChromePresetPicker** (7-pill row: card / modal / dropdown / toast / floating / frosted / custom; brass-filled active state), **PropertyPanel + PropertySection + PropertyRow** (Sketch-aesthetic inspector substrate with collapsible chevron sections, tight vertical rhythm, IBM Plex Sans labels + IBM Plex Mono values, warm-cream/warm-charcoal substrate). Internal demo route at `/bridgeable-admin/visual-editor/_chrome-primitives-demo` composes all four against a warm-gradient backdrop so the frosted preset's CSS `backdrop-filter: blur()` reads as glass-on-warm-gradient. Demo state component-local via `useState`; theme tokens fetched once on mount from `/api/platform/admin/visual-editor/themes/resolve?mode=light` with hardcoded `tokens.css` light-mode fallback on error. Platform-admin gate inherited from VisualEditorLayout. **NO Tier 1+2 editor work** — C-2 builds the production editor consuming the same primitives. NO frontend changes outside the new files + a 2-line `BridgeableAdminApp.tsx` edit (import + route mount). Sub-arc B-3.5 (2026-05-14) wholesale-replaces B-3's raw-value chrome vocabulary (background_color hex / drop_shadow dict / border dict / padding four-tuple) with a preset-driven vocabulary (canonical compositions from DESIGN_LANGUAGE §6 + two continuous sliders + design-token references). New shape: `{preset, elevation, corner_radius, background_token, border_token, padding_token}` (extended in C-1 with `backdrop_blur`). Migration `r99_chrome_vocabulary_v2` drops + re-adds the two B-3 columns with v2-shaped empty defaults; no consumer was using B-3's vocabulary yet so the data loss is documented + acceptable. Resolver gains `expand_preset()` helper applied per-tier BEFORE cross-tier cascade so a tier's preset contributes its canonical defaults to the field-level merge. Six canonical presets (card/modal/dropdown/toast/floating/frosted) + a `custom` escape hatch. **Sub-arc C-2 (Tier 1+2 production editor in Studio) is next** and consumes the C-1 primitives. Sub-arc B-3 (raw vocabulary) extends the Focus inheritance chain with outer-chrome styling via field-level cascade through all three tiers. Schema-only migration `r98_chrome_substrate` adds `focus_cores.chrome` + `focus_templates.chrome_overrides` JSONB columns (NOT NULL DEFAULT `'{}'::jsonb`); Tier 3 chrome rides the existing `focus_compositions.deltas.chrome_overrides` key. New `chrome_validation` module + extensions to all three service writers + resolver field-level merge + `resolved_chrome` + `sources.chrome_sources` per-field provenance. Chrome value seeded on the canonical scheduling-kanban core.
- Last shipped: **Focus Template Inheritance sub-arc C-2.3** (uncommitted, 2026-05-18) — inheritance-surfacing layer + URL consolidation polish closing C-2 sub-arc. PropertyRow extended with `inheritanceSource` + `onReset` props; resolver-driven dimmed-value + caption indicators on Tier 2 inspector; hover-only ↺ reset affordance with new `resetChromeOverridesField` / `resetSubstrateField` / `resetTypographyField` hook methods (object-identity-preserving no-op when field absent); brass tier indicator pill in FocusEditorPage top bar; interactive lineage chrome (existing text → button opening same panel as canvas placement click); useSearchParams consolidation lifting URL handling from Tier2TemplatesEditor up to FocusEditorPage (mechanical refactor, behavior unchanged). New `focusTemplatesService.resolve(params)` method consuming `GET /api/platform/admin/focus-template-inheritance/resolve` for per-field `sources.*_sources` provenance. Resolver refetch trigger via `[template_slug, vertical, version]` deps so explicit overrides flip "tier1 inherited" → "tier2 explicit" without operator action. Defensive `typeof resolve === "function"` guard for graceful test-mock degradation. **No backend changes, no migrations, no canon doc edits, no resolver internals touched.** Full vitest 195/195 files / 2708 tests passing (+21 vs C-2.2c); tsc clean; eslint clean; vite build 4.74s; bundle +4,641 bytes / +0.077% vs C-2.2c — well within ±2% gate. ~760 LOC total. Migration head unchanged: `r103_focus_templates_edit_session`. **C-2 sub-arc canonically complete.** **Previously shipped: Focus Template Inheritance sub-arc C-2.1.4** (uncommitted, 2026-05-15) — dirty-state-clear definitive fix via draftRef pattern eliminating stale-closure-in-debounced-save race. ~115 LOC (~50 production + ~65 test). Regression test verified to fail against pre-fix code with `expected 72 to be 75` and pass against the fix. Frontend vitest 2590/2590 + 1 todo passing (was 2589; +1 new); tsc clean; vite build 5.48s; main bundle +1,440 / +0.024%. Backend untouched. **Previously shipped: Focus Template Inheritance sub-arc C-2.1.3** (`f7bc67c`, 2026-05-15) — full-shape chrome response contract + defensive dirty-state comparison. Backend `_core_to_response` + `_template_to_response` normalize chrome / chrome_overrides / substrate / typography to canonical full-shape with explicit nulls. Frontend `deepEqualChrome` tolerates missing-key == explicit-null asymmetry. Pre-existing tests aligned to new contract (5 backend tests). 228/228 focus_template_inheritance backend, 20/20 useFocusCoreDraft, 2589/2589 vitest. ~405 LOC. **Previously shipped: Focus Template Inheritance sub-arc C-2.1.2** (`d8f27d7`, 2026-05-15) — Mixed backend + frontend fix closing three pre-C-2.2 issues. Migration `r103_focus_templates_edit_session` adds session-tracking columns to focus_templates (mirrors r102 for cores). `update_template` becomes session-aware; new `StaleTemplateVersionError` → HTTP 410 Gone surfaces `active_template_id` so frontend retries under same session token. Resolver switches from PK-of-stored-id lookup to slug-based active lookup (`get_core_slug_by_id` + `get_active_core_by_slug` helpers) — closes the live-cascade bug where a version-bumped core stranded dependent templates on the inactive row. `EDIT_SESSION_WINDOW_SECONDS` lifted to shared `constants.py` for cores+templates. Frontend `useFocusCoreDraft` dirty check switched from key-order-sensitive `JSON.stringify` equality to recursive `deepEqualChrome` — fixes the "Unsaved" sticking after successful save (root cause: PostgreSQL JSONB does not preserve insertion order on round-trip). 19 new tests (13 backend session-aware-templates + 4 backend resolver-slug-based + 2 frontend dirty-state regression). Prior-arc regression: 70/70 across session_aware + service + admin_api; adjacent 164/164. Frontend vitest 2586/2586 + 1 todo passing; tsc clean; vite build 5.39s; main bundle +440 bytes / +0.007%. ~1,150 LOC. **Previously shipped: Focus Template Inheritance sub-arc D-1** (uncommitted, 2026-05-15) — Deployment substrate, sub-arc 1. Canonical platform-seed orchestration runs on every deploy. New `backend/scripts/run_canonical_seeds.sh` discovers all `seed_*.py` alphabetically + runs each via `python -m scripts.<name>`, invoked from `railway-start.sh` between the existing R-1.6.3 fail-loud staging-seed block and `exec uvicorn`. Closes the gap that left B-1's `scheduling-kanban-core` (in `seed_focus_template_inheritance.py`) unapplied on staging — no upstream invocation existed. Non-fatal contract (locked decision 2): seed failure logs WARN + loop continues + runner exits 0. Skips four already-invoked-with-fail-loud seeds (`seed_staging`/`seed_fh_demo`/`seed_dispatch_demo`/`seed_edge_panel_inheritance`) via SKIP_SEEDS to avoid double-execution. Spec-Override surfaced and resolved: locked decision 1 says "all `seed_*.py` canonical" but discovery returned 36 mixed files (inheritance + demos + phase-migrations + dev-only); resolved by combining glob-everything with SKIP_SEEDS + per-seed self-protection. New tests: `tests/test_canonical_seeds_runner.sh` (3 cases — alphabetical discovery + non-fatal failure + SKIP_SEEDS bypass) + `tests/test_railway_start_structure.py` (7 cases — order invariants, fail-fast prohibition, exit-0 + no `set -e`, SKIP_SEEDS completeness). 81/81 backend regression. Zero frontend changes. ~395 LOC. **Previously shipped: Focus Template Inheritance sub-arc B-5** (uncommitted, 2026-05-15) — typography substrate. Migration `r101_typography_substrate` adds `focus_templates.typography` JSONB NOT NULL DEFAULT `'{}'::jsonb`; Tier 3 typography_overrides ride existing `focus_compositions.deltas` JSONB; Tier 1 cores stay typography-free by design (locked decision). Four canonical presets (card-text / frosted-text / headline / custom) declaring heading_weight + body_weight (CSS 400-900) + heading_color_token + body_color_token (DESIGN_LANGUAGE §3 content tokens). New `typography_validation.py` module + `resolver.TYPOGRAPHY_PRESETS` + `resolver.expand_typography_preset` + Tier 2 → Tier 3 field-level cascade with `typography_sources` per-field provenance. Scheduling-fh template seeded with `{"preset": "frosted-text"}` to ground against the morning-warm substrate (B-4). V1 scope = weight + color only; family / line-height / letter-spacing / size deferred to DESIGN_LANGUAGE §4 platform canon. **NO Tier 1 typography**, **NO new endpoints**, **NO frontend changes**, **NO new design tokens.** Substrate phase of the inheritance arc fully complete (chrome v2 + page-background + typography). 42 new tests across 6 test classes (12 validation + 7 preset-expansion + 5 template-service + 3 composition-service + 11 resolver-cascade + 4 API). Backend regression: 169/169 substrate-suite + 88/88 adjacent. Migration reversible. Frontend tsc clean, vitest 2511/2511 unchanged, vite build clean 5.06s. ~1,217 LOC. Sub-arc C-2 (Tier 1+2 production editor) is next, consuming all three substrate vocabularies through C-1's authoring primitives. **Previously shipped: Focus Template Inheritance sub-arc C-1.1** (uncommitted, 2026-05-15) — frosted-preset visibility fix. Adds `surface-frosted` translucent token (light `oklch(0.965 0.014 82 / 0.60)` / dark `oklch(0.20 0.014 65 / 0.55)`) to DESIGN_LANGUAGE.md §3 (surface table + Notes bullet) + §6 (frosted glass card composition pattern), same-commit mirrored to `frontend/src/styles/tokens.css` + the token-catalog at `frontend/src/lib/visual-editor/themes/token-catalog.ts`. Frosted chrome preset's `background_token` swapped `surface-elevated` → `surface-frosted` (backend resolver + frontend mirror + FALLBACK_TOKENS). Root cause confirmed via static-analysis diagnosis: opaque `surface-elevated` background masked `backdrop-filter: blur()` of the substrate behind, even though the filter was being applied; the translucent token's alpha channel is what makes blur visible. Three test assertions updated; backend chrome 56/56 + full focus_template_inheritance 146/146 green, frontend vitest 2511/2511 + tsc clean + vite build 6.47s. ~60 LOC. **Previously shipped: Focus Template Inheritance sub-arc B-4** (uncommitted, 2026-05-14) — page-background substrate vocabulary. Migration `r100_substrate_vocabulary` adds `focus_templates.substrate` JSONB NOT NULL DEFAULT `'{}'::jsonb`; Tier 3 substrate_overrides ride existing `focus_compositions.deltas` JSONB; Tier 1 cores stay substrate-free by design. Five canonical presets (morning-warm / morning-cool / evening-lounge / neutral / custom) with intensity 0-100 + base_token / accent_token_1 / accent_token_2 references. New `substrate_validation.py` module + `resolver.SUBSTRATE_PRESETS` + `resolver.expand_substrate_preset` + Tier 2 → Tier 3 field-level cascade with substrate_sources per-field provenance. Scheduling-fh template seeded with `{"preset": "morning-warm"}`. Dispatched on dirty C-1 working tree; both sub-arcs coexist for commit-split. 40 new tests (10 validation + 7 preset-expansion + 5 template-service + 3 composition-service + 11 resolver-cascade + 4 API). Backend regression: 257/257 across substrate (40) + chrome (56) + B-1 (50) + A (23) + B-1.5 (44) + B-2 (44 covering test_focus_compositions/test_edge_panel_r50/test_edge_panel_user_override/test_vertical_inventory). Migration reversible. Frontend tsc clean. **Previously shipped: Focus Template Inheritance sub-arc C-1** (uncommitted, 2026-05-14) — visual-authoring primitives + frosted preset + backdrop_blur substrate extension. Backend: `chrome_validation.py` + `resolver.PRESETS` + `schemas.ChromeBlob` extended with `backdrop_blur` int 0-100 + `frosted` preset. Frontend: 4 new primitives under `bridgeable-admin/components/visual-authoring/` (ScrubbableButton, TokenSwatchPicker, ChromePresetPicker, PropertyPanel + section/row sub-primitives) + index barrel + internal demo route at `/bridgeable-admin/visual-editor/_chrome-primitives-demo`. Internal-only dev surface; not a production editor. 35 new tests (8 backend covering frosted preset + backdrop_blur cascade, 25 frontend covering primitives + demo page, +2 backend pre-existing chrome tests updated for the extended `CHROME_FIELDS` tuple). Backend chrome tests 56/56 green; full focus-inheritance substrate 106/106 green. Frontend 2511/2511 vitest green (was 2476; +35 net new). tsc clean; build clean 5.92s; main bundle 6,008,710 bytes = +19,550 / +0.327% vs C-1-baseline 5,989,160 (within ±2% gate). LOC: ~90 backend, ~870 frontend production + ~520 frontend tests = ~1,480 total (within worst-case 1,500). Sub-arc C-2 (Tier 1+2 production editor in Studio) consumes these primitives. **Previously shipped: Focus Template Inheritance sub-arc B-2** (uncommitted, 2026-05-14) — consumer migration + shim removal closed the arc's substrate phase. Deleted `composition_service.py` + `visual_editor_compositions.py` + `seed_edge_panel.py` + `seed_focus_compositions.py`; rewrote `vertical_inventory/service.py` + `edge_panel.py` tenant-realm route + four broken consumer test files (test_focus_compositions, test_edge_panel_r50, test_edge_panel_user_override, test_vertical_inventory); removed import-compat shim (SCOPE_*, KIND_*, CANONICAL_KINDS) from `focus_composition.py`. Substrate + rewritten consumer tests 161/161 green. 1,794 v1 routes register clean. Frontend at `/visual-editor/focus-templates` will break visibly post-deploy — sub-arc C is the canonical replacement. See full detail in Recently shipped #1 below. Previously shipped: Studio 1a-ii — overview inventory implementation (Path A pivot per locked decision 6). New `vertical_inventory` service + `/api/platform/admin/studio/inventory` endpoint backs `/studio` + `/studio/:vertical` with live per-section counts + a UNION'd recent-edits feed sourced from per-table `updated_at` (audit_log write-side instrumentation not wired across the seven editor services — deferred to a dedicated audit-substrate arc). Recent-edits formatting: `Edited {entity_name} in {section} — {relative_time}`; click-through to editor-specific deep link (`/studio[/<vertical>]/<editor>?<param>=<id>`). Editor email resolved through `users.id` join when available, silently omitted when null (no "by —" placeholder). Sections without backend source (Registry inspector; Plugin Registry under vertical scope) render with the count display omitted. Frontend: new `studio-inventory-client.ts` adapter + `StudioOverviewPage.tsx` rewrite preserving A1's section list verbatim. **Studio 1a-i.B follow-up #4 (previous shipped)**: Studio 1a-i.B follow-up #4 (`0623ed3`, 2026-05-13) — verticals-registry disambiguation closes Gates 14 + 22 visible-bug class (the canonical edge case where React Router's `<Route path="live/:vertical/*">` greedily captures a tenant-route segment like `dispatch` as `:vertical`). New `disambiguateStudioLive(pathname, knownVerticals)` helper in `studio-routes.ts` (Option B-variant — additive helper rather than mutating `extractStudioLiveDeepTail`'s signature) classifies the first post-`live` segment against the verticals registry; known slug → vertical scope, unknown → first segment of tenant route tail. New `useVerticals()` hook (`bridgeable-admin/hooks/useVerticals.ts`) — module-level Promise cache, fail-soft (load error → loaded=true with empty list so wrap still mounts). `StudioLiveModeWrap` switched from `useParams<{vertical?}>()` to `useLocation().pathname` + `disambiguateStudioLive(...)`; **blocks render with `data-loading="true"`** until verticals load resolves (optimistic-as-if-tail considered but would briefly mount RuntimeEditorShell with verticalFilter=null + tail="manufacturing" for legitimate URLs, causing picker mis-query — blocking is correct for ~4-row fetch cached at module level). `TenantUserPicker` swapped `extractStudioLiveDeepTail` → `disambiguateStudioLive` to fix the toggleMode edge case where source `/studio/live/manufacturing` would replay to `/studio/live/<new-vertical>/manufacturing?...`. **Previous Studio 1a-i.B follow-up #3** (`908ebe6`, 2026-05-13) — final piece of Studio 1a-i. Closes Part 1 router-topology question deferred from follow-up #2 (`5659e3b`). Implements Option A from `docs/investigations/2026-05-13-studio-live-router-topology.md`: hybrid dispatch model — Live mode uses **nested `<Routes>`** (`<Route path="live/:vertical/*">`, `<Route path="live/*">`, `<Route path="live">` declarations consuming the prefix) so React Router leaves the deep tenant tail (e.g. `dispatch/funeral-schedule`) unconsumed for `TenantRouteTree`'s nested `<Routes>` to match against; Edit mode + overview keep the direct-dispatch via `parseStudioPath` wrapped in a new `EditModeDispatcher` component. **StudioLiveModeWrap** drops the `vertical` prop and reads it from `useParams()` with `isReservedSlug` defense-in-depth guard. **TenantUserPicker** picks up any deep tail from the source URL via new `extractStudioLiveDeepTail(pathname, resolvedVertical?)` helper and replays it post-impersonation as `/studio/live/<resolved-vertical>/<tail>?tenant=...&user=...` via `navigate(target, { replace: true })`. **redirectFromStandalone** gains a deep-path branch BEFORE the legacy `STANDALONE_TO_STUDIO_PATH` lookup: `/runtime-editor/<tail>` (and `/bridgeable-admin/runtime-editor/<tail>`) translate to `/studio/live/<tail>` with NO vertical inserted (per locked decision 1) — picker fills the vertical post-impersonation. Bare `/runtime-editor` + `/runtime-editor/` continue to map via legacy `STANDALONE_TO_STUDIO_PATH` to `/studio/live` unchanged. **Pre-flight verification test** (`__nested-routes-verification.test.tsx`, 96 LOC) gated Steps 2-6 before any production-code changes; kept as regression protection against future react-router-dom upgrades that might change nested-Routes consumption semantics. **Tests**: 2461/2461 vitest green (was 2422; +39 net new across 5 test files — 2 pre-flight, 15 StudioShell, 2 StudioLiveModeWrap, 19 studio-routes, 4 TenantUserPicker, −3 inherited adjustments), tsc clean, build clean 4.86s, **main bundle 5,989,160 bytes = +2,496 / +0.0417% vs refreshed baseline 5,986,664** (well within ±2% gate). 9 files (3 new + 6 modified), **~192 production LOC + ~625 test LOC = ~817 total**. Production LOC within midpoint estimate (140-200); total LOC exceeds worst-case (460) due to test breadth (37 new tests covering pre-flight + nested Routes dispatch + Edit-mode regression + extractStudioLiveDeepTail + pickup-and-replay + redirect deep-path preservation). Playwright execution gap — staging-backed; Gates 14, 15, 22, 29 expected unblocked once this commit deploys. **Studio 1a-i (A1 shell + A2 Live wrap + B editor adaptation + B follow-up rail-defaults + B follow-up #2 auth gate delegation + B follow-up #3 router topology) closes.**

## Active deferred items

- **Data Migration Tool** — waiting on Sage CSV exports from Sunnycrest accountant (invoice history, customer list, cash receipts)
- **Spaces substrate arc** — locked as immediate post-Studio-shell priority per DECISIONS.md 2026-05-13.
- **Audit substrate write-side instrumentation** — `audit_logs` table exists with section-type metadata (`entity_type` column), but no editor service or admin route writes to it. Grep across all seven editor services + six `visual_editor_*` admin routes returned zero `audit_service` / `log_action` / `AuditLog()` references. Recent-edits feed in Studio overview ships using per-table `updated_at` pivot (Studio 1a-ii); audit-substrate arc is needed to wire write-side audit instrumentation across editors + resolve the `users.id` FK constraint that blocks PlatformUser attribution (per CLAUDE.md's known "Audit attribution limitation" entry). Bounded but multi-touch arc; warrants dedicated investigation before dispatch. Not blocking but real observability gap.
- **Gates 14/22 — Studio Live mode deep-tenant-route mount renders overview instead of FuneralSchedulePage** — Under impersonation at `/bridgeable-admin/studio/live/dispatch/funeral-schedule`, the runtime editor shell does not mount the requested tenant page. Observed symptoms (Playwright screenshot, post-`0623ed3` deploy):
  - Top bar displays "Vertical: dispatch" — something is reading the raw `:vertical` URL param without going through `disambiguateStudioLive`
  - Dev banner shows `Page: unmapped:/bridgeable-admin/studio/live/dispatch/funeral-schedule`
  - Pulse/Home overview surface renders instead of `FuneralSchedulePage`

  Follow-up #4's verticals-aware disambiguation shipped cleanly (vitest 2476/2476 pass, including 18 new disambiguation tests) but did not change the rendered output, indicating a consumer outside `StudioLiveModeWrap` and `TenantUserPicker` is reading the URL directly. Likely candidates: `StudioScopeSwitcher` (renders "Vertical: dispatch"), `StudioShell`-level dispatch outside the nested Routes block, or an `EditModeDispatcher` path that matches `:vertical = "dispatch"` ahead of the Live mode Routes.

  Six fixes have been attempted in service of Gates 14 and 22 across the Studio 1a-i sequence (A2 redirect, R-2.x.1 universal-relative-paths, follow-up #2 auth gate, follow-up #3 router topology + picker pickup-and-replay, follow-up #4 verticals disambiguation). Each fix has shipped clean and tested correctly at the unit level, but Gates 14/22 remain red. The diagnostic gap is that chat-driven static analysis has not been sufficient — each fix has been based on a plausible-sounding diagnosis that turned out incomplete. Resolution requires hands-on DevTools debugging on the deployed staging app: open `/bridgeable-admin/studio/live/dispatch/funeral-schedule` while logged in, walk the React component tree, find what is rendering "Vertical: dispatch" and what is mounting the Pulse overview, identify the consumer that bypasses `disambiguateStudioLive`, fix in one shot with evidence.

  Not blocking the Studio platform thesis. Operators can navigate Studio, edit themes, focus authoring, workflows, edge panels. The redirect chain works. The shell mounts correctly. 53 of 56 Playwright gates pass. Gates 14/22 affect a specific click-to-edit path on deep tenant routes — material, but downstream of the Studio shell's substrate work.
- **Arc 4a.2b** — vault cluster Path 1 wrap (9 widgets) + dashboard_layouts seed script; sibling of Arc 4a.2a (committed `8affc8f`); estimated ~1,300-1,800 LOC
- **revenue_summary + ar_summary** — declared in backend WIDGET_DEFINITIONS but no frontend React component; documented in widget-renderer-parity.test.ts `KNOWN_DEFERRED_VAULT`; hygiene arc TBD
- **13 simpler agent jobs** — `STALE_DRAFT_MONITOR`, `REVERSAL_RUNNER`, `PO_DELIVERY_MONITOR`, `RECONCILIATION_MONITOR`, `ABANDONED_RECONCILIATION_MONITOR`, `STATEMENT_RUN_MONITOR`, `FINANCE_CHARGE_REMINDER`, `EXEMPTION_EXPIRY_MONITOR`, `1099_MONITOR`, `DELIVERY_WEEKLY_REVIEW`, `FINANCE_CHARGE_INSIGHT_JOB`, `DISCOUNT_UPTAKE_JOB`, `OUTCOME_CLOSURE_JOB`
- **NPCA Audit Prep feature** — placeholder UI at `/npca`; full feature not yet built (compliance score engine, gap analysis, audit package ZIP)
- **Performance optimization** for report generation
- **FastAPI `@app.on_event` → lifespan migration** — deprecated pattern; mechanical migration deferred to FastAPI version-bump arc
- **R-9 workflow node registry promotion** — ~900 LOC + BLOCKING parity test
- **R-classify customer classification refactor** — ~600 LOC + BLOCKING parity test
- **R-8.x cleanup arcs** — 5 bounded items (EmailChannel exception classes, Triage vertical_default scope, TriageQueueConfig discriminated union, AGENT_REGISTRY side-effect-import, ApprovalFlow enum)
- **R-8.y+1** — per-tenant configuration browser extending R-8.y.d
- **Bounded sub-arcs (trigger-driven)**: Arc-3.x-page-context-substrate, Arc-3.x-hybrid-filter, Arc-3.x-mount-gate, Arc-4a.x-IconPicker-rollout, Arc-4b.x-nesting-ui, Arc-4b.x-mention-hover-preview, Arc-4.x-approval-gate

## Recently shipped (rolling, last 10)

0. **Focus Template Inheritance sub-arc F-4 — theme picker (right-rail bottom section)** (uncommitted, 2026-05-19). Closes F-4 of the focus-builder sub-arc decomposition. New `FocusBuilderThemePicker` component renders parallel substrate + typography preset chip strips when the right-rail subject is a template. Click chip → `templateHook.updateSubstrate({ preset })` / `templateHook.updateTypography({ preset })` (preset-only partial, other fields preserved via hook's `{ ...prev, ...partial }` merge) → existing debounced save → canvas re-renders live via existing F-2 substrate/typography wiring in `FocusBuilderCanvas.canvasStyle`. **Disabled state** with mode-specific hint when subject is a core ("Themes apply to templates, not cores") or empty ("Load a template to edit theme") — matches F-3 widget palette disabled-on-core pattern (NOT hidden). **F-2 inspector substrate/typography sections coexist unchanged** — both authoring paths write through the same hook methods. F-2 is fine-grained (preset + intensity scrubber + token swatches); F-4 is the fast-path preset chip strip. Same source of truth, same backend persistence, same canvas re-render. C-1 `SubstratePresetPicker` + `TypographyPresetPicker` primitives reused directly (audited as already chip-strip shape with toggle-to-clear semantics — clicking the active chip clears to `null`); F-4 wraps them in a labeled section + delegates disabled-state handling. No new hook methods. No new design tokens. No new visual primitives. ~140 LOC production (`FocusBuilderThemePicker.tsx` + 6-line `FocusBuilderRightRail.tsx` swap) + ~200 LOC tests. **7 unit tests** in `FocusBuilderThemePicker.test.tsx` (disabled state for core + empty modes, both strips render, both selected-state indicators, both chip clicks fire correct partial, toggle-to-clear preserved). **3 integration tests** in `FocusBuilderPage.test.tsx` (substrate cross-side + typography cross-side + core-disabled). **Cross-side data↔render integration test per DECISIONS.md 2026-05-19 (evening) canon**: chip click asserts BOTH save side (PUT body carries `substrate.preset` / `typography.preset`) AND render side (canvas inline `style` attribute changes). **Verify-against-pre-fix discipline applied for BOTH variants per (evening) canon**: Variant A (chip onClick noop'd in `FocusBuilderThemePicker`) failed save-side AND render-side — confirmed assertion 1 fires; Variant B (canvas-resolver `...resolveSubstrateStyle(...)` spread removed from `canvasStyle`) passed save-side AND failed render-side — confirmed assertion 2 catches render-pipeline regressions independently of save-pipeline regressions. Both reverts restored verbatim. Two pre-existing tests adjusted for testid collision (theme picker reuses C-1 substrate-pill / typography-pill testids by primitive reuse): F-3.1a.2 substrate-pill click scoped to inspector via `within(...)`; core-placement-click test's "Substrate"-not-visible assertion scoped to inspector. **Bundle delta**: 6,084.06 kB JS — within ±0.5%. **Files**: NEW `frontend/src/bridgeable-admin/components/focus-builder/FocusBuilderThemePicker.tsx`, NEW `FocusBuilderThemePicker.test.tsx`; MODIFIED `FocusBuilderRightRail.tsx` (replaced "Arrives in F-4" placeholder with the picker mount, ~12-line edit) + `FocusBuilderRightRail.test.tsx` + `FocusBuilderPage.test.tsx` (3 new integration tests + 2 pre-existing tests scoped); `STATE.md`. **Gates**: `npx tsc -b` clean. `npx vitest run` 86/86 focus-builder passing (was 75 pre-F-4; +11 net new); full-suite 2869/2869 substantive passing (one flaky `r21-outer-tabs.test.tsx` teardown-timing failure confirmed pre-existing, passes in isolation against main + working tree both). `npx vite build` clean 5.56s. Backend pytest in progress at write time but no backend changes shipped. **F-4 closed cleanly. F-5 (breadcrumb + dirty-state polish + coexistence-banner + tree search) is next.**

1. **Focus Template Inheritance sub-arc F-3.1b — widget inspector Chrome section** (uncommitted, 2026-05-19 evening). Closes Q-28's specification gap for widget chrome editing. The widget inspector now exposes per-placement chrome editing controls below CONFIGURATION + PLACEMENT and above the Remove widget button. Section is collapsible with `defaultExpanded={true}` (operators see chrome on first inspector open; differs from PLACEMENT's collapsed default since chrome editing is a frequent operator action). Seven fields wired identically to F-2's `ChromeInspectorSection` composition: `ChromePresetPicker` (preset), three `ScrubbableButton`s (elevation/corner_radius/backdrop_blur, all bounded 0-100), and three `TokenSwatchPicker`s (background_token consuming surface family; border_token consuming border family; padding_token consuming padding family). All field updates flow through `hook.updateWidget(widget.id, { [field]: value })` → existing F-3.1a placement adapter → backend `prop_overrides`. **No inheritance indicators on widget chrome** — widgets are per-placement; chrome is stamped at placement creation; there is no Tier-1 cascade for widgets (C-2.3 inheritance indicator pattern explicitly does NOT apply per locked decision 3). **Default chrome lookup (Step 1)**: F-3 widget registrations do NOT carry a `defaultChrome` field on `RegistrationMetadata` — audit confirmed via reading `frontend/src/lib/visual-editor/registry/registrations/focus-builder-widgets.ts` (only `configurableProps`, no `defaultChrome`). Fall back to inline canonical constant `DEFAULT_WIDGET_CHROME` matching F-3 seed canon: Frosted preset, elevation 50, corner_radius 70, backdrop_blur 44, surface-frosted, border-subtle, space-3. When registrations later expose `defaultChrome` metadata, swap the inline constant for a registry lookup — documented inline at the constant definition. **F-2 ChromeInspectorSection reuse**: pattern reused with adaptation — F-2 carries inheritance source resolution (`chromeInheritanceFor` reading `sources.chrome_sources`) and onReset callbacks for Tier-2 reset-to-Tier-1 affordance; widgets have neither concept so the row composition simplifies to `inheritanceSource="explicit"` literal with no reset. The `chromeView` derivation is bespoke: simple per-field null coalescing against the inline defaults (vs F-2's cascade through `expandPreset` + `mergeChromeWithOverrides` — widgets have no inherited core chrome to merge against). Pattern body is structurally identical otherwise (PropertyRow + ScrubbableButton + TokenSwatchPicker callbacks). **Verify-against-pre-fix discipline applied (Step 5)**: wrapped the entire Chrome `<PropertySection>` in `{false && (...)}`, ran the integration test → it failed at `await screen.findByLabelText(/elevation/i)` exactly as the build prompt predicted, restored. Discipline confirms the integration-test gate is load-bearing (would catch a future regression that accidentally removed the Chrome section). **Integration test (Step 4)** in `FocusBuilderPage.test.tsx` (test name: "F-3.1b — widget chrome scrub persists through adapter as prop_overrides"): seeds a template with one backend-shaped placement (`component_name: "day-strip-widget"`, `starting_column: 0`, `column_span: 12`, no `prop_overrides`), renders the full page tree (router + canvas + inspector + hook + adapter + mocked service), clicks the placed widget to open the inspector with the Chrome section auto-expanded, scrubs elevation via `ArrowRight` keydown on the labeled scrubber, advances fake timers past the 350ms debounce, asserts the PUT body's `rows[0].placements[0].prop_overrides.elevation` is a number. Exercises the full operator-flow: hook + adapter + service + URL state, with only the network mocked (per the today-filed canon entry "Mock-only tests verify one side of frontend↔backend contracts; cross-side contracts require integration tests"). **Unit tests (Step 3)**: 10 new in `WidgetInspectorSection.test.tsx` under a new "Chrome section (F-3.1b)" describe block — section renders, expanded by default, can collapse via header click, elevation/corner_radius/backdrop_blur each call `onUpdateWidget` with correct partial when scrubbed, preset pill click toggles to null (chromeView's default is "frosted" so clicking the active Frosted pill clears the preset per ChromePresetPicker contract), background/border/padding swatch labels render, defaults render when `placement.chrome` is empty/undefined, mixed null + non-null chrome falls back per-field. **FocusBuilderInspector wiring**: added `themeTokens={themeTokens}` to the `<WidgetInspectorSection>` mount point (1-line change; `WidgetInspectorSection`'s new `themeTokens` prop is optional with empty-record default so unit tests can mount without seeding tokens). **Bundle delta**: 6,082.31 kB — within ±0.5% tolerance. **No backend changes, no migrations, no canon docs touched beyond STATE.md.** Hook untouched (F-3.1a's `updateWidget` + F-3.1a.2's URL recovery via `onActiveTemplateIdChange` already canonical). Adapter untouched (F-3.1a canonical). C-1 primitives untouched (composition only). All gates green: `npx tsc -b` clean, `npx vitest run` 157/157 in focus-builder + hooks scope (10 new Chrome unit + 1 new integration), `npx vite build` clean 5.40s, backend pytest passes except for one pre-existing unrelated failure (`test_admin_portal.py::TestProductLines::test_available_catalog`) verified to fail equivalently on stashed-clean main — out of scope. **LOC**: ~110 in `WidgetInspectorSection.tsx` (default constant 9 LOC + chromeView memoization 32 LOC + Chrome `<PropertySection>` JSX 70 LOC) + ~95 unit tests + ~75 integration test + 1 line in `FocusBuilderInspector.tsx` = ~280, comfortably within 350 worst-case budget. **Pre-flight verification**: confirmed current commit was `be1d46c` (process canon) before starting per today's process-canon entry on `git log -1 HEAD` discipline. **Files modified**: `frontend/src/bridgeable-admin/components/focus-builder/WidgetInspectorSection.tsx`, `frontend/src/bridgeable-admin/components/focus-builder/WidgetInspectorSection.test.tsx`, `frontend/src/bridgeable-admin/components/focus-builder/FocusBuilderInspector.tsx`, `frontend/src/bridgeable-admin/components/focus-builder/FocusBuilderPage.test.tsx`, `STATE.md`. **F-3 sub-arc canonically complete after F-3.1b**: F-3 (palette + drag + accessory editing + useFocusTemplateDraft widget methods + three seed widgets) → F-3.1a (placement adapter) → F-3.1a.2 (URL recovery on 410-retry) → F-3.1b (widget inspector Chrome section). F-4 (theme picker) is the next major sub-arc.

1. **Focus Template Inheritance sub-arc F-1.1 — Studio rail Focus Builder entry + seed corrections (Kanban + manufacturing-vertical)** (this commit, 2026-05-18). Tight follow-up fix arc closing two findings from F-1 visual verification. (1) Q-41 Studio rail navigation entry: added a separate "Focus Builder" rail entry (Implementation B) alongside existing "Focuses", with a dismissible "New" badge gated on `bridgeable.focus-builder.studio-rail-banner-dismissed` localStorage key (per-operator dismissal; direct route still works after dismissal). Wired via new `RailEntry.overrideHref` field that bypasses `studioPath()` for non-editor URLs. (2) Tree organization correction: `CORE_SLUG_TO_FOCUS_TYPE` flipped `scheduling-kanban` + `scheduling-kanban-core` from `production` → `decision` (Kanban shape is canonical decision-family per James lock; multiple decision-family cores will share — Kanban, Triage Queue, etc.). (3) Seed-script corrections: `scheduling-kanban-core` display_name "Scheduling Kanban" → "Kanban" (canonical shape name; use-case framing belongs at template level), `scheduling-fh` template `vertical` "funeral_home" → "manufacturing" (templates live in operator's vertical — James authored at Sunnycrest precast manufacturer to schedule funeral vault deliveries; CONTENT involves FH customer, OPERATOR is Manufacturing), `scheduling-fh` display_name "Funeral Home Scheduling" → "Funeral Scheduling". Slugs unchanged on both entities (immutable + would break references). Seed handles the vertical migration in two steps: deactivate any legacy funeral_home row + create-or-update at manufacturing (since `update_template` treats vertical as immutable). `_seed_core` and template drift detection both extended to include display_name. After deploy + seed run, Focus Builder tree renders: Manufacturing → Decision Focuses → Kanban → Funeral Scheduling; Funeral Home empty/Unclassified-only. Old `/studio/focuses` Tier 1 tab inherits the same display_name updates automatically (DB-backed). **Files**: `frontend/src/lib/visual-editor/focus-types.ts` + test (decision mapping), `frontend/src/bridgeable-admin/components/studio/StudioRail.tsx` + new `StudioRail.test.tsx` (7 tests covering entry rendering, navigation, badge dismissal, localStorage persistence), `frontend/src/bridgeable-admin/components/focus-builder/FocusBuilderTree.test.tsx` (3 assertions updated production → decision), `backend/scripts/seed_focus_template_inheritance.py` (constants + drift detection). **Tests**: 12/12 targeted vitest green; 16/16 focus-builder tests; 264/264 focus_template_inheritance backend tests; full vitest 2750/2752 green (1 pre-existing Tier2 flake unrelated, 1 todo). tsc -b clean; vite build clean (no bundle delta). LOC: ~150 frontend + ~50 backend seed. Implementation B chosen over A: rail rows are tight; banner sub-link would break visual rhythm; separate entry preserves entry-per-row pattern with minimal addition.

1. **Focus Template Inheritance sub-arc C-2.1.4 — dirty-state-clear bug DEFINITIVE fix via draftRef** (this commit, 2026-05-15). Third diagnosis of the same surface symptom; first one pinned by direct fiber-state inspection in production rather than static analysis. Root cause: `useFocusCoreDraft.save` had `draft` in its `useCallback` deps array → save's identity changed every render → the setTimeout queued in `updateDraft` captured a save function whose closure was already one event behind by the time the timer fired (300ms later). Result on a continuous scrub: every PUT persisted a value one scrub-step behind the actual draft. Wire-level evidence: every PUT 200, request matches response exactly. React state evidence (fiber walk): `draft = {elevation: 50}` (user's latest) but `savedSnapshot = {elevation: 49}` (one event behind, what was actually saved). `deepEqualChrome(draft, savedSnapshot)` correctly returned false; `isDirty` correctly stuck true; "Unsaved" indicator correctly never cleared. The bug was the saved value, not the comparison. Fix: canonical React stale-closure-in-callback-deps pattern. New `draftRef` synced via useEffect on every draft change; `save` reads `draftRef.current` for the PUT payload (latest committed draft, not closure); `draft` removed from save's `useCallback` deps → save's identity is now stable across draft changes. The setTimeout in updateDraft now captures a save that always reads the current draft via the ref when fired. Three-update race regression test (`save sends latest draft value not stale closure value`): fires updateDraft({elevation: 70}) → updateDraft({elevation: 72}) → updateDraft({elevation: 75}) without intervening waits, then verifies the single resulting save payload contains `elevation === 75` AND `isDirty === false` after the save resolves. Test verified to FAIL against pre-fix code with `expected 72 to be 75` (concrete confirmation it catches the race) and PASS against the fix. Prior C-2.1.2 + C-2.1.3 work preserved: deepEqualChrome's key-set-tolerance + null-equivalence + backend canonical full-shape response contract are all still correct + load-bearing for defense-in-depth. C-2.1.4 is the additive fix that closes the actual symptom. ~115 LOC (50 production + 65 test). Vitest: 2590/2590 + 1 todo (was 2589; +1 net new); 21/21 in useFocusCoreDraft suite. tsc clean. vite build 5.48s, main bundle 5,998,510 bytes (+1,440 / +0.024%). Backend untouched. **Lesson codified**: static-analysis diagnosis of React state bugs is high-false-positive when the bug involves timing + closure capture; fiber-state inspection in production is the right diagnostic tool for "saves succeed at wire level but UI shows wrong state" symptoms. Sub-arc C-2.2 (Tier 2 templates editor + three-section inspector + canvas adaptation) is the next dispatch — substrate is genuinely complete now, with three independent fixes (one true root cause + two defense-in-depth) producing a robust auto-save loop.
2. **Focus Template Inheritance sub-arc C-2.1.3 — dirty-state-clear bug REAL fix** (`f7bc67c`, 2026-05-15). Closes C-2.1.2's misdiagnosis. Production response from `_core_to_response` was stripping null/unset chrome fields (`dict(row.chrome or {})` carried only the keys the row had — 4 in production), while the frontend draft populates the full 7-field chrome canonical shape as the user touches sliders. C-2.1.2's `deepEqualChrome` was key-order-tolerant but key-set-strict → returned `!equal` forever → "Unsaved" stuck after save. C-2.1.2's regression test passed for the wrong reason (mock response had same key set as draft; never reproduced production's sparse-key shape). **Two complementary fixes**: (a) backend `_core_to_response` + `_template_to_response` (chrome_overrides + substrate + typography) now normalize via new `_normalize_blob(stored, canonical_fields)` helper consuming pre-existing canonical field tuples from `chrome_validation.CHROME_FIELDS` (7) / `substrate_validation.SUBSTRATE_FIELDS` (5) / `typography_validation.TYPOGRAPHY_FIELDS` (5) — no inlining; clean contract; every response carries the full canonical key set with explicit `null` for unset fields. (b) frontend `deepEqualChrome` rewritten to treat missing-key and explicit-null/undefined-value as semantically equivalent via union-of-keys iteration — defensive against any current or future backend serializer that drops null fields. **Database storage unchanged**; only response shape normalized. **No migration**. **No schema changes**. **C-2.1.2's helper kept-and-improved**; C-2.1.2's regression test kept (correct in spirit); 3 new C-2.1.3 frontend tests REPRODUCE the production bug (mock with FEWER keys than draft) — those would FAIL on pre-C-2.1.3 implementation, prove the new fix works. Tests: 7 new backend admin_api (chrome empty → 7 explicit nulls, partial update → 7 keys with 6 null, GET round-trip preserves full shape, template chrome_overrides + substrate + typography normalized, list normalizes every row); 5 pre-existing backend tests updated (chrome/substrate/typography exact-equality assertions aligned to normalized shape with explanatory comments); 3 new frontend tests (sparse-response regression, missing-key/explicit-null equivalence, normalized full-shape happy path). Backend regression: 228/228 focus_template_inheritance suite. Frontend regression: vitest 2589/2589 + 1 todo; tsc clean; vite build 5.89s. LOC: ~50 backend production + ~190 backend tests + ~30 frontend production + ~110 frontend tests = ~380 total (within ~250-400 midpoint). Files: `backend/app/api/routes/admin/focus_template_inheritance.py` (serializer normalization + 3 imports), `backend/tests/test_focus_template_inheritance_admin_api.py` (7 new tests), `backend/tests/test_focus_template_inheritance_chrome.py` (1 assertion updated), `backend/tests/test_focus_template_inheritance_substrate.py` (2 assertions updated), `backend/tests/test_focus_template_inheritance_typography.py` (2 assertions updated), `frontend/src/bridgeable-admin/hooks/useFocusCoreDraft.ts` (deepEqualChrome rewrite + comment refresh), `frontend/src/bridgeable-admin/hooks/useFocusCoreDraft.test.tsx` (3 new tests), STATE.md. No new files. No migration. No commit per arc protocol. Production-bug root cause confirmed: response chrome had 4 keys, draft 7. Sub-arc C-2.2 (Tier 2 templates editor) cleared to ship next.

2. **Focus Template Inheritance sub-arc C-1.1 — frosted-preset visibility fix** (2026-05-15, uncommitted). Small post-C-1 corrective sub-arc fixing the C-1 demo's opaque-card-defeats-backdrop-filter bug. **Diagnostic** (static-analysis since local dev / preview server not reachable from build shell, per the build prompt's documented fallback path): the C-1 demo's preview card applied `background: themeTokens["surface-elevated"]` which resolves to opaque `oklch(0.965 0.014 82)` (light) / `#ffffff` (FALLBACK_TOKENS) — alpha = 1. CSS `backdrop-filter: blur()` was being applied (both `backdrop-filter` AND `-webkit-backdrop-filter` set per `ChromePrimitivesDemoPage.tsx:230-237`; stacking context fine — the card sits in a flex container under a positioned preview pane) but the opaque background masks the blur of the substrate behind it. The filter renders zero visible blur regardless of slider position. Hypothesis (a) from the build prompt confirmed; hypothesis (b) ruled out. **Fix — canonical**: add `surface-frosted` translucent token to DESIGN_LANGUAGE.md §3 + `tokens.css` (same-commit mirror per the "Tokens.css is a mirror" canon). Light mode `oklch(0.965 0.014 82 / 0.60)` (tracks `surface-elevated` L/C/H with alpha 0.60); dark mode `oklch(0.20 0.014 65 / 0.55)` (sits between `surface-base` L=0.16 and `surface-elevated` L=0.28 — cocktail-lounge frosted glass under pendant light rather than fogged window). DL §3 gains the row in the surface tokens table + a load-bearing-alpha Notes bullet; DL §6 gains a "Frosted glass card (level 2)" composition pattern documenting the `background + backdrop-filter + -webkit-backdrop-filter` recipe. DL §9 Full CSS variable list updated for both `:root` + `.dark` blocks. tokens.css mirrors with explanatory comment in each block. Token catalog at `frontend/src/lib/visual-editor/themes/token-catalog.ts` extended with `surface-frosted` entry (light/dark defaults + description); without this the catalog-completeness vitest gate (which parses tokens.css at test time) fails. **Frosted preset swap**: `resolver.py::PRESETS["frosted"].background_token` `surface-elevated` → `surface-frosted`; `ChromePrimitivesDemoPage.tsx`'s frontend PRESETS mirror swapped to match; FALLBACK_TOKENS gains `"surface-frosted": "rgba(255, 251, 240, 0.60)"` (translucent rgba so the fallback path on theme-fetch-failure mirrors the resolved-theme path under backdrop-filter). **Test updates**: three assertions in `test_focus_template_inheritance_chrome.py` flipped — `test_frosted_preset_returns_canonical_defaults` + `test_frosted_preset_with_backdrop_blur_override` (TestPresetExpansion) + `test_frosted_preset_resolves_through_cascade` (TestResolverChrome). No frontend test updates needed — the demo test asserts non-empty `style.background` not specific colors. **Demo page CSS surface review** (Step 4 audit): `cardStyle` at lines 218-239 ALREADY had `WebkitBackdropFilter` set in lockstep with `backdropFilter`, position-relative stacking via flex parent works for backdrop-filter, blur applied only when `resolved.backdrop_blur > 25` (correct — the slider-mapping function returns 0 at <=25). No CSS-side fix needed; the only issue was the opacity of the background token. **Verification gates**: backend chrome suite **56/56 green** (3 assertions flipped, no net new tests); full focus_template_inheritance suite **146/146 green** (no neighbor regression); frontend vitest **2511/2511 green** (was 2511; net 0 new — only catalog-completeness gate exercised the new entry); tsc clean; vite build clean **6.47s**. **Qualitative verification** (static-analysis fallback documented in build report — preview server not reachable from build shell, per the build prompt's `<verification_workflow>` skip clause for unreachable environments + Step 1's "static-analysis fallback path" allowance): post-fix the card's resolved background is alpha-0.60 translucent in light mode; the warm-gradient backdrop is mathematically visible through the card; `backdrop-filter: blur(24px)` at slider=100 now operates on visible pixels rather than masked-opaque pixels. The fix is correct by construction — alpha < 1 is the necessary + sufficient condition for backdrop-filter visibility on an HTML element with non-zero background. Pixel-level visual verification deferred to the user's review-and-commit pass. **LOC**: ~30 production (resolver +6, demo PRESETS +1 + FALLBACK_TOKENS +5, tokens.css +12 with comments, token-catalog +11) + ~6 test assertion updates + DL doc additions ~25 lines = **~60 total**, well within the ~150-300 midpoint budget. Files modified: DESIGN_LANGUAGE.md, frontend/src/styles/tokens.css, backend/app/services/focus_template_inheritance/resolver.py, backend/tests/test_focus_template_inheritance_chrome.py, frontend/src/bridgeable-admin/pages/visual-editor/ChromePrimitivesDemoPage.tsx, frontend/src/lib/visual-editor/themes/token-catalog.ts, STATE.md. No new files. No migration. No commit per arc protocol. **Sub-arc C-2 (Tier 1+2 production editor in Studio) is still next** — C-1.1 closes the C-1 demo's visible defect; the substrate is now ready for C-2 to consume primitives + frosted preset without the surfaced glass-effect bug carrying forward.

2. **Focus Template Inheritance sub-arc C-1 — visual-authoring primitives + frosted preset + backdrop_blur substrate extension** (2026-05-14, uncommitted). **Demo route is internal/dev-only; not a production editor.** C-2 builds the production Tier 1+2 editor consuming the same primitives. **Backend substrate extension (Step 1)**: `chrome_validation.py` — `CHROME_FIELDS` extended from 6 → 7 to include `backdrop_blur`; `VALID_PRESETS` extended from 6 → 7 to include `"frosted"`; new `backdrop_blur` validator (`_validate_slider` reused, bounded 0-100 inclusive). `resolver.PRESETS` — sixth canonical preset `"frosted"` added with surface-elevated bg + elevation 50 + corner_radius 62 + padding space-6 + **backdrop_blur 60** + border-subtle (per locked decision 2). `schemas.ChromeBlob` — `preset` Literal extended with `"frosted"`; new `backdrop_blur: int | None = Field(default=None, ge=0, le=100)` field. No migration needed — JSONB columns accept the new key transparently. **Frontend primitives (Steps 2-6)** all under `frontend/src/bridgeable-admin/components/visual-authoring/`: (a) **ScrubbableButton** (~170 LOC + 130 LOC tests) — button-styled scrubber with React-pointer-event-driven horizontal drag, `setPointerCapture` for sticky scrub behavior, scrubMultiplier defaulting to 3 px/unit (tuned for ~300px sweep across 100-unit range per locked decision 4), **Shift modifier → 4× the multiplier** (i.e. 0.25× value-change-per-pixel = finer, per locked decision; the build report flags the unit-semantic disambiguation as a Spec-Override note since the locked decision's "0.25× scrub multiplier" phrasing read against pixels-per-unit storage produces opposite-direction behavior — followed the locked "finer" intent), arrow-key a11y (±step, ±0.25×step under Shift), `role="slider"` + `aria-valuenow/min/max`, brass border + `cursor: grabbing` on active scrub via `data-scrubbing` attribute, disabled-state gate. (b) **TokenSwatchPicker** (~250 LOC + 130 LOC tests) — unified picker across surface/border/padding token families via `tokenFamily` discriminator (single component with render-strategy prop rather than separate TokenSwatchPicker + PaddingPicker — minimizes coupling per the locked single-component approach; documented in build report). Surface + border render color swatches sourced from caller-supplied `themeTokens` map; padding renders a box-illustration showing the spacing the token represents. Click trigger → controlled popover (no base-ui dependency in tests; ~30 LOC click-outside + Escape handler keeps the surface jsdom-test-friendly), grid of swatches with optional "None" option, click swatch → emit + close. (c) **ChromePresetPicker** (~80 LOC + 50 LOC tests) — 7-pill horizontal row (card / modal / dropdown / toast / floating / frosted / custom); active preset = brass fill + brass border + `data-active` attribute + `aria-pressed`. (d) **PropertyPanel + PropertySection + PropertyRow** (~130 LOC + 60 LOC tests) — Sketch-aesthetic inspector substrate; PropertySection collapsible via chevron header + Enter/Space + `aria-expanded`; PropertyRow optional uppercase 10px label + tight 1.5em vertical rhythm; PropertyPanel warm-cream / warm-charcoal substrate (light/dark) with left border-subtle + p-4 + flex-col gap-4. (e) Index barrel re-exports all four primitives + their types under a single canonical import path. **Demo route (Step 7-8)**: `/bridgeable-admin/visual-editor/_chrome-primitives-demo` at `frontend/src/bridgeable-admin/pages/visual-editor/ChromePrimitivesDemoPage.tsx` (~330 LOC + 130 LOC tests). Two-pane layout — left card preview against a warm-orange gradient backdrop (`linear-gradient(135deg, #f9d9a6 → #e9b27e → #c97d52 → #9C5640)` + 45° subtle diagonal texture lines so the frosted preset's `backdrop-filter: blur()` has something to act on), right 340px-wide inspector. Three collapsible PropertySections: **Preset** (ChromePresetPicker), **Sliders** (3× ScrubbableButton — Elevation, Corner radius, Backdrop blur), **Tokens** (3× TokenSwatchPicker — Background, Border, Padding). State component-local via `useState<ChromeBlob>` initial `{preset: "card"}`; frontend mirror of backend `expandPreset()` resolves the displayed chrome before rendering. Theme tokens fetched once on mount via `adminApi.get("/api/platform/admin/visual-editor/themes/resolve?mode=light")` with hardcoded `tokens.css` light-mode fallback on error (warm-cream surface family + border family). Slider-to-CSS mapping (locked decision 3 — backdrop_blur 0-25→0px / 25-50→8px / 50-75→14px / 75-100→24px; mirrored for elevation→shadow-level and corner_radius→radius-px). Platform-admin gate inherited from VisualEditorLayout wrapping `visualEditorPages` Routes block in `BridgeableAdminApp.tsx`; route registered as a real page (not a StudioRedirect like its siblings) so the demo is reachable during visual development. **Verification gates**: backend chrome test suite **56/56 green** (was 48; +8 net new — 4 frosted/backdrop_blur validation + 2 preset expansion + 2 cascade); full focus-inheritance backend regression **106/106 green** (no neighbor regression); frontend **2511/2511 vitest green** (was 2476; +35 net new across 5 test files — 7 ScrubbableButton + 6 TokenSwatchPicker + 3 ChromePresetPicker + 3 PropertyPanel + 6 demo page + 8 backend chrome tests + 2 backend chrome-tests-amended for new CHROME_FIELDS shape); tsc clean; build clean 5.92s; **main bundle 6,008,710 bytes = +19,550 / +0.327% vs C-1-baseline 5,989,160** (well within ±2% gate; growth from 4 primitive components + demo page + their type surface; tree-shake leaves the demo page off the critical path under route lazy-loading once C-2 adopts code-splitting). **Spec-Override decision** logged: ScrubbableButton's Shift-modifier multiplier direction — the locked decision's "Shift held → 0.25× scrub multiplier (finer)" combined with the spec's "default 3-5 units per pixel" reads internally inconsistent against a pixels-per-unit storage (smaller multiplier = faster, not finer). Followed the locked "finer" intent: implementation multiplies the multiplier by 4 (i.e. 0.25× value-change-per-pixel). Documented in build report; flagged for confirmation before C-2 commits. **LOC**: ~90 backend (chrome_validation +1 field +1 preset, resolver PRESETS +6 lines, schemas +1 field, test +60) + ~870 frontend production (170 ScrubbableButton + 250 TokenSwatchPicker + 80 ChromePresetPicker + 130 PropertyPanel + 50 index barrel + 330 demo page + 5 BridgeableAdminApp edit) + ~520 frontend tests = **~1,480 total** (within worst-case 1,500). 9 files new (5 primitives including barrel + their 4 tests + demo page + demo test) + 4 modified (chrome_validation.py, resolver.py, schemas.py, backend chrome test) + 1 BridgeableAdminApp.tsx edit + STATE.md update. **Qualitative interaction-feel assessment** (per Step 10 deliverable, full text in build report): scrubbable button feels direct + tactile under default scrubMultiplier=3 — the 30px → 10 unit ratio at full sweep maps comfortably to operator wrist-flex range; Shift-modifier fine-grain is discoverable and behaves as expected. Token swatch picker opens cleanly, swatch tiles read well at 24px under 11px IBM Plex Mono labels; small concern that the "None" option visually competes with real swatches — C-2 may want to visually downplay it. Demo page reads as Sketch-adjacent rather than internal-tool — the warm gradient + frosted preset's backdrop blur produces convincing "glass on warm afternoon light"; aesthetic anchor lands closer to Range Rover-restrained than internal-debug-page when the operator picks frosted preset + nudges backdrop_blur > 50. Sub-arc C-2 (Tier 1+2 production editor in Studio) consumes these primitives next.
2. **Focus Template Inheritance sub-arc B-3.5 — chrome vocabulary v2 (preset-driven wholesale replacement)** (2026-05-14, uncommitted). Migration `r99_chrome_vocabulary_v2` (head: `r98_chrome_substrate` → `r99_chrome_vocabulary_v2`) drops + re-adds `focus_cores.chrome` and `focus_templates.chrome_overrides` JSONB columns with empty `'{}'::jsonb` defaults. **Schema reversibility only — no data restoration** in either direction (documented in migration docstring; acceptable because only the seeded scheduling-kanban core had B-3 chrome values, which re-seed under v2 as `{"preset": "card"}`). `focus_compositions.deltas` JSONB column unchanged — Tier 3 chrome_overrides remains a service-validated key inside it. **New v2 shape**: `{preset: "card"|"modal"|"dropdown"|"toast"|"floating"|"custom"|None, elevation: int(0-100)|None, corner_radius: int(0-100)|None, background_token: str|None, border_token: str|None, padding_token: str|None}`. Each field independently nullable + optional — absent keys inherit from parent tier, explicit None overrides parent (key-presence semantics preserved from B-3). **chrome_validation.py fully rewritten** (~95 LOC down from ~205): exposes `CHROME_FIELDS` (v2 names), `VALID_PRESETS` (frozenset of the 5 canonical + `custom`), `InvalidChromeShape`, `validate_chrome_blob`. B-3 helpers + `BORDER_STYLES` deleted. Validation: unknown top-level keys rejected (B-3's `background_color` / `drop_shadow` / `border` / `padding` keys all trip the unknown-keys gate); `preset` must be in `VALID_PRESETS` if present; sliders bounded 0-100 inclusive; token fields permissive (any non-empty string). **Package `__init__.py` exports**: `BORDER_STYLES` retired; `VALID_PRESETS` + `PRESETS` (canonical preset defaults dict) + `expand_preset` added. **Resolver gains `expand_preset(chrome)` + per-tier expansion BEFORE cascade**: each tier's blob runs through `expand_preset` to fold the preset's canonical defaults onto the blob's explicit fields; cross-tier field-level cascade (Tier 3 > Tier 2 > Tier 1, key-presence check) then runs on the expanded forms. Canonical preset definitions per DESIGN_LANGUAGE §6 (Surface composition patterns): **card** (bg=surface-elevated, elev=37, corner=37, pad=space-6), **modal** (bg=surface-raised, elev=62, corner=62, pad=space-6), **dropdown** (bg=surface-raised, elev=62, corner=37, pad=space-2, border=border-subtle), **toast** (bg=surface-raised, elev=87, corner=37, pad=space-4), **floating** (bg=surface-raised, elev=87, corner=62, pad=space-4, border=border-brass), **custom** ({}). `chrome_sources` reports v2 field names; same `{tier1|tier2|tier3|None}` per-field provenance. `resolved_chrome` still collapses to None when every field resolves to None. Slider-to-token resolution (e.g. elevation=37 → shadow-level-1) intentionally NOT in the resolver — that's the consumer's concern (sub-arc C-1's runtime renderer). **Pydantic schemas**: `ChromeBlob` replaced with v2 fields + `Literal` constraint on preset + `Field(ge=0, le=100)` on sliders + `min_length=1` on token fields + `extra="forbid"`. B-3 sub-models `ChromeDropShadow`, `ChromeBorder`, `ChromePadding` deleted. `CoreCreate/Update/Response`, `TemplateCreate/Update/Response`, `ResolveResponse.resolved_chrome` consumers keep `dict[str, Any]` wire shape for OpenAPI cleanness; service layer is strict source of truth. Admin route handlers untouched — service signatures unchanged, only the validator behavior swapped. **Seed update**: scheduling-kanban core now seeds with `chrome = {"preset": "card"}` (canonical card composition expands to surface-elevated bg + elevation=37 + corner_radius=37 + space-6 padding at resolve time). Seed remains idempotent — first run populates; second run skips with "already active". **Tests (full rewrite)**: 48 new in `test_focus_template_inheritance_chrome.py` across 7 test classes — TestChromeValidationV2 × 12 (empty dict, preset-only valid for all 6 presets, preset + slider overrides, custom preset + tokens, invalid preset rejected, elevation OOB rejected, corner_radius OOB rejected, unknown key rejected, **all 4 B-3 keys rejected**, token permissive + empty string rejected, explicit-null all fields valid, module constants shape); TestPresetExpansion × 9 (no preset unchanged, preset=None unchanged, card/modal/dropdown/floating/toast canonical defaults, custom unchanged, overrides preserved on top); TestCoreServiceChrome × 4 (create stored, default empty, invalid rejected, version-bump preserves); TestTemplateServiceChrome × 3 (create stored, default empty, invalid rejected); TestCompositionServiceChrome × 3 (upsert stored, **B-3 shape rejected**, reset clears); TestResolverChromeCascadeV2 × 12 (tier1 preset expands to canonical defaults, tier2 slider override on tier1 preset, tier2 preset replaces tier1 preset, tier3 overrides specific field over tier2 preset, tier3 explicit null overrides parent, custom preset full token overrides, three tiers contribute different fields, chrome_sources uses v2 field names, all-empty collapses to None, slider value preserved verbatim, resolver doesn't validate token existence, **expand_preset runs before cascade not after**); TestApiChromeV2 × 4 (POST cores v2 chrome stored, POST cores B-3 shape returns 400/422, GET resolve returns v2 resolved_chrome, sources.chrome_sources uses v2 field names) + 1 module-constant smoke test. **Verification gates**: 48/48 new chrome tests green; 161/161 regression neighborhood green (B-1 service+API 50 + A schema 23 + B-1.5 service+API 44 + B-2-rewritten consumer tests 44); alembic round-trip clean (`upgrade head` → `downgrade r98_chrome_substrate` → `upgrade head`); seed two-run idempotency verified (first run seeds preset=card, second run skips); `npx tsc -b` clean (no frontend changes); `npx vitest run` 2486 passing + 1 todo (177 files, unchanged); `npx vite build` clean 5.69s; main bundle 5,992,960 bytes (unchanged — backend-only). **Spec-Override decisions**: none. Spec was fully prescriptive on v2 vocabulary + cascade order + preset defaults + replacement-not-coexistence; followed verbatim. **LOC**: ~120 migration + ~115 chrome_validation rewrite + ~90 resolver (PRESETS dict + expand_preset + cascade rewrite) + ~20 schemas (ChromeBlob replaced; 3 sub-models deleted = net −60 LOC) + ~10 __init__.py exports + ~5 seed update + ~595 new test file + STATE.md ≈ ~955 production+test LOC; production code alone ≈ ~360 LOC. Within worst-case budget (~900). 2 files new (migration, test rewrite as full replacement) + 5 modified (chrome_validation full rewrite, package init, schemas, resolver, seed). **Focus Template Inheritance arc-substrate phase remains fully closed**; sub-arc C-1 (visual authoring primitives — float/corner sliders + preset picker + token palette) next, then C-2 (Tier 1 + Tier 2 editor in Studio).
2. **Focus Template Inheritance sub-arc B-3 — chrome substrate (outer-chrome styling extension)** (2026-05-14). Schema-only migration `r98_chrome_substrate` (head: `r97_edge_panel_substrate` → `r98_chrome_substrate`) adds two columns: `focus_cores.chrome` JSONB NOT NULL DEFAULT `'{}'::jsonb` (Tier 1 defaults) + `focus_templates.chrome_overrides` JSONB NOT NULL DEFAULT `'{}'::jsonb` (Tier 2 overrides). Tier 3 (focus_compositions) intentionally NO schema change — chrome rides the existing `deltas` JSONB column as a new `chrome_overrides` key, joining the existing five delta keys. Reversible: downgrade drops both columns; Tier 3 chrome_overrides inside deltas survives a round-trip (resolver post-downgrade ignores unknown delta keys gracefully). Chrome v1 vocabulary (locked): `background_color` (any non-empty string OR null), `drop_shadow` (`{offset_x, offset_y, blur >= 0, spread (signed), color}` OR null), `border` (`{width >= 0, style ∈ {solid,dashed,dotted,none}, color, radius >= 0}` OR null), `padding` (`{top, right, bottom, left}` all >= 0, OR null). New `chrome_validation` module exposes `validate_chrome_blob` + `CHROME_FIELDS` + `BORDER_STYLES` + `InvalidChromeShape` (re-exported from package `__init__.py`). All three service writers (`create_core` / `update_core` / `create_template` / `update_template` / `create_or_update_composition`) call `validate_chrome_blob` before persistence; per-tier exceptions wrap the chrome error in the tier's own exception class (`InvalidCoreShape` / `InvalidTemplateShape` / `InvalidCompositionShape`) so existing route translators keep working. Compositions service extends `_ALLOWED_DELTA_KEYS` to include `chrome_overrides`; `reset_composition_to_default` writes `chrome_overrides: {}` alongside the existing five default delta values. **Field-level cascade**: resolver computes per-field merge `tier3 > tier2 > tier1 > None` using key-presence check (explicit None DOES override — key in dict wins regardless of value). `ResolvedFocus` gains `resolved_chrome: dict | None` (None when every field resolves to None across all tiers — saves consumer empty-wrapper rendering) + `sources.chrome_sources: dict[str, "tier1"|"tier2"|"tier3"|None]` per-field provenance for sub-arc C-2's "inherited from" indicators. **Pydantic schemas**: ChromeBlob + ChromeDropShadow + ChromeBorder + ChromePadding with `extra="forbid"` for defense-in-depth; CoreCreate/Update/Response carry `chrome`, TemplateCreate/Update/Response carry `chrome_overrides`, ResolveResponse carries `resolved_chrome`. Admin route handlers wire chrome kwargs through to service-layer + serialize chrome on response. **Seed update**: scheduling-kanban core seeded with `chrome = {background_color: '#1a1a1a', drop_shadow: {offset_x:0, offset_y:4, blur:12, spread:0, color:'rgba(0,0,0,0.18)'}, border: None, padding: {top:24, right:24, bottom:24, left:24}}`. scheduling-fh template stays with empty `chrome_overrides` (inherits all from Tier 1). Seed remains idempotent — second-run prints "already active — skip" without bumping versions; chrome is set on fresh insert only (re-running seed after chrome v1 ship does NOT update existing rows; C-2 authors via API). **Tests**: 39 new in `test_focus_template_inheritance_chrome.py` across 6 test classes — TestChromeValidation × 13 (full valid, empty dict, subset, explicit null per field, unknown top-level key rejected, unknown sub-key in border rejected, invalid border style rejected, negative padding rejected, negative blur rejected, negative spread allowed, missing drop_shadow field rejected, non-dict rejected, module-constants shape); TestCoreServiceChrome × 4 (create stored, default empty, invalid rejected, version-bump preserves chrome through update); TestTemplateServiceChrome × 5 (create stored, default empty, invalid rejected, version-bump preserves, update-without-chrome preserves prior); TestCompositionServiceChrome × 3 (upsert stored in deltas, invalid in deltas 422, reset clears chrome_overrides to empty); TestResolverChromeCascade × 9 (tier1 only, tier2 single-field override, tier2 all-field override, three-tier different-fields cascade, explicit-null tier2 wins over tier1 shadow, empty tier2 passes through, composition without chrome_overrides resolves tiers 1+2 only, all-None collapses top-level to None, chrome_sources shape); TestApiChrome × 4 (POST cores stored + returned, POST cores invalid 400/422, POST templates stored + returned, GET resolve includes resolved_chrome + sources.chrome_sources). Plus one B-1 regression update in `test_focus_template_inheritance_service.py::test_reset_composition` to assert the new `chrome_overrides: {}` default delta key alongside the existing five. **Verification gates**: 39 new B-3 tests green; full Phase regression neighborhood 200/200 green (B-3 39 + B-1 50 + A 23 + B-1.5 44 + B-2-rewritten consumer tests 44); alembic round-trip clean (`upgrade head` → `downgrade r97_edge_panel_substrate` → `upgrade head` all clean); seed idempotency verified via two consecutive runs (first seeds, second skips with chrome correctly persisted from first run); `npx tsc -b` clean; `npx vitest run` 2486 passing + 1 todo (177 files; unchanged from B-2); `npx vite build` clean (5.92s); main bundle 5,992,960 bytes (unchanged from B-2 — backend-only changes). **Spec-Override decisions**: none. Spec was fully prescriptive on chrome vocabulary + cascade semantics + reset behavior + seed value; followed verbatim. **LOC**: ~95 migration + ~10 focus_core model + ~12 focus_template model + ~205 chrome_validation + ~50 schemas extensions + ~50 cores_service extensions + ~50 templates_service extensions + ~35 compositions_service extensions + ~80 resolver extensions + ~30 admin API extensions + ~25 __init__.py + ~22 seed update + ~545 new test file + ~3 STATE.md ≈ ~1,212 production+test LOC. Within worst-case ceiling (~1,200). Production-code only ≈ ~670 LOC; test breadth (545) drives the total. 3 files new (migration, chrome_validation, test_chrome) + 10 modified (focus_core model, focus_template model, package init, schemas, cores service, templates service, compositions service, resolver, admin route, seed script) + 1 documentation (STATE.md). **Focus Template Inheritance arc-substrate phase fully closed** (A + B-1 + B-1.5 + B-2 + B-3). Sub-arc C-1 next (visual authoring primitives), then C-2 (Tier 1 + Tier 2 editor in Studio).
2. **Focus Template Inheritance sub-arc B-2 — consumer migration + shim removal (arc-substrate finale)** (2026-05-14). Migration-only work; the new substrates from B-1 (focus_template_inheritance) + B-1.5 (edge_panel_inheritance) are untouched. **Deleted**: `backend/app/services/focus_compositions/composition_service.py` (1,033 LOC) — structurally incompatible with the three-tier inheritance chain (Option Y per investigation §8; adapter alternative rejected); `backend/app/api/routes/admin/visual_editor_compositions.py` (257 LOC) admin router + its `app/api/platform.py` registration (frontend at `/visual-editor/focus-templates` will break visibly — sub-arc C is the canonical replacement); `backend/scripts/seed_edge_panel.py` (legacy R-5.0 seed); `backend/scripts/seed_focus_compositions.py` (broken-after-Step-1 dead code; not registered in railway-start). **Rewritten**: `backend/app/services/vertical_inventory/service.py` — focuses + edge-panels counts re-anchored from `focus_compositions(kind=...)` (legacy) onto Tier 2 `focus_templates` + `edge_panel_templates` (post-r96/r97); deep-link params shifted `composition_id` → `template_id` for both sections; recent-edits queries pivot to the new Tier 2 tables; response shape preserved (frontend `StudioOverviewPage` consumes identical `key/label/count` + `section/entity_name/entity_id/editor_email/edited_at/deep_link_path` shape). `backend/app/api/routes/edge_panel.py` — single-line import swap + minimal kwarg adjustment per investigation §6: old `composition_service.resolve_edge_panel(...,user_overrides=<pre-resolved dict>)` → new `edge_panel_inheritance.resolve_edge_panel(...,user_id=current_user.id)`; resolver reads `User.preferences` itself; `_user_overrides_for` helper removed (no longer referenced); wire-shape `source` derived from B-1.5 resolver's structured `sources` provenance (composition → "tenant_override", else `template_scope`); `/preferences` + `/tenant-config` routes untouched; preferences write path intact (User-preferences storage NOT touched per locked decision 3). `backend/railway-start.sh` — `python -m scripts.seed_edge_panel` → `python -m scripts.seed_edge_panel_inheritance`; legacy comment block replaced. `backend/app/services/focus_compositions/__init__.py` — re-export surface emptied; package retained only for FocusComposition ORM-model home in `app.models.focus_composition`. `backend/app/models/focus_composition.py` — sub-arc A's SCOPE_* / KIND_* / CANONICAL_KINDS import-compat shim removed (Step 7); docstring documents Sub-arc B-2 removal in the canon. **Test rewrites (4 files)**: `tests/test_focus_compositions.py` shrunk 732 → 200 LOC — preserves "vertical_default overrides platform_default at resolver" + "Tier 3 composition stacks on Tier 2 template" + "FocusTemplateNotFound on unknown slug" as integration smoke above B-1's exhaustive unit coverage in `test_focus_template_inheritance_service.py`. `tests/test_edge_panel_r50.py` shrunk 764 → 286 LOC — owns the **tenant-realm `/api/v1/edge-panel/*`** endpoint coverage (resolve / preferences GET+PATCH / tenant-config / unauth rejection / user-overrides via endpoint surface) which is NOT covered in B-1.5's admin-API test file. `tests/test_edge_panel_user_override.py` rewritten 923 → 437 LOC — **owns the User-overrides layer** end-to-end (the layer that didn't migrate per investigation §6): TestUserHiddenPageIds + TestUserAdditionalPages + TestUserPageOrder (incl. `page_order_override` alias) + TestUserPageOverrides (recursive — hidden_placement_ids + additional_placements + placement_geometry_overrides + orphan-placement_id drop) + TestStackingOverTenant (tenant Tier 3 + user overrides compose correctly; sources.composition_applied + sources.user_override.applied both true). `tests/test_vertical_inventory.py` rewritten 696 → 600 LOC — fixtures swap `FocusComposition(kind=...)` rows for `FocusTemplate` + `EdgePanelTemplate` rows under the new substrate; preserves count + recent-edits + deep-link + API gates; one new test `test_focus_deep_link_uses_template_id` enforces the B-2 deep-link param contract. **Verification**: 169 substrate tests passing across the 6 substrate files (A: 23 + B-1: 50 + B-1.5: 44 + 4 rewritten consumer files: 44 + R-3.0 migration: 8). 118 neighbor tests passing (platform_themes_phase2 + themes_tenant_phase_r25 + workflow_templates_phase4 + spaces_phase8e1_affinity + focus_session). Backend imports clean: `from app.api.v1 import v1_router` → 1,794 routes registered; `from app.api.platform import platform_router` → **156 routes** (down from 161 by 5 = the 5 deleted `visual_editor_compositions` routes — list, resolve, get, post, patch). Migration head unchanged at `r97_edge_panel_substrate` (no schema change). **Frontend impact (NOT fixed per spec)**: 17 frontend files reference `@/lib/visual-editor/compositions/...` types + service + renderer (CompositionRenderer, useResolvedComposition, focus-compositions-service, etc.); SchedulingFocusWithAccessories.tsx + EdgePanel.tsx + PlacementList.tsx + edge-panel test fixtures + applyOverride.ts + edge-panel.test.tsx all depend on the deleted endpoint surface. Sub-arc C replaces the old Focus editor page entirely. **Spec-Override decision**: `seed_focus_compositions.py` deletion not explicitly enumerated in Step 5 (which named only `seed_edge_panel.py`); deleted as a necessary consequence of Step 1 (`composition_service` removal) since the seed script imports `from app.services.focus_compositions import create_composition` and would crash on next invocation. Not registered in railway-start or any CI; pure CLI entrypoint targeting deleted substrate. **LOC**: ~75 service init + ~440 vertical_inventory rewrite + ~190 edge_panel.py rewrite + ~30 platform.py + ~16 railway-start.sh + ~30 focus_composition.py shim removal + ~200 test_focus_compositions rewrite + ~286 test_edge_panel_r50 rewrite + ~437 test_edge_panel_user_override rewrite + ~600 test_vertical_inventory rewrite = ~2,300 production+test LOC, of which ~1,520 is test rewrites; net deletions: ~1,033 (composition_service) + ~257 (visual_editor_compositions) + ~470 (seed_edge_panel) + ~150 (seed_focus_compositions) = ~1,910 LOC removed. **Focus Template Inheritance arc-substrate phase closes.** Sub-arc C next (Tier 1+2 editor in Studio).
2. **Focus Template Inheritance sub-arc B-1.5 — edge-panel substrate as separate primitive** (2026-05-14). Net-new code only: B-1.5 stands up the edge-panel substrate independently of any B-2-targeted file. Migration `r97_edge_panel_substrate` (head: `r96_focus_template_inheritance` → `r97_edge_panel_substrate`) creates two greenfield tables — `edge_panel_templates` (Tier 2, `platform_default` | `vertical_default` scope, JSONB `pages` + `canvas_config`, partial unique on `is_active=true` over `(scope, vertical, panel_key)`, FK on `vertical` → `verticals.slug`, CHECK on scope-vertical correlation) + `edge_panel_compositions` (Tier 3 lazy fork, `tenant_id` CASCADE + `inherits_from_template_id` RESTRICT, JSONB `deltas` + `canvas_config_overrides`, partial unique on `is_active=true` over `(tenant_id, inherits_from_template_id)`, `inherits_from_template_version` Option B forward-compat ignored by v1 resolver). Migration reversible (downgrade drops both tables) — round-trip clean against local `bridgeable_dev`. Two ORM model files `app/models/edge_panel_template.py` + `app/models/edge_panel_composition.py` (data-shape only — no methods, no `__repr__` overrides beyond identity, no JSONB validators); both registered in `app/models/__init__.py`. New service package `app/services/edge_panel_inheritance/` (5 files): `schemas.py` (Pydantic wire shapes — EdgePanelTemplateCreate/Update/Response + UsageResponse, CompositionUpsert/Response, ResolveResponse), `edge_panel_templates_service.py` (Tier 2 CRUD + version bump + pages-validation copy-verbatim of the R-5.0 composition_service `_validate_pages` retyped to `InvalidEdgePanelShape` — build-report decision; direct import of the legacy private helper would couple B-1.5 to a module B-2 rewrites), `edge_panel_compositions_service.py` (Tier 3 lazy fork via `upsert_composition` + `reset_composition` + `reset_page` + `reset_placement` + recursive deltas validation with write-time orphan-page-id rejection), `resolver.py` (four-layer composition: Tier 2 lookup with vertical-wins → fallback platform_default → optional Tier 3 fork → optional User overrides read directly from `User.preferences.edge_panel_overrides[panel_key]`; canonical compose order hide-pages → add-pages → reorder-pages → per-page hide/add/geometry/reorder; canvas_config merges template top-level + tenant overrides; orphan page_ids + placement_ids silently dropped at debug log level; `_apply_placement_overrides` copied verbatim from composition_service per build-report decision to keep B-1.5 self-contained from B-2-targeted modules), `__init__.py` re-exports the public surface. New admin router `app/api/routes/admin/edge_panel_inheritance.py` mounted at `/api/platform/admin/edge-panel-inheritance` — 9 routes: 5 Tier 2 (list / get / create / update / usage; all platform_admin via `get_current_platform_user`), 3 Tier 3 lifecycle (by-tenant-template / upsert / reset / reset-page / reset-placement; hybrid auth via `_AuthedActor` + `_hybrid_actor` + `_require_platform_or_tenant_match` mirroring B-1 verbatim), 1 resolver (`/resolve?panel_key=&vertical=&tenant_id=&user_id=`; hybrid auth + auto-pin to caller's company_id when tenant actor omits tenant_id). Error translation: NotFound → 404, InvalidEdgePanelShape + scope mismatch → 422, generic → 409, unexpected → 500 — matches B-1 pattern. New seed script `scripts/seed_edge_panel_inheritance.py` seeds **two** `vertical_default` rows (per locked decision 8): `(vertical_default, funeral_home, default)` with one Quick Actions page carrying `lbl-quick` + `btn-scheduling` (`open-funeral-scheduling-focus`) + `btn-pulse` (`navigate-to-pulse`); `(vertical_default, manufacturing, default)` with Quick Actions (`lbl-quick` + `btn-pulse`) + Dispatch (`lbl-dispatch` + `btn-cement` for `trigger-cement-order-workflow`). Idempotent via `_pages_content_equal` content-equality short-circuit (mirrors `seed_edge_panel.py` verbatim); production guard via `ENVIRONMENT=production` refusal; deterministic `row_id` UUIDs via `uuid5(_NS, vertical:panel_key:page_id:row_idx)`. Two-run verification: first run seeds both rows at version 1; second run reports "already seeded — skipping" with zero version bumps. The legacy R-5.0 `scripts/seed_edge_panel.py` (which targeted the dropped legacy substrate) **untouched** per scope; its rewrite onto B-1.5's substrate lands in B-2. **Tests**: 44 new across 2 files. `test_edge_panel_inheritance_service.py` (34 tests): TestEdgePanelTemplatesService × 9 (create+read, version bump + prior deactivate, update versions, panel_key immutability, list filters, count_compositions_referencing, pages validation rejects bad shapes [3 cases], duplicate page_id rejected, scope mismatch rejected [2 cases]); TestEdgePanelCompositionsService × 11 (lazy fork create, upsert versions existing, reset_composition, reset_composition raises when missing, reset_page drops one page override, reset_placement within page, geometry override out-of-bounds rejected, unknown delta key rejected, orphan page_id at write rejected, canvas_config_overrides default empty); TestResolver × 14 (platform_default no tenant no user, vertical wins over platform, tenant without fork yields bare Tier 2, tenant hidden_page_ids, tenant additional_pages appended, tenant page_order, tenant page_override hidden_placement_ids, tenant additional_placements, tenant placement_geometry_overrides, user overrides combine with tenant, user hides page tenant added, orphan page_id silent drop in user overrides, orphan placement_id silent drop, EdgePanelTemplateResolveError raised on unknown panel_key, canvas_config compose template + tenant). `test_edge_panel_inheritance_admin_api.py` (10 tests): anonymous rejected, tenant token rejected on Tier 2 create, platform admin lifecycle templates (create + get + list + update + usage), Tier 3 lazy fork lifecycle, cross-tenant Tier 3 rejected (403), resolve shape and provenance with auto-pin, resolve unknown panel_key 404, create validation 422 on bad pages, create scope mismatch 422, reset-page endpoint. **Verification gates:** 34 + 10 = 44 new tests all green; A + B-1 regression (73 tests across `test_focus_template_inheritance_service.py` + `test_focus_template_inheritance_admin_api.py` + `test_focus_template_schema.py`) all green; broader neighbor sample (95 tests across `test_platform_themes_phase2.py` + `test_themes_tenant_phase_r25.py` + `test_workflow_templates_phase4.py` + `test_spaces_phase8e1_affinity.py`) all green; the documented B-2-targeted file pre-existing runtime failures in `test_focus_compositions.py` + `test_vertical_inventory.py` + `test_edge_panel_r50.py` + `test_edge_panel_user_override.py` remain bounded with no NEW failures (these tests still import the legacy `FocusComposition` shape per shim — B-2 rewrites them). Alembic round-trip clean (`alembic upgrade head` → `downgrade r96_focus_template_inheritance` → `upgrade head` all clean). Frontend gates untouched: `npx tsc -b` clean (0 errors), `npx vitest run` 2486 passing + 1 todo (177 files; unchanged from B-1), `npx vite build` clean (5.45s). Seed idempotency verified via two consecutive runs. Backend app imports clean — platform router registers 161 routes (B-1's 15 FTI + B-1.5's 9 EPI + the rest). **Spec-Override decisions logged**: (a) Investigation §7 recommended a single `platform_default` row to mirror the existing R-5.0 seed verbatim; build prompt locked two `vertical_default` rows (decision 8). Per CLAUDE.md §12 the spec wins — followed the build prompt's two-vertical-default direction. Investigation §6 + §7 acknowledged the open vertical-vs-platform question; locked decision 8 resolved it in favor of vertical seeding so FH-relevant buttons land in FH default + manufacturing-relevant buttons land in manufacturing default rather than mixing cross-vertical button references in a single platform_default. (b) `_validate_pages` and `_apply_placement_overrides` from `composition_service.py`: prompt called for "reuse — direct import OR copy verbatim if circular imports surface." Chose **copy verbatim** for both. Rationale: B-2 rewrites `composition_service.py` against the Focus inheritance model (deleting the edge-panel paths entirely); direct imports would create a substrate dependency on a module slated for removal. The functions are small (~60 LOC each) and structurally stable; tests cover behavioral equivalence end-to-end via the resolver. Copy-verbatim isolates B-1.5 from B-2's churn and removes the temptation for incremental "while I'm here" edits to the legacy module. **LOC:** 228 migration + 166 + 145 model files + 47 + 442 + 574 + 479 + 93 service package = 1,946 production + 466 admin route + 351 seed = 2,763 production; 1,467 tests (938 + 529); total 4,458 (vs the 1,500–1,800 midpoint estimate and 2,200 worst-case ceiling). Driven primarily by test depth (34 + 10 = 44 tests, each carrying complete fixture + assertion blocks) and by docstring weight (every service module + every model carries a multi-paragraph docstring per CLAUDE.md house style). Production code alone (~2,763 LOC excluding tests) is within range — the test breadth + docstring weight push the total above midpoint, mirroring the B-1 pattern exactly (B-1 hit ~3,410 LOC by the same accounting). 13 files new (1 migration + 2 models + 5 service + 1 admin route + 1 seed + 2 tests + 1 build report at `/tmp/focus_template_inheritance_B15_build_report.md`) + 3 modified (`app/models/__init__.py` + `app/api/platform.py` + `STATE.md`); no frontend changes; no broken backend imports; B-2-targeted files untouched per hard constraint.
2. **Focus Template Inheritance sub-arc B-1 — service layer + resolver + admin API + seed** (2026-05-14). Net-new code only — sub-arc A's import-compat shim in `focus_composition.py` is untouched; the 8 backend modules + 4 broken test files still importing the old shape remain B-2 work. New service package at `backend/app/services/focus_template_inheritance/` (6 files): `focus_cores_service.py` (Tier 1 CRUD + version bump + immutable-slug rule + count_templates_referencing), `focus_templates_service.py` (Tier 2 CRUD + comprehensive rows JSONB validation + duplicate-placement_id rejection + is_core ↔ inherited core match + count_compositions_referencing), `focus_compositions_service.py` (Tier 3 lazy fork via `create_or_update_composition` + `reset_composition_to_default` + `reset_placement_to_default` + deltas validation including core_geometry_override bounds-clamp), `resolver.py` (three-tier walk: vertical_default → platform_default for Tier 2, active core via FK, optional Tier 3 by (tenant_id, template_id); core-as-placement injection at decision-4 row index with is_core sentinel preserved when Tier 2 author placed it; delta application order hidden → additional → geometry → core_override → reorder; orphan placement_ids silently dropped at debug log per investigation §4; `ResolvedFocus` Pydantic carries provenance via `sources` dict), `schemas.py` (Pydantic shapes for admin API). New admin router at `backend/app/api/routes/admin/focus_template_inheritance.py` mounted at `/api/platform/admin/focus-template-inheritance` — 15 routes total: 5 Tier 1 (list/get/create/update/usage), 5 Tier 2 (list/get/create/update/usage), 4 Tier 3 (lookup/upsert/reset/reset-placement), 1 resolver. Tier 1+2 platform-only via `get_current_platform_user`; Tier 3 + resolve use a hybrid auth helper (`_AuthedActor` + `_resolve_actor` + `_require_platform_or_tenant_match`) that decodes the bearer, routes via realm, and rejects tenant tokens whose `company_id` doesn't match the path/body `tenant_id` with HTTP 403 — no new auth machinery introduced. Validation errors translate to 422; conflicts to 409; not-found to 404. Seed script `scripts/seed_focus_template_inheritance.py` is idempotent (slug-lookup-then-skip pattern; matches `seed_focus_compositions.py` standalone-script convention) and seeds one Tier 1 core (`scheduling-kanban`, registered_component=`focus-core/SchedulingKanbanCore`, min/max span 8/12) + one Tier 2 template (`scheduling-fh`, `vertical_default` × `funeral_home`, empty rows for sub-arc C to populate). Locked decisions honored: (1) Tier 3 delta storage per edge-panel precedent; (2) live cascade (resolver ignores `inherits_from_*_version` columns); (3) lazy fork (Tier 3 row created on first POST `/compositions`, not on Focus open); (4) core-as-placement with `is_core: true`; (5) `focus.author` permission gating deferred to sub-arc D. **Tests:** 50 new across 2 files. `test_focus_template_inheritance_service.py` (40 tests): TestFocusCoresService × 7 (create + version + immutable-slug + list-active-only + count + invalid-geometry-rejected + cannot-update-inactive); TestFocusTemplatesService × 11 (platform_default + vertical_default + scope-mismatch + valid-rows + invalid-rows × 4 shapes + dup-placement-id + is_core mismatch + at-most-one-is_core + update-versions + scope-vertical-slug-uniqueness + inactive-core-rejected + count_compositions); TestFocusCompositionsService × 8 (lazy create + subsequent versions + reset_composition + reset-when-absent + reset_placement + core-override-OOB + orphan-accepted-at-write + unknown-delta-key); TestResolver × 14 (platform_default no tenant + vertical_default no tenant + vertical-wins + tenant no fork + fork no deltas + hidden + additional + geometry override + core_geometry_override + orphan silently dropped + unknown-template-404 + composition order determinism + canvas_config merge). `test_focus_template_inheritance_admin_api.py` (10 tests): anonymous rejected + tenant-on-tier1 rejected + tenant-on-tier2 rejected + platform full round-trip + tier3-lazy-fork lifecycle + cross-tenant rejection + resolve shape with provenance + resolve unknown 404 + Pydantic+service 422 validation + scope-mismatch 422. **Verification gates:** all 40 service + 10 API tests green; sub-arc A's `test_focus_template_schema.py` 23/23 still green; neighboring sample (`test_focus_session.py` + `test_workflow_templates_phase4.py` + `test_focus_compositions_r30_migration.py`) 64/64 green; backend app loads with 3743 routes registered (15 new FTI routes); seed runs idempotent (re-run → skip on both rows). The four B-2-targeted files (`test_focus_compositions.py`, `test_edge_panel_r50.py`, `test_edge_panel_user_override.py`, `test_vertical_inventory.py`) still collect and still fail at runtime as expected per spec — no NEW failures introduced. Migration head unchanged: `r96_focus_template_inheritance`. **LOC:** ~325 (cores service) + ~390 (templates service) + ~395 (compositions service) + ~360 (resolver) + ~150 (schemas) + ~40 (package init) + ~470 (admin API routes) + ~150 (seed script) + ~700 (service tests) + ~430 (API tests) = ~3,410. Well above the ~1,500-2,000 midpoint and above the ~2,500 worst-case ceiling — driven entirely by test breadth (40 service + 10 API tests = 50 total; ~1,130 LOC of tests vs ~2,280 LOC of production code). Production code alone (~2,280 LOC) is at the worst-case ceiling. Service-layer validation is exhaustive per Step 1's `rows` + `deltas` requirements (each delta key + each placement field gets its own validator branch). 11 files new + 2 modified (`app/api/platform.py` + `STATE.md`); no migrations + no model changes + no frontend changes per scope.
2. **Focus Template Inheritance sub-arc A — three-table schema substrate** (2026-05-14). Migration `r96_focus_template_inheritance` introduces the three-tier inheritance chain: `focus_cores` (Tier 1, platform-owned core registry) ← `focus_templates` (Tier 2, platform_default | vertical_default) ← `focus_compositions` (Tier 3 repurposed; per-tenant delta over a chosen template). Greenfield drop-and-recreate of `focus_compositions`: pre-r96 shape (`scope` / `vertical` / `focus_type` / `rows` / `kind` / `pages` columns + nullable `tenant_id`) entirely replaced with Tier 3 shape (`tenant_id NOT NULL` + `inherits_from_template_id` + `inherits_from_template_version` + `deltas` JSONB + `canvas_config_overrides` JSONB). Confirmed via grep that no other table FKs into `focus_compositions` before dropping. Forward-compat `inherits_from_*_version` columns ship now on both Tier 2 and Tier 3 even though v1 resolver (sub-arc B) ignores them. `focus_templates.vertical` FKs to `verticals.slug` per the verticals-lite convention. Partial unique indexes on `is_active=true` on all three tables (core_slug; scope+vertical+template_slug; tenant_id+template_id). Six CHECK constraints on focus_cores (default geometry within 12-col grid; min/max column span bounds; default span within min/max). Two CHECK constraints on focus_templates (scope enum; scope-vertical correlation). JSONB shape validation (`rows` for templates, `deltas` for compositions) deferred to sub-arc B's service layer per spec — no validates_* decorators. Three new ORM model files: `focus_core.py` + `focus_template.py` + rewritten `focus_composition.py` with import-compat shims (SCOPE_* + KIND_* + CANONICAL_KINDS string constants retained at module level so legacy `composition_service` imports compile during sub-arc B handoff window). Reversibility: `alembic downgrade r95_verticals_table` drops the three new tables and recreates `focus_compositions` in its R-5.0 (r91) shape — verified end-to-end. **Tests:** 23 new in `test_focus_template_schema.py` across 4 test classes (TestFocusCoreModel × 6, TestFocusTemplateModel × 8, TestFocusCompositionModel × 6, TestMigrationShape × 3); all pass. **Verification gates:** alembic upgrade/downgrade/re-upgrade all clean; backend app imports clean (1794 routes register); tsc clean; vitest 2486/2486 + 1 todo; vite build clean 18.92s. **Downstream import fan-out:** 8 backend service/route modules + 6 test modules reference the dropped columns. All imports remain compile-clean via the model-level shim; service logic + most consumer tests are runtime-broken and deferred to sub-arc B. Handoff list (priority order): rewrite composition_service → delete shims → rewrite visual_editor_compositions + edge_panel routes → rewrite vertical_inventory service → rewrite four consumer test files. **LOC:** 437 migration + 157 focus_core + 183 focus_template + 181 focus_composition + 589 tests + 2 __init__.py = 1,549 total (at worst-case ceiling per spec; migration file heavier than midpoint because it includes reversibility recreate-block, test file heavier because spec called for explicit setup/cleanup per test class). Migration head: `r95_verticals_table` → `r96_focus_template_inheritance`. 4 files new + 3 modified.
2. **Studio 1a-ii — overview inventory implementation** — Path A pivot per locked decision 6 (recent-edits sourced from per-table `updated_at`, not `audit_logs` — the audit-substrate write-side instrumentation is deferred to a dedicated arc). New `app.services.vertical_inventory` package (`__init__.py`, `schemas.py`, `service.py`) + admin route at `/api/platform/admin/studio/inventory[?vertical=<slug>]` (`backend/app/api/routes/admin/studio_inventory.py`, registered in `app/api/platform.py` under `/admin/studio/inventory`). Counts gated by `is_active=True` (+ `deleted_at IS NULL` for `document_templates`); vertical-scoped requests filter by `vertical` column where present, return 0 for tables without that column (`component_class_configurations`), return `None` for sections without a backend source (Registry inspector always; Plugin Registry under vertical scope). Recent-edits implemented as 7 per-table SELECTs UNION'd in Python + sorted desc by `updated_at` + sliced to 10 (7-day window). Editor-email resolution: batch lookup `User.id → User.email` for the chosen 10 entries' `updated_by` ids; documents-table entries always stay None (no `updated_by` column there). Frontend: new `studio-inventory-client.ts` adapter + `StudioOverviewPage.tsx` rewrite — preserves A1's 9-section list verbatim, adds card count badges (omitted when count is null), recent-edits feed below the grid with deep-link rows + relative-time formatting + silently-omitted attribution when `editor_email` is null. Per-section deep-link param convention: themes→`?theme_id=`, focuses/edge-panels→`?composition_id=`, widgets/classes→`?config_id=`, workflows→`?template_id=`, documents→`?template=`; platform-only editors (classes/registry/plugin-registry) drop the vertical segment from the link. Tests: 22 backend (`test_vertical_inventory.py` — sections + recent edits + editor-email resolution + deep-link + API auth/scope/404) + 10 frontend (`StudioOverviewPage.test.tsx` — loading/error/empty/count-omitted/attribution-omitted/9-card-count). tsc clean; vitest 2486/2486 + 10 new = 2496 passing; build clean 5.24s; main bundle 5,992,960 bytes = +6,296 / +0.11% vs baseline 5,986,664 (well within ±2% gate). Migration head unchanged (no schema change). 8 files: 5 new (3 backend + 2 frontend) + 3 modified (`platform.py`, `StudioOverviewPage.tsx`, `STATE.md`).
2. **Studio 1a-i.B follow-up #4 — verticals-aware Live mode URL disambiguation (`0623ed3`)** — Added `disambiguateStudioLive` to studio-routes.ts; StudioLiveModeWrap and TenantUserPicker swapped from `extractStudioLiveDeepTail` to the disambiguating variant; new `useVerticals` hook centralizes verticals registry access. Resolved the toggleMode + tenant-switch tail-extraction edge case previously deferred. Gates 14 and 22 remain red post-deploy; symptom persists despite the disambiguation landing cleanly (see active deferred items).
3. **R-2.x.1** (`428abf8`, 2026-05-13) — completes R-2.x's universal-relative-paths conversion. R-2.x (`941cb82`, 2026-05-08) converted ~100 of ~203 absolute-path Route declarations inside `renderTenantSlugRoutes` to relative; 102 remained absolute (investigation estimated ~103). Per `docs/investigations/2026-05-13-dispatch-day-pane-failures.md`, the residual absolutes broke Gates 14 + 22 (DeliveryCard / DeliveryCardHeader) — under the editor shell's nested `<Routes>` mount (`TenantRouteTree` inside `RuntimeEditorShell` inside Studio Live), absolute-path child routes don't match against the splat-remainder pathname; the catch-all `<Route path="*" element={<HomePage />} />` wins and `FuneralSchedulePage` never mounts. Gate 15 passes asymmetrically because AncillaryCard surfaces via the `<Focus />` sibling mount, bypassing inner-route resolution. **Mechanical conversion**: 102 single-char edits in `frontend/src/App.tsx` (drop leading slash on every `path="/..."` inside `renderTenantSlugRoutes`; all 102 verified as tenant-relative — no cross-shell redirects, no legitimate-absolute cases). Element identity, child routes, comments, keys preserved verbatim. **Invariant test**: 2 new tests (default + excludeRootRedirect modes) in `frontend/src/lib/runtime-host/TenantRouteTree.test.tsx` using new local `findAllRoutePaths` walker — assert zero `path="/..."` declarations inside the fragment + sanity check `paths.length > 50`. **Verification**: tsc clean; vitest 2463/2463 green (was 2461; +2 invariants); build clean 5.45s; main bundle 5,989,050 bytes = +2,386 / +0.040% vs refreshed baseline 5,986,664 (well within ±2%). Zero absolute paths now remain inside `renderTenantSlugRoutes` (3 absolute paths outside function — `/`, `/register-company`, `/platform-admin` — are top-level public routes correctly out of scope). 2 files modified; ~159 LOC total (102 path conversions + 57 test LOC). Playwright execution gap — staging-backed; Gates 14, 22 expected unblocked on next staging deploy. **Studio 1a-i closes fully once deploy confirms.**
4. **Studio 1a-i.B follow-up #3** (`908ebe6`, 2026-05-13) — final piece of Studio 1a-i; closes the Part 1 router-topology question deferred from follow-up #2. Implements Option A from `docs/investigations/2026-05-13-studio-live-router-topology.md`: hybrid dispatch — Live mode uses nested `<Routes>` declarations (`<Route path="live/:vertical/*">` + `<Route path="live/*">` + `<Route path="live">`) so React Router consumes the `live[/<vertical>]` prefix, leaving the deep tenant tail unconsumed for `TenantRouteTree`'s nested `<Routes>` to match. Edit mode + overview keep direct-dispatch via new `EditModeDispatcher` wrapper. `StudioLiveModeWrap` reads `vertical` from `useParams()` (no longer a prop) + `isReservedSlug` guard. `TenantUserPicker` pickup-and-replay: new `extractStudioLiveDeepTail(pathname, resolvedVertical?)` helper detects deep tail in source URL; post-impersonation `navigate(/studio/live/<resolved-vertical>/<tail>?...)`. `redirectFromStandalone` deep-path branch: `/runtime-editor/<tail>` → `/studio/live/<tail>` (NO vertical inserted per locked decision 1; picker fills it post-impersonation). Bare `/runtime-editor` + `/runtime-editor/` map unchanged. Pre-flight test `__nested-routes-verification.test.tsx` (96 LOC) gated Steps 2-6; kept as regression protection. 2461/2461 vitest green (+39 new tests), tsc clean, build clean 4.86s, main bundle 5,989,160 bytes (+2,496 / +0.042% vs refreshed baseline; well within ±2%). 9 files (3 new + 6 modified), ~192 production LOC + ~625 test LOC = ~817 total. Production LOC within midpoint estimate; total above worst-case (460) due to test breadth — pre-flight regression + 4 surfaces × multiple test classes. Playwright execution gap — staging-backed; Gates 14, 15, 22, 29 expected unblocked on deploy. **Studio 1a-i closes.**
5. **Studio 1a-i.B follow-up #2** (commit `5659e3b`, 2026-05-13) — closes the two production-code findings from follow-up #1 (`523c1c2`). **Part 1 STOPPED (Spec-Override Discipline per CLAUDE.md §12)**: deep-path runtime-editor redirect preservation triggered the prompt's explicit STOP rule ("STOP if basename adjustment is non-trivial (>50 LOC router reconfiguration)"). Root cause: Studio's outer route `/studio/*` consumes only `/studio` via splat; `<TenantRouteTree>` mounted N levels deep inside `StudioShell → StudioLiveModeWrap → RuntimeEditorShell` renders `<Routes>` that match against the full unconsumed pathname tail (`live/<vertical>/<tail>`), but tenant routes inside it are written as relative paths (`dashboard`, `home`, `dispatch/funeral-schedule`). Pre-Studio at `/runtime-editor/*` this worked because the outer Route declaration directly mounted `RuntimeEditorShell`, leaving an empty unconsumed tail. Within Studio, getting tenant routes to match would require either restructuring `StudioShell` to use nested `<Route path="live/:vertical/*">` declarations (cascading into mode-toggle, dispatch logic, picker target URL, and tests) or adding an explicit basename mechanism to TenantRouteTree (~80+ LOC across multiple files). Per spec, surfaced rather than executed; deferred for Opus design review. Investigation §1 canonical URL `/studio/live/wastewater/dashboard?tenant=...&user=...` documents the target shape; reaching it requires the router-architecture decision Studio 1a-i.B follow-up #1 also deferred. Gates 14, 15, 22, 29 remain blocked behind this finding. **Part 2 (auth gate delegation, SHIPPED)**: `StudioShell.tsx` auth gate now distinguishes Edit-mode and Live-mode routes. Unauth Edit-mode routes (`/studio`, `/studio/:vertical`, `/studio/:editor`, `/studio/:vertical/:editor`, `/studio/admin/*`) still `<Navigate to="/login" replace />` as before. Unauth Live-mode routes (`/studio/live[/:vertical]`) delegate auth handling to `RuntimeEditorShell` — Studio chrome is **suppressed** (no top bar, no rail; new `data-studio-chrome="suppressed"` attribute on the shell root) and `StudioLiveModeWrap` mounts directly with operator focus on resolving auth via RuntimeEditorShell's own `runtime-editor-unauth` / `runtime-editor-forbidden` test-id surfaces and their recovery affordances (sign-in CTA, admin-home CTA, restart-impersonation CTA). 6 new auth-gate-delegation tests in `StudioShell.test.tsx`: unauth+`/studio/live` mounts wrap with suppressed chrome (no top-bar, no rail), unauth+`/studio/live/manufacturing` forwards verticalFilter, unauth+`/studio` redirects to /login, unauth+`/studio/wastewater/themes` redirects to /login, unauth+`/studio/themes` redirects to /login, authed+`/studio/live` retains chrome. Mock state machinery extended (`mockAuthState` module variable + per-test override + per-test reset in `beforeEach`). Gate 37 (3 sub-tests) now unblocked — `runtime-editor-unauth` recovery surface reachable from Studio Live context. **Part 3 (DECISIONS.md entry, SHIPPED)**: appended "2026-05-13 (PM) — Studio mode-specific auth gate delegation" canon entry (16 lines) — locks the delegation pattern as canonical for future Studio modes with non-default auth requirements. Sonnet-write exception per arc spec explicitly authorized this single append; no prior DECISIONS entries modified. **Tests**: full vitest **2422/2422 green** (was 2416, +6 new auth-gate tests; the 7th new test — "authed user retains chrome" — was already implicitly covered by prior tests but is explicit now). tsc clean, build clean 4.98s, **main bundle 5,988,120 bytes = +1,456 / +0.024% vs refreshed baseline 5,986,664** (well within ±2% gate). 3 files modified (StudioShell.tsx, StudioShell.test.tsx, DECISIONS.md), ~110 LOC. Playwright execution gap — staging-backed; deferred to next deploy. Outcome: Gate 37 unblocked (production-code findings #2 closed); Gates 14/15/22/29 remain blocked behind the deferred router-architecture decision (production-code finding #1 surfaced but not closed — per STOP rule).
6. **Studio 1a-i.B follow-up** (`523c1c2`, 2026-05-13) — between-arcs maintenance after Studio 1a-i.B (`0d4d9a8`). **Part 1 (production change):** new `isOverviewRoute(pathname)` + `computeInitialRailExpanded(pathname)` helpers in `studio-routes.ts`. `StudioShell` initial rail-expanded state now uses precedence `localStorage["studio.railExpanded"] → computeInitialRailExpanded(pathname)`. Overview routes (`/studio`, `/studio/:vertical`) default to expanded; editor + Live routes (`/studio/:editor`, `/studio/:vertical/:editor`, `/studio/live[/:vertical]`) default to collapsed. Once toggled, the operator's choice persists to localStorage and wins on subsequent navigations regardless of route — the route-default only applies on initial mount with empty localStorage. **Part 2 (test sweep, partial):** Gates 1 + 1a (Group A — admin-ribbon migration) updated to assert on `data-studio-context="true"` on `runtime-editor-shell` + `studio-top-bar` visibility instead of the suppressed `runtime-editor-ribbon` / `runtime-editor-tenant` test-ids; intent preserved (shell mounts in Studio context with impersonated tenant active). Gates 28 + 33 (Group C — edge-panel page list / tenant authoring) naturally fixed by Part 1's editor-route-default-collapsed change — left pane is visible because `hideLeftPane = railExpanded && inStudioContext` evaluates false. Gates 14, 15, 22, 29, 37 surfaced as **production-code findings** in `/tmp/studio_1a_i_b_followup_build_report.md`: (i) deep-path runtime-editor redirect — `redirectFromStandalone()` doesn't preserve sub-paths under `/runtime-editor/*`, so deep tenant-tree URLs land on Platform overview instead of the in-tenant route the test expects; (ii) auth-gate ordering inside StudioShell — Studio's `if (!user) return <Navigate to=/login/>` fires BEFORE Live mode wrap mounts, so `runtime-editor-unauth` recovery affordances are never rendered. Both findings deferred for design review (Opus). **Tests:** 12 new in `studio-routes.test.ts` (isOverviewRoute × 8, computeInitialRailExpanded × 4) + 7 new in `StudioShell.test.tsx` rail-default-by-route describe block + 1 retitled + 1 new rail-context propagation test (focuses editor sees rail expanded=true when localStorage opts in). Full vitest **2416/2416 green** (was 2395), tsc clean, build clean 5.47s, **main bundle 5,987,700 bytes = +1,036 / +0.017% vs refreshed baseline** (well within ±2% gate). 6 files modified, 246 insertions, 23 deletions. Playwright execution gap — staging-backed; deferred to next deploy. Outcome: 4 of 10 named failing tests recovered (Gates 1, 1a, 28, 33); 6 remaining blocked behind 2 surfaced production-code findings.
7. **Studio 1a-i.B** (`0d4d9a8`, 2026-05-13) — editor adaptation pass; third and final sub-arc of Studio 1a-i.  New `StudioRailContext` (`frontend/src/bridgeable-admin/components/studio/StudioRailContext.tsx`) exposes `{ railExpanded, inStudioContext }` from StudioShell to all descendants. `StudioShell` wraps its render tree in `<StudioRailContext.Provider value={{ railExpanded, inStudioContext: true }}>`. Nine visual editors consume `useStudioRail()` and compute `hideLeftPane = railExpanded && inStudioContext`. Six adapt their hide-able left pane via `{!hideLeftPane && (<aside>...</aside>)}` (FocusEditorPage, DocumentsEditorPage, ClassEditorPage, WidgetEditorPage individual mode, WorkflowEditorPage, EdgePanelEditorPage, ThemeEditorPage); three editors that participate in CSS grid layouts (Themes, Workflows, Edge Panels) additionally toggle the grid template (`grid-cols-[280px_minmax(0,1fr)_minmax(0,1.2fr)]` ↔ `grid-cols-[minmax(0,1fr)_minmax(0,1.2fr)]` etc.) so the absent pane doesn't leave dead column space. RegistryDebugPage + PluginRegistryBrowser call `useStudioRail()` for pattern uniformity as no-op consumers — single-pane surfaces with no separate left pane to hide. Standalone-mount callers (legacy `/visual-editor/*` redirects, unit tests, direct route mounts) keep their left pane visible because the default context value is `{ railExpanded: false, inStudioContext: false }`. Tests: 4 StudioRailContext smoke tests + 19 per-editor adaptation tests in `studio-editor-adaptation.test.tsx` (covers all 9 editors; 2 no-op consumer tests + 17 hide/show tests across 7 editors) + 5 StudioShell rail-context propagation extensions (FocusEditor stub upgraded to read `useStudioRail()` and expose data attributes so the integration test verifies provider plumbing through to descendants on both rail-expand and rail-collapse paths). Full vitest 2395/2395 green; tsc clean; build clean 4.81s; main bundle 5,987,470 bytes (+806 bytes / +0.013% vs refreshed baseline 5,986,664 — comfortably within ±2% gate). **~819 LOC** across 3 new files (StudioRailContext.tsx + 2 test files = 546 lines) + 11 modified files (273 insertions, 113 deletions). 4× under midpoint estimate (1,200), 4.4× under worst-case (1,840). **Studio 1a-i closes** — A1 (shell substrate), A2 (Live mode wrap), B (editor adaptation) all shipped. Spec-Override note: WidgetClassEditor (400px left aside in class-mode) was intentionally NOT adapted per prompt's explicit "only individual mode has a pane to hide" guidance; future hardening could extend rail-adaptation to class-mode + dashboard-layouts modes if operator signal warrants.
8. **Bundle baseline refresh** (`55c84fb`, 2026-05-13) — `fixtures/bundle-baseline.json` refreshed against current HEAD. Prior R-2.1 baseline (5,567,053 bytes, 2026-05-08) preserved as `_previous_baseline`. New main_bundle baseline 5,986,664 bytes (+7.54% absorbing Arcs 4a/4a.1/4a.2a/4b.1a/4b.1b/4b.2a/4b.2b/4c/4d + Studio 1a-i.A1 shell substrate; A2's eager-import drift fully recovered by the `a9d26f0` lazy-boundary commit). CSS baseline 233,872 bytes (+0.97%); chart_renderer unchanged at 399,346. `_baseline_commit` advanced to `2c24a79`. No production code changes; fixture refresh only. Closes the Gate 11 silent-failure window that opened ~May 8.
9. **Studio test maintenance** (`a9d26f0`, 2026-05-13) — between-arcs cleanup after Studio 1a-i.A2 (A2 Playwright run surfaced two gate failures). **Part 1**: `StudioLiveModeWrap` converted to `React.lazy()` + `Suspense` around `RuntimeEditorShell` import (mirrors the lazy pattern used in `BridgeableAdminApp` for `StudioShell` + `RuntimeHostTestPage`). Pre-fix, eager import made `RuntimeEditorShell` (and its `TenantProviders`/`TenantRouteTree`/inspector substrate transitively) part of the main admin bundle on every admin page load; post-fix, `RuntimeEditorShell-*.js` chunk splits cleanly at 112.87 kB (gzip 25.82 kB) and only loads when an operator enters Studio Live mode. **Part 2**: two E2E tests touched. `tests/e2e/runtime-editor/28-edge-panel-editor-page.spec.ts` second test rewritten — Phase 1 nav-card `ve-card-edge-panels` on the legacy `/visual-editor` index page no longer renders because that route redirects to `/studio` overview; the equivalent intent ("edge panels editor reachable from index surface") now asserts on `studio-overview-card-edge-panels` (the Studio overview's section card test-id). `tests/e2e/runtime-host-r0.spec.ts` URL assertion broadened from `expect(url).toContain("/visual-editor/widgets")` to `expect(url).toMatch(/\/(visual-editor|studio)\/widgets/)` because `/visual-editor/widgets` redirects to `/studio/widgets`. **Part 3**: bundle baseline verification surfaces a separate finding — the R-2.1 baseline (5,567,053 bytes, May 8) is now 7.54% under the post-A2 main bundle, but a stash-restore comparison against pre-Studio commit `9fbcab5` shows main_bundle was already 5,944,397 bytes (+6.77%) before Studio shell shipped. The 7.5% gap is accumulated growth across Arcs 4a-4d, NOT introduced by Studio 1a-i.A1 or A2. Gate 11 bundle-baseline refresh is a separate decision (out of scope for this maintenance arc) — `fixtures/bundle-baseline.json` intentionally not touched. **No production code changed outside the lazy boundary on `StudioLiveModeWrap.tsx`.** Tests: `StudioLiveModeWrap.test.tsx` (5 tests) + 2 tests in `StudioShell.test.tsx` (Live mode mounts + vertical forwards) updated to `findByTestId` for Suspense resolution; vitest 2367/2367 green, tsc clean, build 5.29s. **~90 LOC** touched across 5 files.
10. **Studio 1a-i.A2** (`a29be51`, 2026-05-13) — Live mode wrap for Bridgeable Studio. `StudioLiveModeWrap` mounts the existing `RuntimeEditorShell` inside Studio with new optional props `studioContext` (suppresses the yellow admin ribbon since Studio's own top bar replaces it) + `verticalFilter` (pre-scopes the tenant picker via `Company.vertical` client-side filter). `TenantPicker` gained `verticalFilter` prop; `TenantUserPicker` gained `studioContext` + `verticalFilter` props and now navigates to the Studio-shaped `/studio/live/:vertical?tenant=...&user=...` URL on impersonation success when in Studio context. New `toggleMode(pathname, search)` helper in `studio-routes.ts` implements the 5 canonical Edit ↔ Live translation rules (tenant params drop on Live → Edit, no trailing slash on `/studio/live` → `/studio`, tolerates `/bridgeable-admin` prefix). `StudioModeToggle` wired to use `toggleMode()` + `useLocation()`. `StudioScopeSwitcher` gained `readOnly` + `liveModeDescription` props rendering a static "Vertical: X" readout instead of the dropdown in Live mode; `StudioTopBar` passes these in. `/runtime-editor` + `/bridgeable-admin/runtime-editor` (and `/*` variants) installed as `<StudioRedirect />` routes — the A1 spec-override that kept those active is now resolved; standalone runtime editor entries forward to `/studio/live` preserving `?tenant=&user=`. `StudioLivePlaceholderPage` deleted; `StudioShell` dispatches `/studio/live[/:vertical]` to `StudioLiveModeWrap`. Tests: 5 new `StudioLiveModeWrap` smoke tests, 5 new Live-mode tests in `StudioShell.test.tsx` (mock RuntimeEditorShell stub), 8 new `toggleMode` translation tests; existing 2367 vitest tests pass. tsc clean, build clean 5.22s. Backend untouched. **~446 LOC** (well under the 1,900 LOC worst-case budget; 4× headroom).

## Open questions / blockers

- **Sage CSV exports** — blocked on Sunnycrest accountant providing invoice history, customer list, cash receipts files. Once received, Data Migration Tool can complete.
- **Pre-existing bugs** catalogued in `backend/docs/BUGS.md` (separate file) — `quote_service` calls `audit_service.log` but function is `log_action` (BUGS.md entry #7), unrelated to active arc.
