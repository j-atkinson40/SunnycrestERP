/**
 * Arc 4d — source-badge public surface.
 *
 * Tenth canonical shared authoring component primitive (SourceBadge)
 * + eleventh canonical candidate co-located (ScopeDiffPopover, per
 * extract-when-second-consumer-emerges canon — single consumer today
 * is the inspector-tab wrapping integration).
 *
 * Imported by 6 inspector tabs (ClassTab, PropsTab, ThemeTab,
 * WorkflowsTab, DocumentsTab, FocusCompositionsTab) for canonical
 * scope-source visualization with hover-reveal scope-cascade diff.
 */
export { SourceBadge } from "./SourceBadge"
export type {
  SourceBadgeProps,
  SourceBadgeVariant,
  SourceValue,
} from "./SourceBadge"
export { ScopeDiffPopover } from "./ScopeDiffPopover"
export type {
  ScopeDiffPopoverProps,
  ResolutionScope,
  ResolutionSourceEntry,
} from "./ScopeDiffPopover"
