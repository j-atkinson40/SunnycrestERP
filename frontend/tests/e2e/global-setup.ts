/**
 * Playwright global setup — runs once before all tests.
 *
 * Future use: seed test data, warm caches, verify staging health, etc.
 */
export default async function globalSetup() {
  // Intentionally empty — placeholder for future pre-test setup
}
