/**
 * Playwright global teardown — runs once after all tests.
 *
 * Future use: cleanup test data, close connections, etc.
 */
export default async function globalTeardown() {
  // Intentionally empty — placeholder for future post-test cleanup
}
